---
phase: 06-security-performance-accessibility-release
plan: "04"
type: execute
wave: 2
depends_on: ["06-01", "06-02"]
files_modified:
  - docs/release/risk-evidence.json
  - scripts/check-risk-evidence.mjs
  - tests/release/risk-evidence.test.ts
  - docs/release/gaps/RISK-GAPS.md
  - scripts/audit-release-dependencies.mjs
  - docs/release/dependency-inventory.md
  - THIRD_PARTY_NOTICES.md
autonomous: true
requirements: [SEC-01, SEC-02, REL-01]
must_haves:
  truths:
    - "PF-01 through PF-14 each map exactly once to named, runnable prevention and recovery tests, and deterministic fixtures prove the checker rejects every invalid evidence state."
    - "Missing, stale, skipped/fixme/conditional, unresolved, renamed, or digest-mismatched evidence fails the release gate."
    - "Every shipped dependency has exact purpose/version/license/notice/runtime reachability/advisory disposition; unresolved Critical/High blocks release."
  artifacts:
    - path: "docs/release/risk-evidence.json"
      provides: "Machine-checked PF-01..PF-14 prevention/recovery evidence manifest"
    - path: "scripts/check-risk-evidence.mjs"
      provides: "Freshness, result, skip, test-ID, digest, and resolution gate"
    - path: "scripts/audit-release-dependencies.mjs"
      provides: "Deterministic dependency/license/advisory inventory gate"
  key_links:
    - from: "docs/release/risk-evidence.json"
      to: "test source and runner result artifacts"
      via: "exact file, testId, command, file SHA-256, commit SHA, outcome, timestamp"
      pattern: "PF-14"
    - from: "scripts/check-risk-evidence.mjs"
      to: "tests/release/risk-evidence.test.ts"
      via: "deterministic current/stale commit, source-digest, result, skip, retry, and timestamp fixtures"
      pattern: "sha256"
  prohibitions:
    - "No prose-only risk closure, glob-only test reference, hidden skip/fixme/retry, accepted stale result, or unresolved Critical/High."
    - "No production repair in this audit plan; failures go to RISK-GAPS.md for an explicit file-owning gap-closure plan."
    - "No guessed license, package installation, advisory suppression, or network-unavailable clean claim."
---

<objective>
Make the research risk matrix/checker contract and shipped dependency set mechanically auditable before the release run.

Purpose: A passing suite is insufficient unless every named PF risk has current prevention and recovery evidence and every shipped dependency is understood.
Output: Exact PF evidence manifest, fixture-proven checker, gap artifact, and deterministic dependency/license/notice/advisory inventory. The current release result artifact is deliberately produced later by 06-03.
</objective>

<execution_context>@/Users/aneesahammed/.codex/gsd-core/workflows/execute-plan.md</execution_context>

<context>
@.planning/research/PITFALLS.md
@.planning/research/STACK.md
@.planning/phases/06-security-performance-accessibility-release/06-VALIDATION.md
@.planning/phases/06-security-performance-accessibility-release/06-01-SUMMARY.md
@.planning/phases/06-security-performance-accessibility-release/06-02-SUMMARY.md
@package.json
@package-lock.json
</context>

<artifacts_produced>
| Artifact | Provides |
|---|---|
| `risk-evidence.json` | one authoritative PF-01..14 prevention/recovery mapping |
| `check-risk-evidence.mjs`, test | source/result freshness and completeness enforcement |
| `RISK-GAPS.md` | exact machine-generated unresolved evidence gaps |
| dependency inventory/notices | exact shipped dependency and license/advisory record |
</artifacts_produced>

<file_ownership>
- Task 06-04-01 owns only the risk manifest/checker/checker-test/gap report.
- Task 06-04-02 owns only the dependency audit and generated inventory/notices.
- Audit failures do not authorize edits to referenced production or test files; they fail into `RISK-GAPS.md` or a dependency finding for an explicit closure plan.
</file_ownership>

<tasks>

<task type="auto" tdd="true">
  <name>Task 06-04-01: Gate PF-01..PF-14 with fresh named prevention and recovery evidence</name>
  <read_first>
    - `.planning/research/PITFALLS.md` — authoritative PF-01..PF-14 definitions, severity, prevention, recovery
    - `06-01-SUMMARY.md`, `06-02-SUMMARY.md`, Phase 1–5 summaries — candidate completed evidence
    - Playwright/Vitest result formats and repository skip/retry policies
  </read_first>
  <files>docs/release/risk-evidence.json, scripts/check-risk-evidence.mjs, tests/release/risk-evidence.test.ts, docs/release/gaps/RISK-GAPS.md</files>
  <behavior>
    - Manifest contains the exact set PF-01..PF-14 once each with severity, status=`resolved`, source, at least one prevention reference, and at least one recovery reference.
    - Every reference names an exact repository file, exact test ID/title, command, expected assertion, source SHA-256, and result key; deterministic runner fixtures model current/stale git SHA, test-file digest, pass outcome, attempt/retry/skip state, and timestamp.
    - Checker rejects missing/duplicate/unknown PF IDs, missing files/tests/results, stale commit or file digest, old timestamp, fail/skip/fixme/todo/conditional exclusion, retry-masked success, unresolved status, and absent prevention or recovery evidence.
  </behavior>
  <action>
Write checker contract tests first for every rejection case and a minimal valid manifest. Populate all fourteen PF entries from exact existing named tests—never a directory or prose claim. Parse supplied runner artifacts plus test source/metadata to expose hidden skips/fixmes/conditional project exclusion and reject a pass produced only after retry. With deterministic temporary fixtures, prove freshness means recorded commit equals the explicitly supplied release commit, each referenced test source SHA-256 matches fixture bytes, its named result passed, and its timestamp belongs to the supplied run. On failure regenerate `RISK-GAPS.md` deterministically with PF ID, missing criterion, owner, named test or file, required prevention/recovery evidence, then exit non-zero; do not run the complete release suite or require `.release-evidence/test-results.json` in this plan. 06-03 alone creates and checks that real artifact.
  </action>
  <verify><automated>npm run test -- tests/release/risk-evidence.test.ts</automated></verify>
  <acceptance_criteria>
    - Exactly PF-01..PF-14 are present and every one has a passing named prevention and recovery test.
    - Mutation tests prove each missing/stale/skipped/fixme/retried/unresolved/digest-mismatched variant exits non-zero and identifies the exact PF criterion.
    - A deterministic valid runner fixture passes; no current release-run artifact is a prerequisite or output of 06-04.
  </acceptance_criteria>
  <failure_modes>
    - Renamed tests fail until manifest and fresh runner evidence agree; fuzzy title matching is forbidden.
    - Shallow/no-git environments cannot claim freshness; the gate reports incomplete and blocks release.
    - Recovery means a safe valid open/query after the induced fault, not merely an error toast or process restart.
  </failure_modes>
  <done>Every PF risk has machine-verifiable fresh prevention and recovery proof.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 06-04-02: Produce and gate runtime dependency, license, notice, and advisory inventory</name>
  <read_first>
    - `package.json`, `package-lock.json` — exact dependency graph
    - `.planning/research/STACK.md` — approved purpose/version and risk register
    - built `dist` asset graph and installed package license files — actual shipped inclusion and authoritative metadata
  </read_first>
  <files>scripts/audit-release-dependencies.mjs, docs/release/dependency-inventory.md, THIRD_PARTY_NOTICES.md</files>
  <behavior>
    - Lockfile and dist graph produce one stable runtime inventory with exact versions, purpose, reachability, license/notice sources, chunks, request behavior, and advisory status.
    - Unknown/prohibited license, graph drift, unexpected remote request, unresolved Critical/High, or unavailable fresh advisory result fails closed.
  </behavior>
  <action>
Write deterministic audit fixtures, then map every shipped direct/transitive package to exact version, runtime/dev reachability, purpose, license expression/source, required notice, built chunk, remote-request behavior, and advisory status. Generate stable docs from the lockfile, installed metadata, and dist graph; `--check` fails drift, unknown/missing/prohibited license, unexpected runtime package/request, or Critical/High without a linked approved mitigation and passing regression. Network/audit-service failure is `incomplete`, never clean; do not install or suppress a package here.
  </action>
  <verify><automated>node scripts/audit-release-dependencies.mjs --check && npm audit --omit=dev --audit-level=high</automated></verify>
  <acceptance_criteria>
    - Every shipped runtime package/chunk has purpose, exact version, license/notice source, request behavior, and disposition.
    - Generation is idempotent and `--check` catches lockfile, bundle, license, or notice drift.
    - No unresolved Critical/High advisory or unknown/prohibited license remains.
  </acceptance_criteria>
  <failure_modes>
    - Missing/malformed metadata fails for human review and an explicit closure plan; it is not guessed.
    - Dev-only packages are not called shipped unless the dist graph includes them.
    - Registry unavailability blocks release rather than reusing an undated result.
  </failure_modes>
  <done>Dependency and license evidence is exact, current, reproducible, and release-gated.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation plan |
|---|---|---|---|---|
| T-06-04-01 | Repudiation | PF risk closure | mitigate | Exact named tests, current commit/digests/results, skip/retry rejection |
| T-06-04-02 | Tampering | Evidence manifest | mitigate | Exact-set schema, deterministic checker tests, generated gap report |
| T-06-04-SC | Tampering | Dependency supply chain | mitigate | Lockfile/dist/license/advisory audit; High/Critical block |
</threat_model>

<source_audit>
| Source | ID | Item | Task | Status |
|---|---|---|---|---|
| RESEARCH | PF-01..PF-14 | Named prevention and recovery proof | 01 | COVERED |
| REQ | SEC-01/02 | Security/privacy/dependency evidence | 01,02 | COVERED |
| REQ | REL-01 | Fresh complete machine evidence | 01,02 | COVERED |
| CONTEXT | D-45/D-46 | Audit and Critical/High closure | 01,02 | COVERED |
</source_audit>

<verification>
Run both focused commands and mutation fixtures for every checker rejection. Do not invoke the checker against a current complete release runner artifact here; 06-03 owns suite execution and that ordering. Both manifest-definition gaps and dependency audit findings must be resolved.
</verification>

<success_criteria>
- PF-01..14 each have exact named prevention/recovery references, and fixtures prove fresh passing evidence is accepted.
- Missing/stale/skipped/retried/unresolved evidence always fails closed.
- Shipped dependencies and notices are complete with no unresolved Critical/High or license uncertainty.
</success_criteria>

<output>Create `06-04-SUMMARY.md` with the PF manifest digest, deterministic mutation-gate results, explicit handoff contract for 06-03, dependency inventory digest, advisory/license result, and closed definition-gap IDs.</output>
