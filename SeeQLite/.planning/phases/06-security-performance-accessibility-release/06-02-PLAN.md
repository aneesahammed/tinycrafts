---
phase: 06-security-performance-accessibility-release
plan: "02"
type: execute
wave: 1
depends_on: ["05-01", "05-02", "05-03"]
files_modified:
  - tests/performance/budgets.json
  - tests/performance/release-benchmarks.spec.ts
  - tests/performance/scalability.spec.ts
  - tests/performance/helpers/large-db-fixture.ts
  - scripts/check-seeqlite-bundle.mjs
  - tests/e2e/accessibility.spec.ts
  - tests/e2e/keyboard-workflows.spec.ts
  - tests/e2e/visual.spec.ts
  - tests/e2e/fixtures/accessibility-states.ts
  - docs/release/performance.md
  - docs/release/gaps/PERFORMANCE-GAPS.md
  - docs/release/gaps/ACCESSIBILITY-GAPS.md
autonomous: true
requirements: [PERF-01, A11Y-01, REL-01, REL-02]
must_haves:
  truths:
    - "Measured shell/import/query/result/graph/cancel/rehydration/cache/bundle workloads meet ratcheted browser budgets or enter the documented safe limited state without a freeze."
    - "No safety/file/result/catalog/graph limit is raised merely to pass a benchmark; changes require measured ADR and regression evidence."
    - "Keyboard, axe, contrast, focus, zoom, reflow, reduced motion, forced colors, light, and dark workflows pass every primary feature."
    - "Chromium, Firefox, and WebKit each enforce environment-qualified timing where stable and hard safe-degradation/structural bounds everywhere."
  artifacts:
    - path: "tests/performance/budgets.json"
      provides: "Final environment-qualified ratcheted release budgets"
    - path: "tests/performance/release-benchmarks.spec.ts"
      provides: "Repeatable production timing/transfer/DOM measurements"
    - path: "tests/e2e/accessibility.spec.ts"
      provides: "Axe, contrast, zoom, reduced-motion, forced-colors matrix"
  key_links:
    - from: "tests/performance/release-benchmarks.spec.ts"
      to: "tests/performance/budgets.json"
      via: "browser/environment-specific metric lookup and hard assertion"
      pattern: "budgets.json"
    - from: "tests/e2e/accessibility.spec.ts"
      to: "tests/e2e/fixtures/accessibility-states.ts"
      via: "deterministic all-phase state matrix"
      pattern: "accessibilityStates"
  prohibitions:
    - "No cap increase, accessibility-text removal, target-size reduction, graph-list removal, hidden skip, automatic retry, or Chromium-only release claim to pass a budget."
    - "No analytics/telemetry, synthetic user data, or unrecorded manual checkbox."
    - "No audit task may edit an undeclared production file; failures are written to a gap artifact and require an explicit gap-closure plan."
---

<objective>
Ratchet browser performance/scalability and prove the complete accessible workflow automatically in all supported browser engines.

Purpose: The finished feature set must remain responsive and operable rather than trading safety/accessibility for benchmark success.
Output: Final budgets and benchmark report, complete axe/keyboard/visual matrix, deterministic large-file measurements, bundle gate, and explicit gap artifacts.
</objective>

<execution_context>@/Users/aneesahammed/.codex/gsd-core/workflows/execute-plan.md</execution_context>

<context>
@.planning/phases/06-security-performance-accessibility-release/06-CONTEXT.md
@.planning/phases/06-security-performance-accessibility-release/06-UI-SPEC.md
@.planning/phases/06-security-performance-accessibility-release/06-VALIDATION.md
@.planning/research/SUMMARY.md
@.planning/research/PITFALLS.md
@tests/performance/baseline.json
@playwright.config.ts
</context>

<artifacts_produced>
| Artifact | Provides |
|---|---|
| `budgets.json` + `performance.md` | final environment, samples, percentile/budget, safe-degradation result |
| `release-benchmarks.spec.ts`, `scalability.spec.ts`, `large-db-fixture.ts` | timing plus 64/128/256 MiB import/reopen and bounded transfer/DOM/cache assertions |
| accessibility/keyboard/visual specs | automated all-phase a11y/theme/responsive evidence |
| `PERFORMANCE-GAPS.md`, `ACCESSIBILITY-GAPS.md` | exact blocking findings for separate declared gap-closure plans |
</artifacts_produced>

<file_ownership>
- Task 06-02-01 owns only the five performance/bundle files, `large-db-fixture.ts`, `performance.md`, and `PERFORMANCE-GAPS.md`.
- Task 06-02-02 owns only the four accessibility/visual files and `ACCESSIBILITY-GAPS.md`.
- These are audit/gate tasks, not broad repair authority. A failure outside a task's declared files is recorded with owner, reproduction, severity, prevention test, recovery test, and proposed files; the task fails until a separate gap-closure plan is approved.
</file_ownership>

<scope_rationale>
The 12 files comprise one automated three-engine release-quality capture using the same production artifact, environment qualification, deterministic state matrix, and safe-degradation policy. Performance and accessibility files never cross task ownership; either task's production defect exits through its dedicated gap artifact, so further splitting would duplicate the artifact/environment handoff without reducing edit authority.
</scope_rationale>

<tasks>

<task type="auto" tdd="true">
  <name>Task 06-02-01: Ratchet performance, bundle, and scalability budgets without weakening limits</name>
  <read_first>
    - `.planning/phases/06-security-performance-accessibility-release/06-CONTEXT.md` — D-47, D-49
    - `tests/performance/baseline.json` — Phase 1 environment/provisional metrics
    - current central limit constants and graph/result/file/catalog policies — immutable safety baselines
    - Phase 3/4/5 benchmark summaries — cancel, result, graph, export, cache evidence
  </read_first>
  <files>tests/performance/budgets.json, tests/performance/release-benchmarks.spec.ts, tests/performance/scalability.spec.ts, tests/performance/helpers/large-db-fixture.ts, scripts/check-seeqlite-bundle.mjs, docs/release/performance.md, docs/release/gaps/PERFORMANCE-GAPS.md</files>
  <behavior>
    - Measure cold/warm readiness, 25/50 MiB catalog, 1,000×20 query-to-paint, sort/export, 25/75/150/500 nodes, cancel acknowledgement, rehydration, chunks/bundle, DOM rows, transfer sizes, and cache growth.
    - Use documented per-engine/reference-environment samples/percentiles; cancel acknowledgement remains ≤250 ms and separate from rehydration.
    - In Chromium, Firefox, and WebKit, generate and measure exact 64, 128, and 256 MiB SQLite imports, close/reopen from the original handle, and a second reimport; record readiness, peak/available memory when observable, worker transfer, long tasks, result state, and recovery query.
    - Over-limit fixtures warn/limit/subgraph/fail explicitly; no unbounded transfer/render, long retry loop, or second result page in DOM.
  </behavior>
  <action>
Implement D-47/D-49 by importing Phase 1 baselines and recording final budgets keyed by engine, OS, CPU class, memory class, headless/headed mode, CI image, and sample statistic. Run cold/warm samples without telemetry. Generate valid deterministic 64/128/256 MiB databases at test time with the existing fixture factory (no committed giant binary and no Python/package install), then measure initial import, close/reopen, and second reimport in Chromium, Firefox, and WebKit. Enforce timing thresholds where that engine/environment has a qualified baseline. Where timing is intrinsically noisy or an API is unavailable, the manifest must name the unsupported metric and still hard-fail on safe-degradation, bounded transfer/DOM/cache, no freeze/crash, explicit user state, cancel/recovery, and successful `SELECT 1`; it may not downgrade the whole engine to informational. A provisional limit may stay or tighten; if a production defect is discovered, write it to `PERFORMANCE-GAPS.md` and stop for a declared gap-closure plan rather than editing its owner here. Any cap increase requires a separate ADR/plan, before/after traces, resource proof, and regression—never edit budget alone. Bundle checker asserts initial shell excludes editor/graph/ELK, assets are split, and configured budgets cover JS/CSS/fonts/workers/WASM. Produce a deterministic report with no user data.
  </action>
  <verify><automated>npm run test:performance -- --project=chromium --project=firefox --project=webkit && npm run check:bundle</automated></verify>
  <acceptance_criteria>
    - PERF-01/UI6-AC04 pass with no provisional/null/unqualified budget.
    - Fixed safety caps are unchanged unless an approved ADR/evidence/regression exists.
    - Cross-engine scalability outcomes activate documented safe modes; timing gates and environmental variance are explicit.
    - The report contains import/reopen/reimport measurements for 64/128/256 MiB in each engine, or an engine-qualified unsupported metric with all safe-degradation assertions passing.
    - No full main-thread DB clone, over-cap result/BLOB, >50 result rows in DOM, whole 150/500 graph, or runtime cache growth.
  </acceptance_criteria>
  <failure_modes>
    - No retries or discarded slow samples; warmup/sample exclusion rules are fixed before execution.
    - Unavailable memory API records `not measurable` only for that metric while structural transfer/DOM bounds remain hard.
    - Budget failure blocks release; it cannot be converted to informational by task code.
  </failure_modes>
  <done>Performance/scalability claims are measured, ratcheted, bounded, and reproducible.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 06-02-02: Automate the complete keyboard, axe, focus, theme, zoom, motion, and visual matrix</name>
  <read_first>
    - all `01-UI-SPEC.md` through `06-UI-SPEC.md` — exact owning feature states/focus/copy
    - existing Phase 1–5 E2E page objects — reuse semantic actions
    - `tests/e2e/visual.spec.ts` and axe helpers — current snapshot/violation policy
  </read_first>
  <files>tests/e2e/fixtures/accessibility-states.ts, tests/e2e/accessibility.spec.ts, tests/e2e/keyboard-workflows.spec.ts, tests/e2e/visual.spec.ts, docs/release/gaps/ACCESSIBILITY-GAPS.md</files>
  <behavior>
    - Axe zero serious/critical in both themes at 375/768/1024/1440 for initial, catalog/error/limited, query/result/plan/cancel, ER/no-FK/unresolved/subgraph/failure, export, offline/update, privacy states.
    - Keyboard completes open/inspect/search/run/result/plan/both stops/recovery/history/ER list/join/export/update/privacy with exact focus/announcements and no canvas/pointer dependency.
    - 200% zoom, forced colors, reduced motion, IME, horizontal scrollers, 44px narrow targets, contrast, and responsive state/focus preservation pass.
  </behavior>
  <action>
Implement D-48/D-49 as deterministic state fixtures and user-level Playwright tests. Use roles/names and observable focus/live output; do not call component internals. Run light/dark and viewport matrices with fonts loaded/animation disabled; mask only timestamps/OS antialiasing, never copy/focus/warnings/edges/results. Add contrast assertions for approved semantic tokens and non-color meaning. This audit may change only its declared tests/fixtures: write any production defect to `ACCESSIBILITY-GAPS.md` with exact reproduction, affected requirement, severity, owner, and required lowest-layer regression, then fail for a separate explicit gap-closure plan. Do not hide text or relax target/contrast thresholds.
  </action>
  <verify><automated>npm run test:e2e -- tests/e2e/accessibility.spec.ts tests/e2e/keyboard-workflows.spec.ts tests/e2e/visual.spec.ts</automated></verify>
  <acceptance_criteria>
    - A11Y-01/UI6-AC05/06/10 pass in three engines with zero serious/critical axe violations.
    - Every primary path is keyboard-complete and canvas-independent, with defined focus/announcements.
    - Both themes and all widths/zoom have no page overflow, clipped focus, unreachable action, or information loss.
    - Visual baselines include full TinyCrafts/SeeQLite context and documented before/after for intentional changes.
  </acceptance_criteria>
  <failure_modes>
    - Engine-specific axe/keyboard failure remains blocking; no blanket browser exclusion.
    - Snapshot update without semantic/contrast/focus review is rejected.
    - Responsive mode removing the focused target must move to the specified stable replacement, never body.
  </failure_modes>
  <done>The complete automated accessible/visual workflow is release-gated across themes, widths, and engines.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation plan |
|---|---|---|---|---|
| T-06-02-01 | Denial of service | File/query/result/graph/export/cache | mitigate | Ratcheted metrics plus hard structural/safe-degradation assertions |
| T-06-02-02 | Elevation of privilege | Inaccessible pointer/canvas-only UI | mitigate | Equivalent keyboard/list paths, axe/AT/manual evidence |
| T-06-02-03 | Repudiation | Performance/a11y claims | mitigate | Immutable environment-qualified reports and explicit gap artifacts |
| T-06-02-SC | Tampering | Test/package supply chain | mitigate | Existing pinned tools only; no dependency changes/hidden skips/retries |
</threat_model>

<source_audit>
| Source | ID | Item | Task | Status |
|---|---|---|---|---|
| GOAL | — | Responsiveness/accessibility/browser proof | all | COVERED |
| REQ | PERF-01 | Measured budgets/degradation | 01 | COVERED |
| REQ | A11Y-01 | Automated accessible workflow | 02 | COVERED |
| REQ | REL-01 | Automated release evidence | 01,02 | COVERED |
| REQ | REL-02 | Cross-engine production evidence | 01,02 | COVERED |
| RESEARCH | PF-03,08,13,14 | bounds/graph/a11y/real-browser risks | all | COVERED |
| CONTEXT | D-47..D-49 | Ratchet/manual/safe limits | all | COVERED |
| UI | UI6-AC04..07,10 | metrics/a11y/manual/full layouts | all | COVERED |
</source_audit>

<verification>
Run all task commands, then `npm run test:performance -- --project=chromium --project=firefox --project=webkit && npm run check:bundle && npm run test:e2e -- tests/e2e/accessibility.spec.ts tests/e2e/keyboard-workflows.spec.ts tests/e2e/visual.spec.ts`. Any unresolved performance/accessibility gap blocks the phase.
</verification>

<success_criteria>
- PERF-01/A11Y-01/assigned REL evidence passes without weakened caps or accessibility.
- All supported engines, themes, widths, zoom/motion/forced-colors states meet budgets and primary-workflow outcomes.
- Every production defect is closed by an explicit owning plan with automated regression and rerun proof.
</success_criteria>

<output>Create `06-02-SUMMARY.md` with per-engine budgets, 64/128/256 MiB import/reopen/reimport samples, safe-mode results, axe/visual/keyboard matrix, gap disposition, and residual limitations.</output>
