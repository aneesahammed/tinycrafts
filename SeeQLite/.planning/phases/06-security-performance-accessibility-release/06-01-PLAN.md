---
phase: 06-security-performance-accessibility-release
plan: "01"
type: execute
wave: 1
depends_on: ["05-01", "05-02", "05-03"]
files_modified:
  - index.html
  - src/features/privacy/PrivacyAndLimitsDialog.tsx
  - src/features/privacy/PrivacyAndLimitsDialog.test.tsx
  - src/features/privacy/privacy.css
  - src/app/AppHeader.tsx
  - tests/security/hostile-rendering.test.ts
  - tests/e2e/security/hostile-content.spec.ts
  - tests/e2e/security/privacy-readonly.spec.ts
  - tests/e2e/helpers/privacy-audit.ts
  - scripts/check-security-invariants.mjs
  - docs/release/gaps/SECURITY-GAPS.md
autonomous: true
requirements: [SEC-01, SEC-02, REL-01]
must_haves:
  truths:
    - "Every database-derived filename, identifier, SQL definition, error, plan detail, relationship label, and value remains bounded inert text under the production CSP."
    - "Unique derived markers occur only in permitted live UI, bounded SQL history, and explicit downloads—not network, URL, logs, caches, service-worker metadata, or unrelated persistence."
    - "The source file hash is unchanged and denied SQL/read-only policy still works after every hostile/error workflow."
    - "Public privacy and limits copy states only protections proven by the artifact and deployed headers."
  artifacts:
    - path: "src/features/privacy/PrivacyAndLimitsDialog.tsx"
      provides: "Proven privacy statement and ten required limitations"
      exports: ["PrivacyAndLimitsDialog"]
    - path: "tests/e2e/security/hostile-content.spec.ts"
      provides: "All-surface inert-rendering/CSP evidence"
    - path: "tests/e2e/security/privacy-readonly.spec.ts"
      provides: "Marker/source-byte/read-only/recovery evidence"
  key_links:
    - from: "index.html"
      to: "built scripts/workers/WASM/fonts/assets"
      via: "measured minimal meta CSP"
      pattern: "Content-Security-Policy"
    - from: "src/app/AppHeader.tsx"
      to: "src/features/privacy/PrivacyAndLimitsDialog.tsx"
      via: "always-visible Privacy & limits action"
      pattern: "Privacy & limits"
    - from: "tests/e2e/security/privacy-readonly.spec.ts"
      to: "tests/e2e/helpers/privacy-audit.ts"
      via: "one marker audit across all storage/network/log/cache boundaries"
      pattern: "auditPrivacyMarkers"
  prohibitions:
    - "No raw HTML/parser sink, data/cross-origin worker, remote runtime service/font/icon, analytics, crash reporter, or extension loading."
    - "No `unsafe-eval`, header-only claim absent from deployed Pages, absolute security claim, or database-derived diagnostic logging."
    - "No new package, accepted Critical/High advisory without explicit mitigation, or user-data fixture."
    - "Audit tasks may edit only their declared files; a defect elsewhere is recorded in SECURITY-GAPS.md and requires an explicit gap-closure plan."
---

<objective>
Close production content-execution, privacy, source-integrity, read-only, dependency, and public-claim evidence gaps.

Purpose: SeeQLite's local/read-only promise must be demonstrable across hostile data and failure paths, not inferred from browser architecture.
Output: Minimal measured CSP, Privacy & limits surface, hostile/marker suites, invariant scanner, and machine-readable security gap report.
</objective>

<execution_context>@/Users/aneesahammed/.codex/gsd-core/workflows/execute-plan.md</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-security-performance-accessibility-release/06-CONTEXT.md
@.planning/phases/06-security-performance-accessibility-release/06-UI-SPEC.md
@.planning/phases/06-security-performance-accessibility-release/06-VALIDATION.md
@.planning/research/PITFALLS.md
@index.html
@vite.config.ts
@package-lock.json
</context>

<artifacts_produced>
| Artifact | Symbols/content | Release consumer |
|---|---|---|
| `PrivacyAndLimitsDialog.tsx` | `PrivacyAndLimitsDialog`, ten limitations | every viewport/help + copy checks |
| `hostile-content.spec.ts` | inert DOM/CSP/URL/request matrix | SEC-01/release gate |
| `privacy-readonly.spec.ts` | marker/source hash/denial/recovery matrix | SEC-02/release gate |
| `SECURITY-GAPS.md` | exact out-of-scope production defects and required closure tests | Phase 6 gap-closure planning |
</artifacts_produced>

<file_ownership>
- Task 06-01-01 owns only `index.html`, `src/features/privacy/**`, the declared `AppHeader.tsx`, its two declared hostile-rendering tests, and `SECURITY-GAPS.md` for out-of-scope findings.
- Task 06-01-02 owns only the privacy auditor, privacy/read-only E2E, invariant scanner, and `SECURITY-GAPS.md`.
- A failing audit never authorizes opportunistic edits to another production feature. Record file, finding, severity, reproduction, prevention test, recovery test, and required owner in `SECURITY-GAPS.md`; stop the task until a separate gap-closure plan declares those files.
</file_ownership>

<scope_rationale>
These 11 files are one final-artifact security claim: the declared Privacy/CSP surface and the hostile/marker/read-only proof must change together. Task-local ownership separates the small production surface from read-only audits and forces every other finding into `SECURITY-GAPS.md`.
</scope_rationale>

<tasks>

<task type="auto" tdd="true">
  <name>Task 06-01-01: Lock inert rendering, minimal CSP, and truthful Privacy & limits UI</name>
  <read_first>
    - `.planning/phases/06-security-performance-accessibility-release/06-CONTEXT.md` — D-44, D-46
    - `.planning/phases/06-security-performance-accessibility-release/06-UI-SPEC.md` — exact ten limitations, sink/status/focus contract
    - `index.html` and `vite.config.ts` — current CSP/assets/styles/workers
    - `src/app/AppHeader.tsx` — utility action slot
    - `src/features/catalog`, `src/features/query`, `src/features/er`, `src/features/export` renderers — all derived-data surfaces
  </read_first>
  <files>index.html, src/features/privacy/PrivacyAndLimitsDialog.tsx, src/features/privacy/PrivacyAndLimitsDialog.test.tsx, src/features/privacy/privacy.css, src/app/AppHeader.tsx, tests/security/hostile-rendering.test.ts, tests/e2e/security/hostile-content.spec.ts, docs/release/gaps/SECURITY-GAPS.md</files>
  <behavior>
    - Render HTML/script/event/CSS/URL/bidi/zero-width/newline/long markers as bounded isolated text across every surface; no node/attribute/URL/request executes.
    - CSP allows only measured same-origin script/worker/connect/font/img/manifest/style needs and WASM capability; object is none, base/form restricted; no broad eval/data worker.
    - Privacy & limits is reachable before/after open at every width, contains the proven body and all ten exact limitations, clears history through the existing flow, traps/restores focus, and has equal themes.
  </behavior>
  <action>
Write sink/copy tests first, then implement D-44/D-46 in the declared files. Audit all derived-data renderers read-only for text-node usage and bounded accessible attributes; isolate bidi with `dir=auto`/plaintext only where this task owns the file. If another renderer fails, write the exact finding and required prevention/recovery tests to `SECURITY-GAPS.md` and stop for an explicit gap-closure plan—do not edit it here. Set the narrowest production meta CSP that passes the built app, documenting why any inline-style allowance is required by measured graph positioning. Do not include/claim `frame-ancestors` in meta. Add the approved native dialog only—no settings route. E2E injects hostile values into every primary surface and asserts no unexpected element, event, URL, request, style effect, CSP violation, or raw stack.
  </action>
  <verify><automated>npm run test -- src/features/privacy/PrivacyAndLimitsDialog.test.tsx tests/security/hostile-rendering.test.ts && npm run build && npm run test:e2e -- tests/e2e/security/hostile-content.spec.ts --project=chromium</automated></verify>
  <acceptance_criteria>
    - SEC-01 and UI6-AC01/02 pass; CSP runs worker/WASM/editor/ER/export/offline without `unsafe-eval` or remote runtime origins.
    - All ten limitations and approved action labels pass exact-content tests in both themes/responsive modes.
    - Hostile markers remain inert/bounded and generate no URL/request/DOM/style execution.
  </acceptance_criteria>
  <failure_modes>
    - Required asset blocked by CSP shows the defined fatal/degraded state and fails release; policy is not broadened blindly.
    - Database text never enters `title`, unbounded ARIA/live text, selector data attributes, CSS content, or logs.
    - A host-header limitation is disclosed rather than imitated with an equivalent claim.
  </failure_modes>
  <done>Production content and CSP hardening are proven and public copy matches actual controls.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 06-01-02: Prove no-upload, source immutability, read-only policy, and recovery end to end</name>
  <read_first>
    - `tests/e2e/helpers/privacy-markers.ts` and Phase 1 privacy smoke — marker conventions
    - `src/db/read-only-policy.ts` — deny matrix/unknown action behavior
    - `src/db/database-client.ts` — worker epochs/recovery
    - `src/history/history-store.ts` — sole approved bounded SQL persistence
    - `src/pwa/cache-policy.ts` — app-only cache contract
  </read_first>
  <files>tests/e2e/helpers/privacy-audit.ts, tests/e2e/security/privacy-readonly.spec.ts, scripts/check-security-invariants.mjs, docs/release/gaps/SECURITY-GAPS.md</files>
  <behavior>
    - Unique filename/schema/SQL/value/error/export markers traverse every primary and error/cancel/offline flow.
    - Network/URLs/console/Cache Storage/SW metadata/unrelated localStorage/session/source maps exclude markers; bounded SQL history and explicit download are checked against their documented allowed fields.
    - Hash source bytes before/after denied DML/DDL/PRAGMA/attach/extension, allowed reads, cancel, timeout, export, update failure, close/reopen; the next `SELECT 1` succeeds after each recovery.
  </behavior>
  <action>
Implement D-45/D-46 as one reusable production privacy auditor and a real-WASM E2E matrix. Inventory requests/responses, console, page URL/history, storage keys/values, Cache Storage keys/bodies, SW messages, generated downloads, and source-byte hashes without echoing secret markers into failure logs. Re-run the Phase 2 deny-by-default matrix and Phase 3 cancel/timeout recovery under the final artifact. Add a static scanner for raw HTML sinks, deprecated SQLite worker APIs, extension-loading enablement, remote runtime URLs, forbidden logs, and dangerous worker URL schemes; pair source scans with behavior tests rather than treating grep as proof. The scanner/audit may not edit a production file: on any out-of-scope defect, atomically update `SECURITY-GAPS.md` with the exact failure and required closure evidence, fail non-zero, and require a new declared gap-closure plan.
  </action>
  <verify><automated>node scripts/check-security-invariants.mjs dist && npm run test:e2e -- tests/e2e/security/privacy-readonly.spec.ts --project=chromium</automated></verify>
  <acceptance_criteria>
    - SEC-02/UI6-AC03 pass across success, denial, cancellation, export failure, offline update, and recovery.
    - Source hash is identical and next valid query succeeds after every denied/recoverable path.
    - Marker reporting names only boundary/category/count, never prints marker/database content.
  </acceptance_criteria>
  <failure_modes>
    - Any derived network/cache/log/URL leak is release-blocking Critical/High with prevention and recovery regression.
    - Inaccessible browser storage is not assumed clean; audit records unavailable and blocks claim until an alternative proof exists.
    - Static-scan false positives use narrow documented allowlists plus behavior evidence, never global exclusions.
  </failure_modes>
  <done>The no-upload/read-only/source-integrity claims have repeatable final-artifact evidence.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation plan |
|---|---|---|---|---|
| T-06-01-01 | Elevation of privilege | Derived DOM content | mitigate | Escaped bounded text, no raw sink, hostile all-surface E2E |
| T-06-01-02 | Tampering | CSP/worker/WASM | mitigate | Minimal measured policy, same-origin assets, forbidden-scheme/source scan |
| T-06-01-03 | Information disclosure | Network/log/storage/cache | mitigate | Unique-marker boundary audit across success/failure |
| T-06-01-04 | Tampering | Source/read-only state | mitigate | Source hashes, real authorizer deny matrix, post-error recovery |
| T-06-01-SC | Tampering | Audit authority | mitigate | Declared file ownership; findings fail into explicit gap-closure artifacts |
</threat_model>

<source_audit>
| Source | ID | Item | Task | Status |
|---|---|---|---|---|
| GOAL | — | Prove privacy/read-only/security claims | all | COVERED |
| REQ | SEC-01 | Content/execution hardening | 01 | COVERED |
| REQ | SEC-02 | Privacy/security audit | 02 | COVERED |
| REQ | REL-01 | Negative release evidence | all | COVERED |
| RESEARCH | PF-01,05,10,12,14 | Policy/sinks/leakage/artifact/mock hazards | all | COVERED |
| CONTEXT | D-44..D-46 | CSP/marker/high-risk decisions | all | COVERED |
| UI | UI6-AC01..03,07,09 | hostile/CSP/privacy/copy/evidence | all | COVERED |
</source_audit>

<verification>
Run focused commands above, then `npm run typecheck && npm run test && npm run build && node scripts/check-security-invariants.mjs dist && npm run test:e2e -- tests/e2e/security`. Any non-empty unresolved `SECURITY-GAPS.md` blocks the phase.
</verification>

<success_criteria>
- SEC-01/02 and assigned REL-01 evidence are green with Critical/High prevention+recovery tests.
- Public copy contains no protection absent from code/artifact/deployed headers.
- No derived data leak, source mutation, unsafe SQL recovery failure, unresolved security gap, remote runtime dependency, or new package exists.
</success_criteria>

<output>Create `06-01-SUMMARY.md` with CSP directives/rationale, marker boundaries, source/read-only evidence, declared-file ownership, closed gap IDs, and residual hosting limits.</output>
