---
phase: 06-security-performance-accessibility-release
plan: "03"
type: execute
wave: 3
depends_on: ["06-01", "06-02", "06-04", "06-05", "06-06"]
files_modified:
  - ../scripts/build-pages.mjs
  - ../scripts/verify-pages-build.mjs
  - ../.github/workflows/pages.yml
  - tests/e2e/pages-release.spec.ts
  - scripts/release-gate.mjs
  - scripts/check-deployed-seeqlite.mjs
  - .release-evidence/test-results.json
  - docs/release/EVIDENCE.md
  - docs/release/LIMITATIONS.md
  - docs/release/ROLLBACK.md
  - docs/release/RELEASE-CHECKLIST.md
  - docs/release/gaps/RELEASE-GAPS.md
autonomous: false
requirements: [SEC-02, REL-01, REL-02]
must_haves:
  truths:
    - "The Node 24 shared Pages pipeline installs/builds/tests SeeQLite and DataDuck and emits a verified `/seeqlite/` artifact with all relative worker/WASM/PWA assets."
    - "One reproducible release gate runs every required automated matrix with explicit skip/retry/failure visibility and links all 48 requirements to evidence."
    - "Release limitations, dependency notices, deployed-header evidence, current Safari record, and rollback rehearsal are complete before publish approval."
    - "Rollback restores the previous complete site/app without deleting sibling caches/routes or leaving a mixed SeeQLite shell."
    - "The release gate consumes the current PF-01..PF-14 evidence manifest and fails missing, stale, skipped, retried-away, or unresolved prevention/recovery evidence."
  artifacts:
    - path: "../scripts/build-pages.mjs"
      provides: "Shared Node 24 build assembling SeeQLite and existing tools"
    - path: "../scripts/verify-pages-build.mjs"
      provides: "Required/forbidden/subpath/PWA/landing/sibling artifact assertions"
    - path: "scripts/release-gate.mjs"
      provides: "Single fail-fast complete release command and evidence manifest"
    - path: ".release-evidence/test-results.json"
      provides: "Ephemeral current-run result index with release commit, source digests, outcomes, skip/retry state, and timestamps"
    - path: "docs/release/ROLLBACK.md"
      provides: "Tested previous-artifact/site/service-worker rollback procedure"
    - path: "docs/release/gaps/RELEASE-GAPS.md"
      provides: "Exact deployment/release defects for explicit gap-closure plans"
  key_links:
    - from: "../scripts/build-pages.mjs"
      to: "SeeQLite/dist"
      via: "npm ci/build then copy to .pages-build/seeqlite"
      pattern: "seeqlite"
    - from: "../.github/workflows/pages.yml"
      to: "../scripts/verify-pages-build.mjs"
      via: "Node 24 dual-lockfile CI before upload/deploy"
      pattern: "node-version: 24"
    - from: "scripts/release-gate.mjs"
      to: "docs/release/EVIDENCE.md"
      via: "all requirement IDs, commands, artifacts, manual records, checksums"
      pattern: "REL-02"
    - from: "scripts/release-gate.mjs"
      to: "scripts/check-risk-evidence.mjs"
      via: "current commit/test-result PF-01..PF-14 freshness gate"
      pattern: "risk-evidence.json"
  prohibitions:
    - "No inaccurate security/offline/browser/CSV/full-result claim, new feature category, analytics, remote asset, or SeeQLite-only landing layout override."
    - "No deploy on a failing/skipped/retried-away gate, unresolved Critical/High, absent Safari/manual record, or untested rollback."
    - "No DataDuck regression waiver, origin-wide cache cleanup, root-relative SeeQLite asset, or source/test file in Pages artifact."
    - "No audit/checkpoint task may repair an undeclared production file; findings fail into RELEASE-GAPS.md for an explicit gap-closure plan."
---

<objective>
Integrate SeeQLite into the shared Pages pipeline, prove the complete release matrix, publish truthful limitations, and rehearse rollback.

Purpose: The milestone is complete only when the exact public artifact and sibling products—not a local app build—meet every requirement and recovery gate.
Output: Dual-app Node 24 build/CI/verifier, risk-aware release gate/evidence/limitations/rollback, deployed header/scope validation.
</objective>

<execution_context>@/Users/aneesahammed/.codex/gsd-core/workflows/execute-plan.md</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/06-security-performance-accessibility-release/06-CONTEXT.md
@.planning/phases/06-security-performance-accessibility-release/06-UI-SPEC.md
@.planning/phases/06-security-performance-accessibility-release/06-VALIDATION.md
@.planning/phases/06-security-performance-accessibility-release/06-01-SUMMARY.md
@.planning/phases/06-security-performance-accessibility-release/06-02-SUMMARY.md
@.planning/phases/06-security-performance-accessibility-release/06-04-SUMMARY.md
@.planning/phases/06-security-performance-accessibility-release/06-05-SUMMARY.md
@.planning/phases/06-security-performance-accessibility-release/06-06-SUMMARY.md
@../index.htm
@../scripts/build-pages.mjs
@../scripts/verify-pages-build.mjs
@../.github/workflows/pages.yml
@../dataduck/package.json
</context>

<artifacts_produced>
| Artifact | Provides |
|---|---|
| shared build/verifier/workflow | Node 24 SeeQLite+DataDuck artifact/release regression |
| `release-gate.mjs`, `.release-evidence/test-results.json`, `EVIDENCE.md` | complete suite execution plus current 48-ID/PF-01..14 traceability |
| limitations/checklist/rollback docs | public truth, deploy prerequisites, tested recovery |
</artifacts_produced>

<file_ownership>
- Task 06-03-01 owns only the shared Pages scripts/workflow and Pages E2E.
- Task 06-03-02 owns only the release/deployed checkers, generated `.release-evidence/test-results.json`, and declared release/gap documents.
- These verification tasks never opportunistically edit app, DataDuck, landing, test, or infrastructure files outside their declarations. Record the exact failure, owner, proposed files, prevention test, and recovery test in `RELEASE-GAPS.md`; block until a separate gap-closure plan completes.
</file_ownership>

<scope_rationale>
These 12 files are the single shared-site release transaction: Task 1 produces/verifies the Pages artifact and Task 2 runs evidence/deploy/rollback gates against that exact output. Ownership is disjoint, sequential, and gap-only outside declarations; splitting would add an unversioned artifact handoff at the final release boundary.
</scope_rationale>

<tasks>

<task type="auto" tdd="true">
  <name>Task 06-03-01: Integrate Node 24 dual-app Pages build, verification, CI, and sibling regressions</name>
  <read_first>
    - `../scripts/build-pages.mjs` — current DataDuck/static tool assembly
    - `../scripts/verify-pages-build.mjs` — current required/forbidden checks
    - `../.github/workflows/pages.yml` — Node 20/DataDuck-only install cache
    - `scripts/verify-seeqlite-build.mjs` — Phase 5 internal SeeQLite artifact contract
    - `../dataduck/package.json`, tests, bundle checker — sibling gates
  </read_first>
  <files>../scripts/build-pages.mjs, ../scripts/verify-pages-build.mjs, ../.github/workflows/pages.yml, tests/e2e/pages-release.spec.ts</files>
  <behavior>
    - Build removes old artifact, builds DataDuck and SeeQLite from `npm ci` lockfiles on Node 24, copies SeeQLite dist to `.pages-build/seeqlite`, and preserves all existing tools/root files.
    - Verifier asserts SeeQLite HTML/manifest/icons/SW/JS/CSS/fonts/DB+layout workers/WASM/lazy chunks, relative references, MIME expectations, generated precache equality, CSP/landing card, and forbidden source/test/map/package files.
    - Workflow caches both lockfiles, installs both apps, runs both tests/typecheck/build/bundle plus combined verifier before upload/deploy.
  </behavior>
  <action>
Implement D-50/D-51 surgically in the three shared files. Add `SeeQLite` directory/build/copy while retaining existing DataDuck/static behavior; make command failure fatal. Move setup-node to Node 24 and include both lockfiles. Expand verification with reusable helpers, SeeQLite internal verifier data, root-path leakage scans, SW scope/cache prefix, manifest target existence, service-worker/precache asset parity, and required/forbidden file families. Add production route/refresh/offline/sibling smoke. Run DataDuck tests and bundle check on Node 24; do not modify DataDuck behavior to make the shared runtime pass.
  </action>
  <verify><automated>node ../scripts/build-pages.mjs && node ../scripts/verify-pages-build.mjs && npm --prefix ../dataduck test && npm --prefix ../dataduck run build && npm --prefix ../dataduck run check:bundle && npm run test:e2e -- tests/e2e/pages-release.spec.ts --project=chromium</automated></verify>
  <acceptance_criteria>
    - REL-01/REL-02 and UI6-AC07/08 production build criteria pass on Node 24.
    - `.pages-build/seeqlite/` is complete/relative and no source/test/lockfile leaks; all old routes/tools remain.
    - DataDuck tests/build/bundle and seeded offline cache survive without waiver.
    - CI cannot upload/deploy on any app/test/verifier failure or hidden skip.
  </acceptance_criteria>
  <failure_modes>
    - Missing dependency install/build output fails rather than silently omitting SeeQLite.
    - Shared artifact cleanup remains directory-scoped and never deletes source/product caches.
    - Node 24 incompatibility is fixed at the owning app/tool; no downgrade to EOL Node.
  </failure_modes>
  <done>The shared production pipeline builds/verifies both applications and every existing route before deploy.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 06-03-02: Run the complete risk-aware release gate, verify deployed truth, and rehearse rollback</name>
  <read_first>
    - `.planning/REQUIREMENTS.md` — all 48 requirements and Definition of Done
    - `06-01-SUMMARY.md`, `06-02-SUMMARY.md`, `06-04-SUMMARY.md`, `06-05-SUMMARY.md`, `06-06-SUMMARY.md`, Phase 5 summaries — final evidence inputs
    - `docs/release/risk-evidence.json`, `scripts/check-risk-evidence.mjs` — exact PF-01..PF-14 contract
    - actual Pages workflow/deployment history and current public headers
    - dependency/license/manual Safari/AT evidence
  </read_first>
  <files>scripts/release-gate.mjs, scripts/check-deployed-seeqlite.mjs, .release-evidence/test-results.json, docs/release/EVIDENCE.md, docs/release/LIMITATIONS.md, docs/release/ROLLBACK.md, docs/release/RELEASE-CHECKLIST.md, docs/release/gaps/RELEASE-GAPS.md</files>
  <behavior>
    - One gate first runs the complete suite and atomically emits `.release-evidence/test-results.json`, then rejects any failed, missing, skipped/fixme/conditional, retry-masked, stale, digest-mismatched, or unresolved requirement/DoD/PF evidence item.
    - Deployed verification proves the actual subpath artifact, headers/MIME/CSP/SW scope/offline/direct refresh/landing/sibling behavior; rollback restores the prior complete version without origin-wide deletion.
  </behavior>
  <action>
Implement D-51/D-52 as a fail-fast release orchestrator and machine-checked evidence manifest. It runs typecheck/lint/unit/real-WASM integration/three-engine production E2E/negative/security/axe/visual/performance/bundle/export/offline/Pages/DataDuck checks. Only after those commands finish, atomically emit `.release-evidence/test-results.json` containing the checked release git SHA, exact command/project/test ID, referenced source SHA-256, outcome, skip/fixme/conditional/retry state, start/end timestamps, and artifact links; never reuse a prior run. Then invoke `check-risk-evidence.mjs` with the explicit release SHA, PF manifest, and newly emitted artifact, reject unexpected skips/retries, verify all 48 requirement IDs and ten DoD items link to fresh evidence, and check signed Safari/AT records. PF-01..PF-14 must each have a named passing prevention and recovery result; missing, stale, skipped/fixme/conditional, retry-masked, digest-mismatched, or unresolved evidence is fatal. A suite failure may emit an atomic diagnostic artifact marked incomplete but must not invoke the PF checker as if complete. Publish exact limitations: read-only, one transient file, declared FKs, WAL sidecars, all caps, offline shell not DB, CSV apostrophe transformation, supported browsers, and absent header protections. Write rollback to the previous artifact/commit with service-worker version behavior, sibling preservation, verification commands, ownership-free steps, and abort criteria. After authorized deploy, inspect `tinycrafts.ai/seeqlite/` headers/MIME/CSP/scope/offline/direct refresh/landing/DataDuck, then rehearse rollback in a production-shaped staging copy (or actual prior deployment only with separate explicit authority). Any discovered defect is written to `RELEASE-GAPS.md` and blocks for an explicit file-owning closure plan; this task does not edit undeclared production.
  </action>
  <verify>
    <automated>node scripts/release-gate.mjs && node scripts/check-deployed-seeqlite.mjs https://tinycrafts.ai/seeqlite/</automated>
    <human-check>Confirm current Safari/VoiceOver/NVDA evidence, deployed header/landing/full flow, limitations/notices, and a completed rollback rehearsal; no unresolved Critical/High finding or skipped requirement.</human-check>
  </verify>
  <acceptance_criteria>
    - All 48 requirements/ten DoD items have fresh linked automated or mandatory manual evidence.
    - PF-01..PF-14 each have current named prevention and recovery evidence; mutation cases for missing/stale/skipped/retried/unresolved evidence fail.
    - UI6-AC07..10 and Phase 6 exit criteria pass with no hidden skip/retry or unresolved Critical/High.
    - Public artifact/landing/copy match actual headers/runtime limits; DataDuck/sibling routes/caches remain green.
    - Rollback restores prior complete artifact and version-matched offline shell, then verification passes.
  </acceptance_criteria>
  <failure_modes>
    - Deployment authentication/authority absence pauses only deploy verification; release remains blocked, never assumed published.
    - An absent header is documented and related claim removed; JavaScript/meta behavior is not called equivalent.
    - Rollback rehearsal failure blocks release and preserves the last known complete artifact; no destructive origin-wide cache action.
  </failure_modes>
  <done>SeeQLite is release-ready/published only after complete evidence, truthful deployed verification, sibling regressions, and tested rollback all pass.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation plan |
|---|---|---|---|---|
| T-06-03-01 | Tampering | Shared Pages artifact/CI | mitigate | Clean build, required/forbidden checks, dual lockfiles/apps, fail before upload |
| T-06-03-02 | Denial of service | Sibling routes/caches | mitigate | DataDuck/static route regressions, scoped SW/cache, rollback rehearsal |
| T-06-03-03 | Repudiation | Release evidence/deployment | mitigate | Fresh risk/test checksums, versions, commands, manual records, deployed header capture |
| T-06-03-SC | Tampering | Actions/packages | mitigate | Pinned action majors/lockfiles, dependency audit, no package change in this plan |
</threat_model>

<source_audit>
| Source | ID | Item | Task | Status |
|---|---|---|---|---|
| GOAL | — | Exact public artifact with proven claims | all | COVERED |
| REQ | SEC-02 | Public privacy/dependency evidence | 02 | COVERED |
| REQ | REL-01 | Complete suite/evidence | 01,02 | COVERED |
| REQ | REL-02 | Pages/DataDuck/Safari/release | 01,02 | COVERED |
| RESEARCH | PF-01..14 | current risk evidence gate | 02 | COVERED |
| CONTEXT | D-50 | build/sibling integration | 01 | COVERED |
| CONTEXT | D-51 | Node 24 complete CI | 01,02 | COVERED |
| CONTEXT | D-52 | exact limitation disclosures | 02 | COVERED |
| UI | UI6-AC07,09,10 | artifact/evidence/DoD | 01,02 | COVERED |
</source_audit>

<verification>
Run the shared-build focused command, then `node scripts/release-gate.mjs`. Confirm that command executes the complete suite, atomically creates a new current-commit `.release-evidence/test-results.json`, and only then invokes the PF checker. The standalone checker command may be repeated against that artifact after the gate, never before it. After authorized deploy run the deployed checker and human release/rollback verification. Any failure or unresolved release gap leaves release blocked.
</verification>

<success_criteria>
- The already-verified TinyCrafts No. 08 entry and `/seeqlite/` exact artifact survive the shared build.
- Node 24 CI and local release gate pass every app/security/performance/a11y/offline/sibling check without hidden skips.
- Requirements/DoD/PF risk/manual/dependency/limitations/deployed-header evidence is fresh, complete, and truthful.
- Rollback is executable, rehearsed, and preserves sibling apps/caches.
</success_criteria>

<output>Create `06-03-SUMMARY.md` with release-gate digest, all evidence links, deployed URL/header capture, DataDuck/sibling results, limitation/notices version, rollback rehearsal, and release status.</output>
