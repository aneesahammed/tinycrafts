---
phase: 05-takeaway-offline-shell
plan: "03"
type: execute
wave: 3
depends_on: ["05-01", "05-02", "05-04", "05-05"]
files_modified:
  - tests/e2e/helpers/cache-audit.ts
  - tests/e2e/helpers/offline-builds.ts
  - tests/e2e/offline.spec.ts
  - tests/e2e/offline-update.spec.ts
  - tests/e2e/offline-privacy.spec.ts
  - scripts/verify-seeqlite-build.mjs
autonomous: true
requirements: [OFF-01, OFF-02, OFF-03]
must_haves:
  truths:
    - "A production-built shell that completed one online visit reloads offline in all supported engines and can open a newly selected local database."
    - "Fresh/offline, incomplete-cache, quota, registration, missing-asset, and update failures are truthful and recoverable without corrupting the last complete shell."
    - "Two tabs can remain on adjacent complete builds without mixed worker/WASM/lazy-chunk failures."
    - "DataDuck and unrelated seeded caches survive every SeeQLite install/update/activation/reset/failure scenario unchanged."
  artifacts:
    - path: "tests/e2e/offline.spec.ts"
      provides: "First/repeat offline and lazy workflow production evidence"
    - path: "tests/e2e/offline-update.spec.ts"
      provides: "Two-build, two-tab, missing-asset, interrupted update, rollback evidence"
    - path: "tests/e2e/offline-privacy.spec.ts"
      provides: "Cache key/body/scope marker and sibling isolation audit"
    - path: "scripts/verify-seeqlite-build.mjs"
      provides: "Static required/forbidden asset and generated precache verifier"
  key_links:
    - from: "tests/e2e/helpers/offline-builds.ts"
      to: "scripts/build-service-worker.mjs"
      via: "creates two content-distinct production artifacts without production test hooks"
      pattern: "build-service-worker"
    - from: "tests/e2e/offline-update.spec.ts"
      to: "src/pwa/sw-template.js"
      via: "real browser install/wait/activate/fetch lifecycle"
      pattern: "ACTIVATE_UPDATE"
    - from: "tests/e2e/offline-privacy.spec.ts"
      to: "CacheStorage"
      via: "before/after cache key and response digest inspection"
      pattern: "dataduck-"
  prohibitions:
    - "No production-only test hook, broad Cache Storage deletion, hidden browser skip, dev-server evidence, or mocked service-worker release claim."
    - "No fixture containing real/sensitive data or generated from a global SQLite/Python installation."
---

<objective>
Prove Phase 5 on the exact built subpath with real service-worker lifecycle, failures, multiple tabs, lazy features, privacy markers, and sibling caches.

Purpose: Unit policy is insufficient for browser install/update semantics; release confidence requires a reproducible artifact matrix.
Output: Production E2E helpers/suites and a static build verifier.
</objective>

<execution_context>@/Users/aneesahammed/.codex/gsd-core/workflows/execute-plan.md</execution_context>

<context>
@.planning/phases/05-takeaway-offline-shell/05-CONTEXT.md
@.planning/phases/05-takeaway-offline-shell/05-UI-SPEC.md
@.planning/phases/05-takeaway-offline-shell/05-VALIDATION.md
@.planning/phases/05-takeaway-offline-shell/05-01-SUMMARY.md
@.planning/phases/05-takeaway-offline-shell/05-02-SUMMARY.md
@.planning/phases/05-takeaway-offline-shell/05-04-SUMMARY.md
@.planning/phases/05-takeaway-offline-shell/05-05-SUMMARY.md
@.planning/research/PITFALLS.md
@playwright.config.ts
@tests/e2e/helpers/production-server.ts
@scripts/verify-seeqlite-build.mjs
</context>

<artifacts_produced>
| Artifact | Evidence |
|---|---|
| `tests/e2e/offline.spec.ts` | online→offline→new local DB plus lazy editor/ER/export |
| `tests/e2e/offline-update.spec.ts` | build A/B, waiting update, two tabs, interrupted install, previous-version recovery |
| `tests/e2e/offline-privacy.spec.ts` | user-marker exclusion and sibling-cache hash equality |
| `scripts/verify-seeqlite-build.mjs` | asset/precache/subpath/forbidden-source invariants |
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 05-03-01: Prove first/repeat offline truth and a complete lazy workflow</name>
  <read_first>
    - `tests/e2e/helpers/production-server.ts` — Pages-like `/seeqlite/` server and MIME behavior
    - `src/pwa/service-worker-client.ts` — complete-cache/runtime states
    - `src/pwa/sw-template.js` — exact precache/fetch behavior
    - `.planning/phases/05-takeaway-offline-shell/05-UI-SPEC.md` — UI5-AC06/08 and first-ever-offline wording
  </read_first>
  <files>tests/e2e/helpers/offline-builds.ts, tests/e2e/offline.spec.ts, scripts/verify-seeqlite-build.mjs</files>
  <behavior>
    - Fresh context offline before any visit fails honestly; it cannot claim a cached app/database.
    - One complete online visit reaches `Ready offline`; subsequent offline direct navigation/reload resolves shell, fonts, DB worker/WASM, CodeMirror, React Flow/ELK, and export paths.
    - While offline, a user chooses a new fixture, inspects catalog/relationship list, runs `SELECT 1`, and exports JSON; no previous database reopens.
    - Registration blocked/unsupported while online shows degradation and open/query/export remain usable.
  </behavior>
  <action>
Create production-artifact tests per D-43 using real builds and browser offline controls. Add a static verifier that recomputes every generated asset's path, byte length, SHA-256 and aggregate cache ID, then cross-checks manifest icon/start/scope, required worker/WASM/font/lazy families, MIME/relative URLs, and forbidden source/maps/database fixtures. Capture console, failed requests, content types, controller/scope/cache names, and current URL on failure. Run the functional offline path per engine; never substitute a service-worker mock.
  </action>
  <verify><automated>npm run build && node scripts/verify-seeqlite-build.mjs dist && npm run test:e2e -- tests/e2e/offline.spec.ts --project=chromium</automated></verify>
  <acceptance_criteria>
    - OFF-01, OFF-03, UI5-AC06, and UI5-AC08 pass against `/seeqlite/`.
    - Every documented lazy workflow asset works offline after one complete visit.
    - Fresh offline and incomplete engine states use honest failure copy; no database persistence/reopen promise appears.
  </acceptance_criteria>
  <failure_modes>
    - A missing/mis-MIME/root-relative asset fails the verifier before E2E.
    - Browser registration/storage denial is recoverable online and produces no retry loop.
    - Failed offline navigation attaches evidence rather than retrying until green.
  </failure_modes>
  <done>Repeat-offline use is proven on the real artifact and fresh-offline limitations are honest.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 05-03-02: Exercise incomplete installs, quota failures, updates, and previous-version recovery</name>
  <read_first>
    - `scripts/build-service-worker.mjs` — content-derived variant generation
    - `src/pwa/service-worker-contract.test.ts` — low-level quota/partial-cache cases
    - `src/features/offline/RuntimeStatus.tsx` — waiting/failure/last-complete copy
    - `.planning/research/PITFALLS.md` — PF-11 recovery criteria
  </read_first>
  <files>tests/e2e/helpers/offline-builds.ts, tests/e2e/offline-update.spec.ts</files>
  <behavior>
    - Install build A; serve content-distinct build B; B waits and reports update ready without auto reload.
    - Force one required B asset to fail or return HTTP 200 with stale/wrong body bytes; digest verification rejects B, B never reports ready/activates, and A remains fully offline-capable.
    - Inject Cache Storage put/quota failure at the contract seam and registration rejection in browser; both show approved fallback while A/current online work survives.
    - Explicit activation reloads only consenting tab; an adjacent tab loaded from A triggers an A lazy editor/ER chunk that it had never loaded before, and the active B worker serves that exact member from A's retained complete cache.
  </behavior>
  <action>
Build D-41/D-43 update matrices without production test switches: create two isolated copied artifacts, add a benign emitted marker before regenerating B's per-asset path/size/SHA-256 manifest and cache ID, and make the local server fail a selected asset, interrupt it, or return a stale A body with a successful 200 for B's path. Pair browser lifecycle with deterministic quota tests. Keep a second A tab open without loading one lazy editor/ER route; after B activates, take the server offline and trigger that route in A, proving B's active worker consults only A's retained completion manifest and exact cached member. Assert version/digest IDs match, non-member/arbitrary paths miss, Update ready appears only after byte-complete install, and A remains usable.
  </action>
  <verify><automated>npm run test -- src/pwa/service-worker-contract.test.ts && npm run test:e2e -- tests/e2e/offline-update.spec.ts --project=chromium</automated></verify>
  <acceptance_criteria>
    - Missing/interrupted/quota-failed or stale-200 digest-mismatched B cannot replace A or show Update ready.
    - Successful B waits for explicit consent; no active tab is reloaded automatically.
    - Current and immediately previous worker/WASM/lazy chunks remain version-matched through two-tab activation; the old tab previous-lazy-chunk path works offline after B activation.
    - Failed update copy states that the last complete version is in use.
  </acceptance_criteria>
  <failure_modes>
    - Test server failure/stale-body mode must target a named generated asset and preserve HTTP 200 in the stale case, so byte verification—not status-only validation—is proven.
    - If a browser cannot expose a deterministic quota trigger, the browser test covers install/registration failure and the fake-cache contract remains mandatory; no skip hides the quota case.
    - Update test timeout is a hard failure with worker/cache/client evidence, not an automatic retry.
  </failure_modes>
  <done>OFF-02 recovery semantics hold across real update lifecycle and deterministic storage failure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 05-03-03: Lock sibling-cache isolation, user-data exclusion, and cross-engine regression</name>
  <read_first>
    - `tests/e2e/offline-update.spec.ts` — update scenarios from Task 05-03-02
    - `src/pwa/cache-policy.ts` — prefix and request eligibility
    - `tests/e2e/helpers/privacy-markers.ts` — marker capture convention
    - `../dataduck/sw.js` — known sibling cache prefix and legacy broad-cleanup risk
  </read_first>
  <files>tests/e2e/helpers/cache-audit.ts, tests/e2e/offline-update.spec.ts, tests/e2e/offline-privacy.spec.ts</files>
  <behavior>
    - Seed `dataduck-*`, another TinyCrafts prefix, and unrelated cache with known key/body digests; all are byte-identical after SeeQLite install/activate/update/reset/failure.
    - Run marked filename/schema/SQL/result/export workflows; no marker occurs in cache keys/bodies, service-worker metadata/messages, URL, network, console, or unrelated storage.
    - SeeQLite caches contain only the generated asset set plus bounded build metadata; reset removes only SeeQLite registration/caches and leaves history/siblings.
  </behavior>
  <action>
Add reusable cache hashing/inspection and production privacy tests per D-42/D-43. Audit cache keys and response bodies before/after every lifecycle path, including two tabs. Verify service-worker registration scope begins/ends at `/seeqlite/`, out-of-scope requests are untouched, and ordinary file/query/export use creates no cache entry. Run the full update/privacy path in Chromium, Firefox, and WebKit; attach engine-specific evidence and forbid blanket retries/skips.
  </action>
  <verify><automated>npm run test:e2e -- tests/e2e/offline.spec.ts tests/e2e/offline-update.spec.ts tests/e2e/offline-privacy.spec.ts</automated></verify>
  <acceptance_criteria>
    - OFF-01..03 and UI5-AC07/10 pass in the three configured engines.
    - Sibling cache keys and body digests are identical before/after every SeeQLite operation.
    - Unique user marker appears only in allowed live UI and explicit downloaded bytes, never app caches/metadata/network/log/URL.
    - Reset preserves query history and removes no non-SeeQLite registration/cache.
  </acceptance_criteria>
  <failure_modes>
    - Cache read/hash denial is a test failure with key-only evidence, not treated as clean.
    - Cross-engine feature difference must retain the product outcome and lower-layer coverage; unexplained skip blocks release.
    - Privacy failure reports marker category/location without echoing the marker value into logs.
  </failure_modes>
  <done>The shared-origin and privacy invariants are permanent production-artifact regression gates.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation plan |
|---|---|---|---|---|
| T-05-03-01 | Repudiation | Offline claims | mitigate | Fresh/repeat production paths and captured controller/cache/asset evidence |
| T-05-03-02 | Tampering | Update asset set | mitigate | Two content IDs, forced required-asset failure, version-match assertions |
| T-05-03-03 | Denial of service | Cache quota/update | mitigate | Last-complete recovery, deterministic quota contract, no retries |
| T-05-03-04 | Information disclosure | Cache/SW metadata | mitigate | Unique-marker audit across keys/bodies/messages/network/log/storage |
| T-05-03-SC | Tampering | Package supply chain | mitigate | No dependency changes; production build uses committed lockfile |
</threat_model>

<source_audit>
| Source | ID | Item | Task | Status |
|---|---|---|---|---|
| GOAL | — | Reopen stable shell offline without sibling corruption | all | COVERED |
| REQ | OFF-01 | Repeat offline | 01,03 | COVERED |
| REQ | OFF-02 | Atomic scoped updates | 02,03 | COVERED |
| REQ | OFF-03 | Graceful fallback | 01,02 | COVERED |
| RESEARCH | PF-11 | Real shared-origin/update detection | 02,03 | COVERED |
| CONTEXT | D-41..D-43 | Complete set/scope/truth | all | COVERED |
| UI | UI5-AC06..10 | Production offline/update/privacy matrix | all | COVERED |
</source_audit>

<verification>
Run `npm run typecheck && npm run test && npm run build && node scripts/verify-seeqlite-build.mjs dist && npm run test:e2e -- tests/e2e/offline.spec.ts tests/e2e/offline-update.spec.ts tests/e2e/offline-privacy.spec.ts` with no hidden skip/retry.
</verification>

<success_criteria>
- All Phase 5 exit criteria and EXP/OFF requirement evidence are green.
- The last complete shell survives every negative update/storage path.
- Every sibling cache and user-data exclusion invariant is proven in the built artifact.
- Online behavior remains usable when offline setup is unavailable.
</success_criteria>

<output>Create `.planning/phases/05-takeaway-offline-shell/05-03-SUMMARY.md` with browser matrix, build IDs, cache before/after evidence, failures exercised, and remaining manual install-prompt note.</output>
