---
phase: 05-takeaway-offline-shell
plan: "05"
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/features/export/ExportDisclosure.tsx
  - src/features/export/ExportDisclosure.test.tsx
  - src/features/export/export.css
  - src/features/results/ResultToolbar.tsx
  - tests/e2e/export.spec.ts
autonomous: true
requirements: [EXP-01, EXP-02, EXP-03]
must_haves:
  truths:
    - "A user sees held row/column counts and every truncation reason before export."
    - "Export never reruns SQL and preserves the held result on success/failure."
    - "CSV loss/formula handling and JSON typing are visibly disclosed before preparation."
    - "Production downloads parse independently and no object URL survives."
  artifacts:
    - path: "src/features/export/ExportDisclosure.tsx"
      provides: "Accessible held-result export state/format/disclosure UI"
      exports: ["ExportDisclosure"]
    - path: "tests/e2e/export.spec.ts"
      provides: "Production no-rerun, parsing, resource, privacy evidence"
  key_links:
    - from: "src/features/results/ResultToolbar.tsx"
      to: "src/features/export/ExportDisclosure.tsx"
      via: "immutable QueryResultDto only; no database client/rerun callback"
      pattern: "ExportDisclosure.*result"
    - from: "src/features/export/ExportDisclosure.tsx"
      to: "src/features/export/download-result.ts"
      via: "tested 05-01 encoder/download contracts"
      pattern: "prepareResultDownload"
  prohibitions:
    - "No worker/database client prop, query rerun, unbounded/full export, hidden truncation, or CSV safety absolute."
---

<objective>Wire the tested export contracts into the result toolbar and prove independently parsed production downloads without rerunning SQL.</objective>
<execution_context>@/Users/aneesahammed/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>
@.planning/phases/05-takeaway-offline-shell/05-CONTEXT.md
@.planning/phases/05-takeaway-offline-shell/05-UI-SPEC.md
@.planning/phases/05-takeaway-offline-shell/05-VALIDATION.md
@.planning/phases/05-takeaway-offline-shell/05-01-SUMMARY.md
@src/features/results/ResultToolbar.tsx
@src/state/app-reducer.ts
</context>

<artifacts_produced>
| Artifact | Symbols/evidence |
|---|---|
| `ExportDisclosure.tsx` | `ExportDisclosure`, ready/preparing/prepared/failed states |
| `export.spec.ts` | complete/zero/truncated/typed CSV+JSON browser downloads and no-rerun proof |
</artifacts_produced>

<file_ownership>
- Task 05-05-01 exclusively owns the four production/component files.
- Task 05-05-02 exclusively owns `tests/e2e/export.spec.ts`; a production defect found by E2E becomes a gap-closure item rather than an undeclared edit.
</file_ownership>

<tasks>
<task type="auto" tdd="true">
  <name>Task 05-05-01: Implement the accessible held-result export disclosure</name>
  <read_first>
    - `05-01-SUMMARY.md` — encoder/tag/formula/filename/object-URL contracts
    - `src/features/results/ResultToolbar.tsx` — held result and invalidation state
    - `05-UI-SPEC.md` — exact copy/focus/responsive contract
  </read_first>
  <files>src/features/export/ExportDisclosure.tsx, src/features/export/ExportDisclosure.test.tsx, src/features/export/export.css, src/features/results/ResultToolbar.tsx</files>
  <behavior>
    - No result disables export with a named reason; zero rows with columns remains exportable.
    - JSON auto-selects for BigInt/BLOB/duplicates/truncated/mixed types; manual choice remains stable.
    - Held—not page—scope, every bound, CSV mutation/loss, and JSON typing appear before action.
    - Result generation invalidation closes stale export; preparation serializes repeated clicks and preserves result.
  </behavior>
  <action>Write component tests first, then implement D-36..D-39 using native section/fieldset/radio/status/alert controls and the 05-01 contracts. Pass only immutable result data and display stem; do not expose a database client or executor. Preserve focus/selection across theme/reflow and meet exact UI5 copy, target, live-region, reduced-motion, and overflow rules.</action>
  <verify><automated>npm run test -- src/features/export/ExportDisclosure.test.tsx</automated></verify>
  <acceptance_criteria>
    - UI5-AC01/02/05/09 and exact disclosure/focus states pass.
    - Worker/query call seams are absent from props/imports.
    - Success/failure/stale invalidation leaves held result and workspace usable.
  </acceptance_criteria>
  <failure_modes>
    - Invalid shape/unknown value/browser API denial yields one bounded recoverable alert.
    - Result changes during encoding suppress stale publication and clean resources.
    - No raw filename/result/error is logged or persisted.
  </failure_modes>
  <done>User-facing export is truthful, bounded, accessible, and consumes only the held result.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 05-05-02: Prove downloads, no-rerun, privacy, and cleanup on the production artifact</name>
  <read_first>
    - `ExportDisclosure.test.tsx` — UI behavior already proven
    - `tests/e2e/helpers/worker-spy.ts` — request count capture
    - `05-VALIDATION.md` — parser/privacy/browser matrix
  </read_first>
  <files>tests/e2e/export.spec.ts</files>
  <behavior>
    - Real complete/zero/truncated/typed results prepare both formats; independent parsers validate bytes.
    - Worker/SQL request counts do not change during export.
    - Blob/click/revoke failures and repeat clicks leave result usable and URL count at baseline.
    - Hostile content remains inert and derived markers appear only in explicit downloads/live UI.
  </behavior>
  <action>Add production `/seeqlite/` E2E for all EXP requirements and UI5-AC01..05/10. Capture downloads and parse them independently; spy request envelopes without payload values; test all three engines, themes, representative widths, 200% zoom, and reduced motion. If a production defect appears outside this task's file, record the exact failing criterion/file/test in the executor summary and stop for an explicit gap-closure plan.</action>
  <verify><automated>npm run test:e2e -- tests/e2e/export.spec.ts</automated></verify>
  <acceptance_criteria>
    - EXP-01..03 pass in Chromium/Firefox/WebKit with no hidden skip/retry.
    - Independent parser, no-rerun, URL cleanup, stale generation, and privacy assertions pass.
    - CSV/JSON disclosure exactly matches produced bytes/tags.
  </acceptance_criteria>
  <failure_modes>
    - Parser must not reuse production encoder logic.
    - Download API variance retains the same user outcome; unexplained engine skip blocks phase.
    - Failure evidence reports categories, not database-derived marker content.
  </failure_modes>
  <done>EXP-01..03 have production three-engine evidence and recovery coverage.</done>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|---|---|---|---|---|
| T-05-05-01 | Repudiation | Scope/disclosure | mitigate | Persistent counts/limits and byte-level independent parsing |
| T-05-05-02 | Information disclosure | UI/download | mitigate | Held-result only, marker audit, no persistence/logging |
| T-05-05-03 | Denial of service | Repeat Blob URL | mitigate | Serialized preparation and cleanup stress |
| T-05-05-SC | Tampering | dependencies | mitigate | No package/lockfile edits |
</threat_model>
<source_audit>
| Source | ID | Item | Task | Status |
|---|---|---|---|---|
| GOAL | — | Disclosed held-result takeaway | both | COVERED |
| REQ | EXP-01..03 | scope/CSV/JSON | both | COVERED |
| RESEARCH | PF-09 | injection/loss/misleading/leak hazards | both | COVERED |
| CONTEXT | D-36..D-39 | export decisions | 01,02 | COVERED |
| UI | UI5-AC01..05,09,10 | complete export UX/evidence | 01,02 | COVERED |
</source_audit>
<verification>`npm run typecheck && npm run test -- src/features/export/ExportDisclosure.test.tsx && npm run build && npm run test:e2e -- tests/e2e/export.spec.ts`</verification>
<success_criteria>All EXP requirements pass without rerun, false completeness, resource leak, privacy leak, or undeclared production edit.</success_criteria>
<output>Create `05-05-SUMMARY.md` with UI/file ownership, parser/browser matrix, no-rerun counters, cleanup evidence, and any gap-closure item.</output>
