---
phase: 05-takeaway-offline-shell
plan: "04"
type: execute
wave: 2
depends_on: ["05-02"]
files_modified:
  - src/pwa/service-worker-client.ts
  - src/pwa/service-worker-client.test.ts
  - src/features/offline/RuntimeStatus.tsx
  - src/features/offline/RuntimeStatus.test.tsx
  - src/features/offline/offline.css
  - src/app/AppHeader.tsx
autonomous: true
requirements: [OFF-01, OFF-02, OFF-03]
must_haves:
  truths:
    - "The client distinguishes registration from a byte-verified complete offline shell."
    - "A waiting update never reloads automatically and activates only after explicit confirmation."
    - "Online workflows remain available after registration/cache/quota/update failure."
    - "Runtime UI never promises database persistence or automatic reopen."
  artifacts:
    - path: "src/pwa/service-worker-client.ts"
      provides: "Typed registration/update/controller/reset lifecycle"
      exports: ["createServiceWorkerClient"]
    - path: "src/features/offline/RuntimeStatus.tsx"
      provides: "Approved install/offline/update/fallback/reset UI"
      exports: ["RuntimeStatus"]
  key_links:
    - from: "src/pwa/service-worker-client.ts"
      to: "dist/sw.js"
      via: "relative scope/updateViaCache none and typed complete-build messages"
      pattern: "updateViaCache.*none"
    - from: "src/features/offline/RuntimeStatus.tsx"
      to: "src/pwa/service-worker-client.ts"
      via: "subscribes to discriminated runtime states"
      pattern: "ready-offline"
  prohibitions:
    - "No Workbox, auto reload, origin-wide unregister/cache delete, registration-equals-ready assumption, or database persistence claim."
---

<objective>Connect the byte-verified worker lifecycle to truthful accessible install/offline/update/reset UI without coupling it to worker generation.</objective>
<execution_context>@/Users/aneesahammed/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>
@.planning/phases/05-takeaway-offline-shell/05-CONTEXT.md
@.planning/phases/05-takeaway-offline-shell/05-UI-SPEC.md
@.planning/phases/05-takeaway-offline-shell/05-VALIDATION.md
@.planning/phases/05-takeaway-offline-shell/05-02-SUMMARY.md
@src/app/AppHeader.tsx
@src/state/app-reducer.ts
</context>
<artifacts_produced>
| Artifact | Symbols |
|---|---|
| `service-worker-client.ts` | `createServiceWorkerClient`, discriminated runtime/update states |
| `RuntimeStatus.tsx` | header/narrow status, update notice, activation/reset confirmations |
</artifacts_produced>
<file_ownership>
- Task 05-04-01 owns only the service-worker client and its test.
- Task 05-04-02 owns only RuntimeStatus/test/CSS/AppHeader integration.
</file_ownership>

<tasks>
<task type="auto" tdd="true">
  <name>Task 05-04-01: Implement the complete-build-aware service-worker client state machine</name>
  <read_first>
    - `05-02-SUMMARY.md` — message/cache/build digest contract
    - `src/pwa/sw-template.js` — waiting/complete/activate/reset messages
    - `src/state/app-reducer.ts` — transient database/draft state
  </read_first>
  <files>src/pwa/service-worker-client.ts, src/pwa/service-worker-client.test.ts</files>
  <behavior>
    - Register `./sw.js` at `./` with update cache bypass; dev removes only SeeQLite-scoped registrations.
    - State becomes ready-offline only after worker reports a complete cache whose build ID matches its verified manifest.
    - Waiting complete update is reported but not activated/reloaded until explicit consent.
    - Security/quota/registration/update/controller errors settle once into online-degraded or offline-incomplete states.
  </behavior>
  <action>Write state-machine tests first, then implement D-40..D-43 with disposable listeners, StrictMode-safe lifecycle, bounded codes, online/offline events, captured install prompt, explicit activation, and prefix-scoped reset. Validate all worker messages against the expected schema/build ID. On consent, message the waiting worker and reload only that tab after controllerchange; do not force other tabs.</action>
  <verify><automated>npm run test -- src/pwa/service-worker-client.test.ts</automated></verify>
  <acceptance_criteria>
    - Registration never implies readiness; only verified complete-build evidence does.
    - No unapproved controllerchange forces reload.
    - Dev/reset never unregisters or deletes sibling state.
    - Every listener/promise settles/disposes once.
  </acceptance_criteria>
  <failure_modes>
    - Unknown/malformed/stale worker messages are ignored and surfaced as bounded degraded state where actionable.
    - Reset partial failure reports remaining SeeQLite state without retry loop.
    - Browser lacks SW/Cache Storage: online use continues.
  </failure_modes>
  <done>The UI has one trustworthy typed source for offline/update state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 05-04-02: Render truthful accessible runtime, update, install, and reset interactions</name>
  <read_first>
    - `service-worker-client.ts` — states/actions from Task 01
    - `05-UI-SPEC.md` — exact status hierarchy/copy/dialog/focus/responsive rules
    - `src/app/AppHeader.tsx` — integration slot
  </read_first>
  <files>src/features/offline/RuntimeStatus.tsx, src/features/offline/RuntimeStatus.test.tsx, src/features/offline/offline.css, src/app/AppHeader.tsx</files>
  <behavior>
    - Install action only appears from a captured browser event; dismissal is neutral.
    - Waiting update confirms transient state loss, exposes Copy SQL for dirty draft, and defaults to Keep current version.
    - Registration/cache/quota/update failure is durable but non-blocking online; incomplete offline engine blocks intake honestly.
    - Reset confirmation deletes only SeeQLite offline copy and preserves history/siblings.
  </behavior>
  <action>Implement approved RuntimeStatus/UpdateNotice/native confirmation using the service client. Test exact copy, focus restoration, live regions, both themes, reduced motion, 375/768/1024/wide layout, dirty SQL copy, retry/reset, and state preservation. This task may edit only its declared production files; a defect elsewhere becomes a named gap in the summary.</action>
  <verify><automated>npm run test -- src/features/offline/RuntimeStatus.test.tsx</automated></verify>
  <acceptance_criteria>
    - OFF-01..03 and UI5-AC06/08/09 state/copy/focus behavior pass.
    - UI never claims a DB is cached/downloaded/reopened.
    - Online open/query/export remain enabled through optional failure.
  </acceptance_criteria>
  <failure_modes>
    - Copy SQL failure blocks reload confirmation until explicitly resolved/cancelled.
    - State control disappearing moves focus to specified stable heading, never body.
    - Reset/update failure is reported without automatic loop.
  </failure_modes>
  <done>Users receive truthful offline/update controls without loss or inaccessible state.</done>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|---|---|---|---|---|
| T-05-04-01 | Spoofing | ready/update state | mitigate | complete-build message schema and cache/build ID match |
| T-05-04-02 | Denial of service | reload/reset | mitigate | explicit consent, scoped reset, no retries |
| T-05-04-03 | Information disclosure | dirty SQL/status | mitigate | local copy only, no logs/messages with SQL |
| T-05-04-SC | Tampering | dependencies | mitigate | no package changes |
</threat_model>
<source_audit>
| Source | ID | Item | Task | Status |
|---|---|---|---|---|
| GOAL | — | Truthful offline/update interaction | both | COVERED |
| REQ | OFF-01..03 | install/update/fallback | both | COVERED |
| RESEARCH | PF-11 | lifecycle/scope/failure | both | COVERED |
| CONTEXT | D-40..D-43 | client/UI decisions | both | COVERED |
| UI | UI5-AC06..10 | runtime UX/a11y/privacy | 02 | COVERED |
</source_audit>
<verification>`npm run typecheck && npm run test -- src/pwa/service-worker-client.test.ts src/features/offline/RuntimeStatus.test.tsx`</verification>
<success_criteria>Client/UI state is complete-build-aware, explicit, scoped, accessible, and non-blocking online.</success_criteria>
<output>Create `05-04-SUMMARY.md` with message/state contracts, copy/focus evidence, browser fallbacks, and gaps.</output>
