---
phase: 02-safe-database-lifecycle-catalog
plan: 03
type: execute
wave: 4
depends_on: [02-02]
files_modified:
  - SeeQLite/src/catalog/types.ts
  - SeeQLite/src/catalog/normalize.ts
  - SeeQLite/src/catalog/normalize.test.ts
  - SeeQLite/src/catalog/extract.ts
  - SeeQLite/src/engine/protocol.ts
  - SeeQLite/src/engine/sqlite.worker.ts
  - SeeQLite/tests/fixtures/catalog/README.md
  - SeeQLite/tests/integration/catalog.spec.ts
autonomous: true
requirements: [CAT-01, CAT-02, CAT-03]
must_haves:
  truths:
    - "The worker extracts complete generation-tagged object summaries/details, including related trigger metadata."
    - "Explorer, later completion/ER, and generated SQL consume one generation-tagged normalized catalog."
    - "Hostile/broken SQLite metadata is bounded and represented as inert incomplete/unresolved DTO state."
  artifacts:
    - { path: "SeeQLite/src/catalog/types.ts", provides: "canonical Catalog/CatalogObject/Column/Index/ForeignKey DTOs" }
    - { path: "SeeQLite/src/catalog/extract.ts", provides: "sqlite_schema + named PRAGMA bootstrap/lazy extraction" }
  key_links:
    - { from: "SeeQLite/src/engine/sqlite.worker.ts", to: "SeeQLite/src/catalog/extract.ts", via: "trusted catalog.bootstrap/catalog.object commands", pattern: "catalog\.bootstrap|catalog\.object" }
    - { from: "SeeQLite/src/catalog/extract.ts", to: "SeeQLite/src/catalog/types.ts", via: "positional named-field normalization including CatalogTrigger", pattern: "CatalogTrigger" }
  prohibitions:
    - "No schema DDL parser, relationship inference, eager all-detail loading, full unbounded list/graph, raw identifier interpolation, raw HTML, query editor, or schema mutation."
    - "No silent metadata omission, stale detail publication, treating unavailable as zero, or hiding limited mode."
---

<objective>
Extract and golden-test one complete bounded normalized SQLite catalog.

Purpose: Implement D-16 through D-18 and provide the canonical model consumed by Phases 3–4.
Output: Stable catalog/trigger DTOs, trusted bootstrap/detail extraction, and golden SQLite edge/hostile/broken fixtures.
</objective>

<execution_context>
@/Users/aneesahammed/.codex/gsd-core/workflows/execute-plan.md
@/Users/aneesahammed/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@SeeQLite/AGENTS.md
@SeeQLite/.planning/REQUIREMENTS.md
@SeeQLite/.planning/research/ARCHITECTURE.md
@SeeQLite/.planning/research/PITFALLS.md
@SeeQLite/.planning/phases/02-safe-database-lifecycle-catalog/02-CONTEXT.md
@SeeQLite/.planning/phases/02-safe-database-lifecycle-catalog/02-UI-SPEC.md
@SeeQLite/.planning/phases/02-safe-database-lifecycle-catalog/02-02-SUMMARY.md
</context>

<artifacts_produced>
- Canonical generation-tagged `Catalog`, ordered object/detail/index/FK records and warnings.
- Trusted `catalog.bootstrap` and deduplicated `catalog.object` worker commands.
- `CatalogTrigger` related metadata: exact trigger name, owning object ID/name, bounded SQL preview/null definition and incomplete status; triggers are not query targets.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Normalize complete SQLite metadata into golden models</name>
  <read_first>
    - CAT-01..04 and D-16/D-17.
    - Architecture Catalog Extraction and PITFALLS PF-06.
    - Phase 2 read-only trusted-command and limit contracts.
  </read_first>
  <files>SeeQLite/src/catalog/types.ts, SeeQLite/src/catalog/normalize.ts, SeeQLite/src/catalog/normalize.test.ts, SeeQLite/src/catalog/extract.ts, SeeQLite/src/engine/protocol.ts, SeeQLite/src/engine/sqlite.worker.ts, SeeQLite/tests/fixtures/catalog/README.md, SeeQLite/tests/integration/catalog.spec.ts</files>
  <behavior>
    - Bootstrap combines main-schema `table_list`, `sqlite_schema`, schema_version and bounded definitions into stable exact IDs/kinds/flags/counts.
    - Lazy details use named fields from table-valued table_xinfo, foreign_key_list, index_list/index_xinfo and ignore future extra columns.
    - Models preserve STRICT, WITHOUT ROWID, view/table/virtual/shadow, hidden/generated, composite PK/FK, implicit parent, auto/expression/partial index, rowid, self/cycle/parallel/missing parent, null definitions and related triggers.
    - Malformed one-object detail returns scoped incomplete warning; generation/schema-version changes invalidate derived detail.
  </behavior>
  <action>
    Per D-16/D-17, define positional input-row decoders and stable DTOs; bind object names to table-valued PRAGMA functions and select named fields. Build main-schema bootstrap eager summary and per-object detail commands under the trusted policy, with every response tagged by database generation/schema version. Add `CatalogTrigger` from `sqlite_schema` trigger rows as related metadata, not a selectable query target: exact stable ID/name, `tbl_name` owner resolution, bounded/null SQL definition and `complete|incomplete|unresolved-owner` status. Include BEFORE/AFTER/INSTEAD OF, quoted/Unicode/HTML-like trigger and owner names, null SQL, orphan/broken schema-row fixtures. Group FK rows by id/seq and preserve unresolved/implicit metadata. Create semantic goldens; compare DTO meaning, not fragile SQL prose.
  </action>
  <verify>
    <automated>cd SeeQLite &amp;&amp; npm run test -- src/catalog/normalize.test.ts &amp;&amp; npm run test:e2e -- --project=chromium --grep "catalog golden models"</automated>
    <assertions>
      - Every named edge and trigger fixture has an expected normalized golden, including hostile and broken trigger metadata.
      - Future-extra PRAGMA columns are ignored; missing required fields produce scoped warning.
      - Hostile quoted/Unicode/HTML/bidi names remain exact data, never code/HTML.
    </assertions>
  </verify>
  <acceptance_criteria>
    - CAT-01..03 metadata is complete and stable for all documented SQLite cases, with trigger counts/related records in object details.
    - One broken object cannot discard bootstrap or other details.
    - All downstream consumers can use this one model without re-querying/interpreting SQLite.
  </acceptance_criteria>
  <failure_modes>
    - Unavailable virtual module preserves schema row and scoped unavailable detail.
    - Null/missing SQL is represented explicitly, not as empty definition.
    - Old generation/schema detail is ignored.
  </failure_modes>
  <done>The canonical catalog passes pure goldens and real-WASM extraction fixtures.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Connect trusted bootstrap/detail commands and prove generation-safe extraction</name>
  <read_first>
    - DTO/normalizers and goldens from Task 1.
    - 02-02 `PolicyContext` and catalog-only exception contract.
  </read_first>
  <files>SeeQLite/src/catalog/extract.ts, SeeQLite/src/engine/protocol.ts, SeeQLite/src/engine/sqlite.worker.ts, SeeQLite/tests/integration/catalog.spec.ts</files>
  <behavior>
    - `catalog.bootstrap` and `catalog.object` are the only catalog commands and use fixed/bound SQL under their exact contexts.
    - Responses include epoch/schemaVersion; stale generation/schema detail is rejected.
    - Trigger rows attach to owner details or explicit unresolved-owner state and never become query targets.
  </behavior>
  <action>Wire positional extraction to the worker's closed trusted commands. Enforce object/column/definition bounds before response transfer, finalize every catalog statement, and emit scoped warnings for one broken detail while retaining bootstrap. Run real-WASM fixtures for every golden including hostile/broken triggers and future-extra simulated PRAGMA fields.</action>
  <verify><automated>cd SeeQLite &amp;&amp; npm run test:e2e -- --project=chromium --grep "catalog golden models" &amp;&amp; npm run test:e2e -- --project=webkit --grep "catalog golden models"</automated></verify>
  <acceptance_criteria>Real extraction equals semantic goldens; trusted contexts cannot be selected by arbitrary user SQL; stale/partial failures are isolated.</acceptance_criteria>
  <failure_modes>Unavailable module/null SQL/broken object or trigger returns explicit incomplete warning and leaves other catalog data usable.</failure_modes>
  <done>Canonical catalog/trigger DTOs are produced safely by real worker commands.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-09 | Tampering | catalog PRAGMA names/rows | mitigate | fixed/bound trusted commands, named field decoders, generation/schema tags. |
| T-02-10 | Elevation/XSS | identifiers/definitions | mitigate | bounded inert DTOs and hostile semantic goldens; React/quoting consumers follow in 02-05/02-06. |
| T-02-11 | Denial of service | huge/broken catalog | mitigate | eager summary/detail budgets and bounded trigger/definition DTOs; UI paging follows in 02-05. |
| T-02-12 | Information disclosure | catalog definitions/errors | mitigate | bounded text DTOs, no logs/storage/network. |
| T-02-SC | Tampering | UI dependency | mitigate | Existing exact `react-resizable-panels`; no new package. |
</threat_model>

<verification>
Run typecheck/unit semantic goldens and real-WASM catalog extraction in all three engines. Validate object/column/definition bounds, generation/schema invalidation, hostile/broken object and trigger fixtures, and no network/storage leakage.
</verification>

<success_criteria>
- CAT-01..03 have complete normalized model and real-WASM extraction evidence.
- All catalog consumers share stable exact IDs, ordered metadata, related triggers and generation invalidation.
- Broken object/trigger metadata is scoped and preserved as explicit incomplete/unresolved state.
</success_criteria>

<output>
Create `SeeQLite/.planning/phases/02-safe-database-lifecycle-catalog/02-03-SUMMARY.md` with DTO symbols, trusted command IDs, fixture/golden inventory, extraction budgets, generation/schema invalidation and hostile/broken trigger evidence. Do not create a branch or commit.
</output>
