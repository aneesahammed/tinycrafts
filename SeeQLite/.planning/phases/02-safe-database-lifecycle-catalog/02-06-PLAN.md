---
phase: 02-safe-database-lifecycle-catalog
plan: 06
type: execute
wave: 6
depends_on: [02-05]
files_modified:
  - SeeQLite/src/sql/quote-identifier.ts
  - SeeQLite/src/sql/quote-identifier.test.ts
  - SeeQLite/src/components/ObjectDetails.tsx
  - SeeQLite/tests/e2e/catalog.spec.ts
autonomous: true
requirements: [CAT-05]
must_haves:
  truths: ["Users can copy an exact quoted identifier or bounded SELECT for any catalog query target.", "Clipboard failure exposes selectable bounded text without changing workspace/database."]
  artifacts:
    - { path: "SeeQLite/src/sql/quote-identifier.ts", provides: "central SQLite double-quote escaping", exports: ["quoteIdentifier"] }
  key_links:
    - { from: "SeeQLite/src/components/ObjectDetails.tsx", to: "SeeQLite/src/sql/quote-identifier.ts", via: "Copy identifier/SELECT actions", pattern: "quoteIdentifier" }
  prohibitions: ["No editor insertion/replacement, dirty-draft decision, query execution, unquoted interpolation, raw HTML or unbounded clipboard text. ER-05 owns dirty-editor join confirmation."]
---
<objective>Ship safe bounded catalog copy actions. Purpose: implement revised CAT-05 without crossing the Phase 2 UI boundary. Output: central identifier quoter, Copy identifier/SELECT/SQL UI and hostile/clipboard tests.</objective>
<execution_context>@/Users/aneesahammed/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@SeeQLite/AGENTS.md @SeeQLite/.planning/REQUIREMENTS.md @SeeQLite/.planning/phases/02-safe-database-lifecycle-catalog/02-UI-SPEC.md @SeeQLite/.planning/phases/02-safe-database-lifecycle-catalog/02-05-SUMMARY.md</context>
<artifacts_produced>`quoteIdentifier`; bounded copy actions/fallback. Phase 4 ER-05 separately owns inserting generated joins into a dirty Phase 3 editor.</artifacts_produced>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Quote every SQLite identifier as exactly one identifier</name>
  <read_first>- Revised CAT-05; hostile identifier fixtures; SQLite double-quote rules.</read_first>
  <files>SeeQLite/src/sql/quote-identifier.ts, SeeQLite/src/sql/quote-identifier.test.ts</files>
  <behavior>- Always wrap in double quotes and double embedded quotes. - Keywords, dots, brackets, Unicode, newline and HTML-like names remain one identifier. - Bounded SELECT re-prepares as one read-only statement.</behavior>
  <action>Implement one pure quote helper over exact string input, with no normalization/case folding. Table-test all requirement edges and round-trip generated `SELECT * FROM {quoted} LIMIT 100;` against matching real fixture objects. Reject non-string/unbounded internal misuse at typed boundary.</action>
  <verify><automated>cd SeeQLite &amp;&amp; npm run test -- src/sql/quote-identifier.test.ts &amp;&amp; npm run typecheck</automated></verify>
  <acceptance_criteria>Every edge produces one intended identifier/statement; helper is exported for Phase 3 completion and Phase 4 joins.</acceptance_criteria>
  <failure_modes>Embedded NUL/control display remains bounded/inert; invalid internal input fails without SQL.</failure_modes>
  <done>Identifier generation is centralized and SQLite-correct.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Add explicit bounded copy actions and fallback</name>
  <read_first>- Revised CAT-05 and UI-SPEC ObjectDetails/Safe Action/clipboard copy.</read_first>
  <files>SeeQLite/src/components/ObjectDetails.tsx, SeeQLite/tests/e2e/catalog.spec.ts</files>
  <behavior>- Copy identifier/SELECT/SQL is explicit and never executes. - Success announces politely; denial exposes selectable exact bounded text. - Trigger metadata has Copy SQL only when bounded definition exists; triggers are not SELECT targets.</behavior>
  <action>Wire ObjectDetails actions to the central quoter and Clipboard API failure seam. Copy SELECT only for query-target objects, fixed LIMIT 100. Copy SQL only for returned bounded definition and label truncation. Render fallback as React text. Do not create editor insertion or dirty-draft logic; ER-05 owns dirty-editor confirmation.</action>
  <verify><automated>cd SeeQLite &amp;&amp; npm run test:e2e -- --project=chromium --grep "copies safe catalog SQL"</automated></verify>
  <acceptance_criteria>CAT-05 success/failure/hostile/truncation cases pass and no database/workspace state changes.</acceptance_criteria>
  <failure_modes>Clipboard absent/denied keeps catalog usable; null/truncated definition never implies full copy.</failure_modes>
  <done>Catalog copy actions are safe, bounded and phase-correct.</done>
</task>
</tasks>
<threat_model>| Threat | Disposition | Mitigation |\n|---|---|---|\n| SQL injection | mitigate | central quoting + reprepare tests |\n| DOM/clipboard disclosure | mitigate | bounded React text and explicit action |</threat_model>
<verification>Run quote units and production hostile/clipboard E2E; assert one statement, no execution/editor mutation and bounded fallback.</verification>
<success_criteria>Revised CAT-05 is complete; ER-05 remains sole dirty-editor confirmation owner.</success_criteria>
<output>Create `02-06-SUMMARY.md`; no branch or commit.</output>
