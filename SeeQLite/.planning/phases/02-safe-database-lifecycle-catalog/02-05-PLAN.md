---
phase: 02-safe-database-lifecycle-catalog
plan: 05
type: execute
wave: 5
depends_on: [02-03]
files_modified:
  - SeeQLite/src/catalog/search.ts
  - SeeQLite/src/catalog/search.test.ts
  - SeeQLite/src/state/app-reducer.ts
  - SeeQLite/src/components/CatalogRail.tsx
  - SeeQLite/src/components/ObjectDetails.tsx
  - SeeQLite/src/components/CatalogWorkspace.tsx
  - SeeQLite/src/styles/catalog.css
  - SeeQLite/tests/integration/catalog.spec.ts
  - SeeQLite/tests/e2e/catalog.spec.ts
autonomous: true
requirements: [CAT-01, CAT-02, CAT-04]
must_haves:
  truths: ["Normal, hostile, broken and limited catalogs remain searchable and lazily inspectable.", "The DOM contains only a capped searched page, never all 5,000 objects.", "Object details include related triggers without making triggers query targets."]
  artifacts:
    - { path: "SeeQLite/src/catalog/search.ts", provides: "bounded search/filter/page derivation" }
    - { path: "SeeQLite/src/components/CatalogWorkspace.tsx", provides: "responsive accessible catalog explorer" }
  key_links:
    - { from: "SeeQLite/src/components/ObjectDetails.tsx", to: "SeeQLite/src/catalog/types.ts", via: "lazy CatalogObject details including CatalogTrigger[]", pattern: "triggers" }
  prohibitions: ["No virtualization dependency, eager all-detail loading, unbounded DOM list, raw highlight HTML, stale detail flash or graph attempt in limited mode."]
---
<objective>Render the canonical catalog as a bounded responsive accessible explorer. Purpose: implement D-18 and CAT-01/02/04. Output: lazy details, capped search paging, limited mode and trigger-related UI.</objective>
<execution_context>@/Users/aneesahammed/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@SeeQLite/AGENTS.md @SeeQLite/.planning/REQUIREMENTS.md @SeeQLite/.planning/phases/02-safe-database-lifecycle-catalog/02-UI-SPEC.md @SeeQLite/.planning/phases/02-safe-database-lifecycle-catalog/02-03-SUMMARY.md</context>
<artifacts_produced>Pure searched/filter/paged projection; listbox/drawer/narrow modes; lazy object details with related trigger metadata and limited-mode capability.</artifacts_produced>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Bound search, detail requests and DOM pages</name>
  <read_first>- CAT-04, D-18, 5,000/50,000 worker budgets, canonical Catalog/CatalogTrigger DTOs.</read_first>
  <files>SeeQLite/src/catalog/search.ts, SeeQLite/src/catalog/search.test.ts, SeeQLite/src/state/app-reducer.ts, SeeQLite/tests/integration/catalog.spec.ts</files>
  <behavior>- Case-insensitive search/filter over bounded summary retains exact display text. - Render projection returns one 100-option window with exact previous/next range controls; changing search/filter resets the window. - Lazy detail keyed generation+schema+object dedupes; stale/failed detail scoped. - Limited mode remains searchable and reports the exact reached budget.</behavior>
  <action>Implement a pure projection with `CATALOG_DOM_PAGE_SIZE=100`, a zero-based searched-page cursor and exact range/count output; never hand the full 5,000-object array to row rendering. Keep the bounded summary in state for search, but replace the rendered window when users activate `Show previous 100` or `Load next {n}` so the listbox never contains more than 100 options. Search/filter/internal-toggle changes reset to the first window. Controls remain outside the listbox, no infinite scroll or virtualization dependency is added, and the UI follows the exact focus/status rules in 02-UI-SPEC. Cache details lazily by generation/schema/object, dedupe in-flight, clear old content immediately on selection and ignore stale replies. Carry only the canonical related-trigger fields within selected detail.</action>
  <verify><automated>cd SeeQLite &amp;&amp; npm run test -- src/catalog/search.test.ts &amp;&amp; npm run test:e2e -- --project=chromium --grep "catalog DOM budget"</automated></verify>
  <acceptance_criteria>Exact/over budgets, 0/99/100/101/5,000 matches, first/previous/next/reset, dedupe/stale/failure and limited-search pass; DOM option count is always at most 100 and range/status copy is exact.</acceptance_criteria>
  <failure_modes>Hostile query/control text inert/bounded; low memory stops page growth with scoped error; no graph flag in limited mode.</failure_modes>
  <done>Catalog search/detail work and DOM rendering are independently bounded.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Render responsive accessible catalog and related triggers</name>
  <read_first>- 02-UI-SPEC CatalogRail/ObjectDetails/columns/index/FK/SQL/state/keyboard/responsive contracts.</read_first>
  <files>SeeQLite/src/components/CatalogRail.tsx, SeeQLite/src/components/ObjectDetails.tsx, SeeQLite/src/components/CatalogWorkspace.tsx, SeeQLite/src/styles/catalog.css, SeeQLite/tests/e2e/catalog.spec.ts</files>
  <behavior>- Listbox search/filter/internal toggle and outside-listbox previous/next-window keyboard model. - Desktop rail/tablet drawer/narrow Objects-Details preserve focus/search/selection. - Related triggers show only canonical exact name, exact owner or `Owner unavailable`, bounded/null SQL and `Metadata incomplete`/`Owner unresolved` status. - Empty/no-match/broken/limited states exact.</behavior>
  <action>Implement semantic listbox and detail tables/tabs per UI-SPEC. Render CatalogTrigger records in the `Related triggers` section as related metadata, not options/query targets; render exact DTO text only and never inspect or parse trigger SQL for timing, event, target or other derived labels. Apply the exact previous/next-window semantics, status announcements and focus movement from the UI contract. Add responsive drawer/inert/focus restoration and 200% zoom/forced-colors/reduced-motion behavior. Assert the 100-option DOM cap after each search and page action.</action>
  <verify><automated>cd SeeQLite &amp;&amp; npm run test:e2e -- --project=chromium --grep "searches and inspects catalog" &amp;&amp; npm run test:e2e -- --project=webkit --grep "searches and inspects catalog"</automated></verify>
  <acceptance_criteria>CAT-01/02/04 and UI states pass with keyboard/axe/themes/viewports; hostile/broken trigger name/owner/SQL/status values remain inert and scoped, and no SQL-derived trigger labels exist.</acceptance_criteria>
  <failure_modes>Internal toggle invalid selection chooses valid target; drawer/resize failure preserves details; unavailable metadata says Unavailable, never zero.</failure_modes>
  <done>Users can search and inspect bounded complete catalog details accessibly.</done>
</task>
</tasks>
<threat_model>| Threat | Disposition | Mitigation |\n|---|---|---|\n| catalog DOM DoS | mitigate | worker budgets + replaceable 100-option searched windows |\n| hostile text/XSS | mitigate | bounded DTO + React text; trigger SQL is never parsed for UI metadata |\n| stale metadata | mitigate | generation/schema/object keys |</threat_model>
<verification>Run search units, real-WASM catalog integration and Chromium/WebKit responsive/keyboard/axe/DOM-budget E2E.</verification>
<success_criteria>Catalog remains useful under normal/hostile/broken/limited schemas; DOM/details are bounded; triggers appear as related metadata.</success_criteria>
<output>Create `02-05-SUMMARY.md`; no branch or commit.</output>
