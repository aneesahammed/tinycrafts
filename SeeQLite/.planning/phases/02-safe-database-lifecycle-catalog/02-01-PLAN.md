---
phase: 02-safe-database-lifecycle-catalog
plan: 01
type: execute
wave: 1
depends_on: [01-06]
files_modified:
  - SeeQLite/src/features/database/file-policy.ts
  - SeeQLite/src/features/database/file-policy.test.ts
  - SeeQLite/src/engine/import-database.ts
  - SeeQLite/src/engine/runtime-limits.ts
  - SeeQLite/src/engine/protocol.ts
  - SeeQLite/src/engine/database-client.ts
  - SeeQLite/src/engine/sqlite.worker.ts
  - SeeQLite/tests/integration/file-lifecycle.spec.ts
  - SeeQLite/tests/fixtures/file-outcomes/README.md
autonomous: true
requirements: [FILE-02, FILE-03, FILE-04]
must_haves:
  truths:
    - "Every candidate receives a cautious bounded file/import outcome before lifecycle UI adopts it."
    - "Files over 512 MiB are blocked before full allocation; files from 256 MiB through 512 MiB require explicit confirmation."
    - "Import cleanup leaves source bytes unchanged and later valid import works after every failure."
  artifacts:
    - { path: "SeeQLite/src/features/database/file-policy.ts", provides: "size/header/sidecar/extension classification", exports: ["classifyFile", "FILE_LIMITS"] }
    - { path: "SeeQLite/src/engine/import-database.ts", provides: "owned read-only copied-buffer import and WAL outcome" }
  key_links:
    - { from: "SeeQLite/src/engine/sqlite.worker.ts", to: "SeeQLite/src/engine/import-database.ts", via: "typed open command after bounded file classification", pattern: "importDatabase" }
  prohibitions:
    - "No extension-only acceptance, full read before hard-cap decision, sidecar merge, automatic integrity scan, OPFS, duplicate retained byte array, or source mutation."
    - "No stale generation publication, automatic reopen loop, multi-file/multi-database state, or certainty that unreadable input is encrypted."
---

<objective>
Create the bounded file classification and proven import-ownership boundary consumed by the lifecycle plan.

Purpose: Implement D-09 through D-12 and establish the generation invariant every catalog/query operation inherits.
Output: Phase 2 topology preflight, pure file policy, exact import/WAL spike, normalized outcomes and real-WASM ownership/cleanup tests.
</objective>

<execution_context>
@/Users/aneesahammed/.codex/gsd-core/workflows/execute-plan.md
@/Users/aneesahammed/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@SeeQLite/AGENTS.md
@SeeQLite/.planning/REQUIREMENTS.md
@SeeQLite/.planning/research/ARCHITECTURE.md
@SeeQLite/.planning/research/PITFALLS.md
@SeeQLite/.planning/phases/02-safe-database-lifecycle-catalog/02-CONTEXT.md
@SeeQLite/.planning/phases/02-safe-database-lifecycle-catalog/02-UI-SPEC.md
@SeeQLite/.planning/phases/01-walking-skeleton-browser-harness/01-03-SUMMARY.md
</context>

<artifacts_produced>
- `FileOutcome`, `FileCandidate`, `FILE_LIMITS`: stable classification contract.
- `ImportResult`: imported connection ownership plus WAL/checkpoint caution; source bytes never mutable.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define exact bounded file outcomes before allocation</name>
  <read_first>
    - `SeeQLite/.planning/phases/01-walking-skeleton-browser-harness/01-06-SUMMARY.md`; require `topology: PASS`.
    - `SeeQLite/.planning/decisions/ADR-001-sqlite-wasm-topology.md` if present; any unresolved Proposed/Rejected topology ADR blocks execution.
    - FILE-01..04 in `SeeQLite/.planning/REQUIREMENTS.md`.
    - D-09..D-11 in Phase 2 CONTEXT and File Outcome Copy Contract in 02-UI-SPEC.
    - PF-03/PF-04 in research PITFALLS.
  </read_first>
  <files>SeeQLite/src/features/database/file-policy.ts, SeeQLite/src/features/database/file-policy.test.ts, SeeQLite/src/engine/runtime-limits.ts, SeeQLite/tests/fixtures/file-outcomes/README.md</files>
  <behavior>
    - No/empty/multiple/directory inputs preserve current state; multiple items return the exact one-file response.
    - `size > 512 MiB` blocks before `arrayBuffer`; `256 MiB <= size <= 512 MiB` requests confirmation; smaller files continue.
    - SQLite magic plus later SQLite open establishes validity; extension is advisory; content/name-recognized WAL/SHM/journal sidecars are rejected.
    - Zero-byte, undersized, truncated, wrong-header, unusual-extension, read failure and size mismatch are distinct normalized outcomes.
  </behavior>
  <action>
    Before changing source, enforce the Phase 2 preflight: 01-06 summary must say `topology: PASS`, all three engines must be listed, and ADR-001 must be absent. If not, stop as blocked without implementing a fallback. Then, per D-09/D-10, implement pure `classifyFile` over bounded metadata and a small prefix read. Use binary-safe magic checks, case-insensitive extension hints, sidecar name and known header signatures, integer-safe byte constants, and exact threshold inclusivity. Never call full `arrayBuffer()` inside classification. Return cautious codes/copy keys; do not label corrupt/encrypted until SQLite open/catalog evidence can refine only to “may be encrypted, incomplete, or corrupt.” Document every binary fixture and expected outcome; do not require a global SQLite command in CI.
  </action>
  <verify>
    <automated>cd SeeQLite &amp;&amp; rg -q '^topology: PASS$' .planning/phases/01-walking-skeleton-browser-harness/01-06-SUMMARY.md &amp;&amp; test ! -e .planning/decisions/ADR-001-sqlite-wasm-topology.md &amp;&amp; npm run test -- src/features/database/file-policy.test.ts &amp;&amp; npm run typecheck</automated>
    <assertions>
      - Spies prove hard-block and multi/directory cases never call full file read or worker creation.
      - Tests cover exact threshold boundaries, Unicode/case names, renamed sidecar signatures, unreadable slice, and browser size mismatch.
    </assertions>
  </verify>
  <acceptance_criteria>
    - Every FILE-01..04 pre-open outcome is deterministic and maps to approved copy.
    - Extension never overrides bytes/SQLite result.
    - Limits live in one exported policy object.
  </acceptance_criteria>
  <failure_modes>
    - Prefix read rejection becomes browser-read-failure and permits a later candidate.
    - Integer overflow/non-finite test doubles fail closed.
    - Renamed sidecars are rejected when reliably identified; ambiguous bytes proceed to SQLite without false certainty.
  </failure_modes>
  <done>File intake can reject or warn safely before expensive allocation and without changing the current generation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove read-only import ownership, WAL handling, and cleanup with real WASM</name>
  <read_first>
    - Architecture “Database Import Spike” and STACK Spike 2.
    - D-11/D-12 and PF-03/PF-04.
    - Phase 1 worker/client contracts and actual package declarations.
  </read_first>
  <files>SeeQLite/src/engine/import-database.ts, SeeQLite/src/engine/protocol.ts, SeeQLite/src/engine/database-client.ts, SeeQLite/src/engine/sqlite.worker.ts, SeeQLite/tests/integration/file-lifecycle.spec.ts, SeeQLite/tests/fixtures/file-outcomes/README.md</files>
  <behavior>
    - Valid rollback and checkpointed WAL-mode main files open from a transferred copied buffer with source hash unchanged.
    - WAL-main outcome carries checkpoint caution; pending sidecar-only changes are never claimed present.
    - Corrupt/truncated/encrypted-looking/open/schema-read/OOM/transfer failures close partial resources and a later valid open works.
    - Repeated open/close returns worker/connection/statement/allocation/listener counts to baseline.
  </behavior>
  <action>
    Execute the bounded D-11 import spike against the exact pinned package: allocate from the transferred typed array, deserialize/open read-only without resize, determine explicit ownership/free sequence, and test copied-buffer-only WAL normalization only if SQLite semantics are proven. Never mutate the File or original main-thread bytes. If normalization is not correct in every engine, return checkpoint guidance through the unchanged protocol rather than misclassifying encryption. Instrument dev/test resource counters without logging file/schema/value data; guarantee cleanup in `finally` for partial allocation, connection, statement, request and worker paths.
  </action>
  <verify>
    <automated>cd SeeQLite &amp;&amp; npm run test:e2e -- --project=chromium --grep "file import ownership" &amp;&amp; npm run test:e2e -- --project=webkit --grep "file import ownership"</automated>
    <assertions>
      - Source before/after hashes match for valid, WAL and denied/failing cases.
      - Resource counters return to baseline across repeated success/failure cycles.
      - WAL fixture expected visible rows match checkpoint state, not sidecar-only state.
    </assertions>
  </verify>
  <acceptance_criteria>
    - Exact ownership/free strategy is recorded in the phase summary.
    - All open failure classes are recoverable by a subsequent valid open.
    - No OPFS, source write, retained ArrayBuffer, or retry loop appears.
  </acceptance_criteria>
  <failure_modes>
    - Unsupported package binding uses the researched transient read-only VFS contingency without changing protocol or source ownership.
    - OOM/worker crash settles once, terminates the epoch, and awaits explicit user reopen.
    - WAL uncertainty produces caution/rejection, never fabricated completeness.
  </failure_modes>
  <done>The copied-buffer import path is proven read-only, bounded, recoverable, and explicitly owned.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-01 | Tampering | file classification | mitigate | Bytes + SQLite open authoritative; extension advisory; source hash invariant. |
| T-02-02 | Information disclosure | retained file/errors | mitigate | File held in memory only; bounded safe metadata/copy; no logging/storage/network. |
| T-02-03 | Denial of service | allocation/import | mitigate | 256/512 MiB policy, prefix-first checks, transfer, OOM stop, no retry loop. |
| T-02-04 | Repudiation/race | worker generations | mitigate | attempt/request IDs, epochs, atomic invalidation, settle once. |
| T-02-SC | Tampering | npm packages | mitigate | No new dependency; existing exact lockfile only. |
</threat_model>

<verification>
Run typecheck/unit, real-WASM lifecycle integration in Chromium/WebKit, then the production three-engine file matrix. Inspect source hashes, resource counters, stale-event assertions, focus/live-region behavior, threshold allocation spies, and prohibited persistence/network surfaces.
</verification>

<success_criteria>
- FILE-02..04 pass for bounded validation, thresholds, WAL/sidecar handling and normalized import failures.
- Import ownership and cleanup are proven on the exact package with source bytes unchanged.
- No prohibited persistence, full read before hard block, retained import buffer, integrity scan or sidecar merge is introduced.
</success_criteria>

<output>
Create `SeeQLite/.planning/phases/02-safe-database-lifecycle-catalog/02-01-SUMMARY.md` with the file-policy outcome matrix, exact threshold/allocation evidence, import ownership decision, WAL behavior, resource/source-hash evidence and exported symbols. Do not create a branch or commit.
</output>
