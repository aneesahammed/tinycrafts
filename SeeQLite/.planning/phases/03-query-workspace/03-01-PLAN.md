---
phase: 03-query-workspace
plan: 01
type: execute
wave: 1
depends_on: [02-03]
files_modified:
  - SeeQLite/src/query/query-types.ts
  - SeeQLite/src/query/query-scope.ts
  - SeeQLite/src/query/query-scope.test.ts
  - SeeQLite/src/engine/protocol.ts
  - SeeQLite/src/engine/statement-policy.ts
  - SeeQLite/tests/integration/query-statement-policy.spec.ts
autonomous: true
requirements: [SQL-02]
must_haves:
  truths:
    - "Selection wins over the full draft, and SQLite—not JavaScript—accepts exactly one non-empty parameter-free read-only statement."
    - "The existing Phase 2 prepareSingleReadOnly() remains the single statement/policy authority and finalizes every rejected handle."
    - "Comments, strings containing semicolons, malformed input, parameters, and true batches receive distinct recoverable outcomes."
  artifacts:
    - { path: "SeeQLite/src/query/query-types.ts", provides: "immutable QuerySnapshot and normalized query errors", exports: ["QuerySnapshot", "QueryUiError"] }
    - { path: "SeeQLite/src/query/query-scope.ts", provides: "selection/full-draft chooser only", exports: ["selectSubmissionSql"] }
    - { path: "SeeQLite/src/engine/statement-policy.ts", provides: "extended single SQLite statement authority", exports: ["prepareSingleReadOnly"] }
  key_links:
    - { from: "SeeQLite/src/query/query-scope.ts", to: "SeeQLite/src/engine/statement-policy.ts", via: "exact chosen SQL is passed unchanged to prepareSingleReadOnly()", pattern: "prepareSingleReadOnly" }
    - { from: "SeeQLite/src/engine/statement-policy.ts", to: "SeeQLite/src/engine/read-only-policy.ts", via: "prepare/tail/parameter/readonly checks run under the connection-lifetime authorizer", pattern: "prepareSingleReadOnly|readonly" }
  prohibitions:
    - "No JavaScript SQL parser, regex splitter, keyword authorization, second policy routine, parameter UI, or batch execution."
    - "No main-thread SQLite handle and no weakening/bypass of Phase 2 authorizer, limits, or cleanup."
---

<objective>Extend the inherited statement-policy contract for immutable Query snapshots and prove exact selection/full-draft and one-statement behavior before results/UI consume it.</objective>

<execution_context>@/Users/aneesahammed/.codex/gsd-core/workflows/execute-plan.md</execution_context>

<context>
@SeeQLite/AGENTS.md
@SeeQLite/.planning/REQUIREMENTS.md
@SeeQLite/.planning/research/ARCHITECTURE.md
@SeeQLite/.planning/phases/02-safe-database-lifecycle-catalog/02-02-SUMMARY.md
@SeeQLite/.planning/phases/03-query-workspace/03-CONTEXT.md
@SeeQLite/.planning/phases/03-query-workspace/03-UI-SPEC.md
@SeeQLite/.planning/phases/03-query-workspace/03-VALIDATION.md

<interfaces>Reuse `prepareSingleReadOnly()` from `SeeQLite/src/engine/statement-policy.ts`; it already owns SQLite prepare/tail/parameter/read-only/finalize semantics under `read-only-policy.ts`. Extend its typed outcome where necessary; do not create another query policy routine.</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Define immutable query scope and extend the inherited policy contract</name>
  <read_first>SeeQLite/AGENTS.md; SeeQLite/.planning/REQUIREMENTS.md SQL-02; SeeQLite/.planning/phases/03-query-workspace/03-CONTEXT.md D-19; SeeQLite/src/engine/statement-policy.ts; SeeQLite/src/engine/statement-policy.test.ts; SeeQLite/src/engine/protocol.ts</read_first>
  <files>SeeQLite/src/query/query-types.ts, SeeQLite/src/query/query-scope.ts, SeeQLite/src/query/query-scope.test.ts, SeeQLite/src/engine/statement-policy.ts</files>
  <behavior>Non-empty selection is exact; otherwise full draft is exact; chooser does not parse/trim/rewrite. Existing `prepareSingleReadOnly()` accepts one statement plus empty/comment tails and rejects empty/comment-only, second statement, named/numbered/positional parameters, unsafe/readonly-false, syntax, and limits while finalizing all handles.</behavior>
  <action>Write failing `query-scope.test.ts` cases first. Define immutable `QuerySnapshot {queryId,requestId,epoch,kind,sql,capturedAt}` and stable `QueryUiError`. Implement only the pure scope chooser. Extend `prepareSingleReadOnly()` in place for any Phase 3 normalized outcome fields, preserving its name, authorizer connection, SQLite tail loop, parameter count, readonly check, and cleanup ownership. Per D-19, no UI/editor parsing participates in authorization.</action>
  <verify><automated>cd SeeQLite &amp;&amp; npm run test -- src/query/query-scope.test.ts src/engine/statement-policy.test.ts &amp;&amp; npm run typecheck</automated></verify>
  <acceptance_criteria>Scope tests fail RED before implementation; inherited policy remains single authority; every rejection has exact code and finalizes; no input rewrite/parser exists.</acceptance_criteria>
  <failure_modes>If exact bindings cannot expose tail/parameter/readonly behavior, stop for an ADR; never duplicate or replace `prepareSingleReadOnly()` with string analysis.</failure_modes>
  <done>The exact selected/full SQL reaches one inherited SQLite statement authority.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove statement-policy outcomes against real WASM</name>
  <read_first>SeeQLite/src/engine/protocol.ts; SeeQLite/src/engine/statement-policy.ts; SeeQLite/src/engine/read-only-policy.ts; SeeQLite/src/engine/sqlite.worker.ts; SeeQLite/tests/integration/sqlite-worker.spec.ts</read_first>
  <files>SeeQLite/src/engine/protocol.ts, SeeQLite/tests/integration/query-statement-policy.spec.ts</files>
  <behavior>Real WASM covers comments/strings/semicolons, CTE, parameters, batches, malformed and denied SQL; nothing rejected is stepped; source bytes unchanged; next SELECT 1 succeeds; errors contain no stack/HTML.</behavior>
  <action>Add RED-first production-worker integration cases using committed fixtures and extend only the shared protocol error discriminants needed by `prepareSingleReadOnly()`. Assert prepared-handle counters and post-error health, not implementation mocks. Keep `query.execute` result wiring for Plan 03-02.</action>
  <verify><automated>cd SeeQLite &amp;&amp; npm run test:e2e -- tests/integration/query-statement-policy.spec.ts --project=chromium &amp;&amp; npm run typecheck</automated></verify>
  <acceptance_criteria>All SQL boundary/policy cases use real worker/WASM; source bytes unchanged; every failure recovers; no duplicate policy API appears.</acceptance_criteria>
  <failure_modes>Any stepped batch/write, unfinalized statement, leaked stack, or unhealthy next query is blocking.</failure_modes>
  <done>The inherited policy is proven ready for query and plan pipelines.</done>
</task>
</tasks>

<artifacts_produced>| Artifact | Symbol | Consumer |
|---|---|---|
| `query-types.ts` | `QuerySnapshot` | controller/result/plan/cancel/history |
| `query-scope.ts` | `selectSubmissionSql()` | Query UI |
| `statement-policy.ts` | extended `prepareSingleReadOnly()` | worker query.execute/query.explain |
</artifacts_produced>

<source_audit>| Source | ID | Coverage | Status |
|---|---|---|---|
| REQ | SQL-02 | Tasks 1–2 | COVERED |
| CONTEXT | D-19 | Tasks 1–2 | COVERED |
| RESEARCH | Single-Statement Preparation/PF-14 | Tasks 1–2 | COVERED |
| GOAL | one exact statement | Tasks 1–2 | COVERED |
</source_audit>

<threat_model>| Threat | Category | Mitigation |
|---|---|---|
| T-03-01-01 | Tampering/Elevation | Existing SQLite authorizer + `prepareSingleReadOnly()` tail/parameter/readonly/limit checks. |
| T-03-01-02 | Information disclosure | Stable bounded error DTO, no stack/HTML/log/network. |
| T-03-SC | Supply chain | No installs or lockfile changes. |
</threat_model>

<verification>Run both task commands, then the statement-policy suite in Chromium/Firefox/WebKit and inspect handle/source-byte recovery evidence.</verification>
<success_criteria>SQL-02 and D-19 pass; `prepareSingleReadOnly()` is the only policy routine; contracts are ready for 03-02/03-04.</success_criteria>
<output>Create `SeeQLite/.planning/phases/03-query-workspace/03-01-SUMMARY.md`.</output>
