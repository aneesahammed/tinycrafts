---
phase: 03-query-workspace
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - SeeQLite/src/query/result-types.ts
  - SeeQLite/src/engine/result-reader.ts
  - SeeQLite/src/engine/result-reader.test.ts
  - SeeQLite/src/engine/protocol.ts
  - SeeQLite/src/engine/sqlite.worker.ts
  - SeeQLite/tests/integration/result-values.spec.ts
  - SeeQLite/tests/fixtures/result-values/README.md
autonomous: true
requirements: [RES-01, RES-02]
must_haves:
  truths:
    - "query.execute reuses prepareSingleReadOnly(), steps through result-reader, and returns only a bounded typed positional protocol DTO."
    - "Duplicate labels, signed 64-bit integers, NULL, embedded NUL, invalid UTF-8 replacement metadata, BLOB/text previews, and zero-row columns remain explicit."
    - "Rows, columns, cells, payload bytes, text bytes, and BLOB bytes are independently bounded and statements finalize on every path."
  artifacts:
    - { path: "SeeQLite/src/query/result-types.ts", provides: "byte-aware positional value/result DTOs", exports: ["SqlValue", "QueryResult", "RESULT_LIMITS"] }
    - { path: "SeeQLite/src/engine/result-reader.ts", provides: "bounded SQLite C-value stepping", exports: ["readBoundedResult"] }
  key_links:
    - { from: "SeeQLite/src/engine/sqlite.worker.ts", to: "SeeQLite/src/engine/statement-policy.ts", via: "query.execute first calls prepareSingleReadOnly()", pattern: "query.execute.*prepareSingleReadOnly" }
    - { from: "SeeQLite/src/engine/sqlite.worker.ts", to: "SeeQLite/src/engine/result-reader.ts", via: "permitted statement is stepped by readBoundedResult()", pattern: "readBoundedResult" }
    - { from: "SeeQLite/src/engine/result-reader.ts", to: "SeeQLite/src/engine/protocol.ts", via: "typed bounded QueryResult response", pattern: "QueryResult" }
  prohibitions:
    - "No object rows, duplicate-label collapse, date inference, full large-cell copy, row-only cap, main-thread stepping, or duplicate statement policy."
---

<objective>Implement the real query.execute → inherited statement policy → byte-aware result reader → protocol pipeline with exact SQLite value semantics and independent budgets.</objective>
<execution_context>@/Users/aneesahammed/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@SeeQLite/AGENTS.md
@SeeQLite/.planning/REQUIREMENTS.md
@SeeQLite/.planning/phases/03-query-workspace/03-CONTEXT.md
@SeeQLite/.planning/phases/03-query-workspace/03-VALIDATION.md
@SeeQLite/.planning/phases/03-query-workspace/03-01-SUMMARY.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Define byte-aware exact positional result contracts and accounting</name>
  <read_first>SeeQLite/.planning/REQUIREMENTS.md RES-01/RES-02; SeeQLite/.planning/phases/03-query-workspace/03-CONTEXT.md D-22/D-23; SeeQLite/.planning/research/ARCHITECTURE.md Query Execution; SeeQLite/.planning/research/PITFALLS.md PF-03/PF-07; SeeQLite/src/engine/protocol.ts</read_first>
  <files>SeeQLite/src/query/result-types.ts, SeeQLite/src/engine/result-reader.ts, SeeQLite/src/engine/result-reader.test.ts, SeeQLite/tests/fixtures/result-values/README.md</files>
  <behavior>Columns stay ordered/duplicated/available at zero rows. INTEGER is bigint. NULL/REAL/TEXT/BLOB distinct. TEXT includes sourceByteLength, heldByteLength, preview, truncated, hadDecodingReplacement; embedded NUL retained; invalid UTF-8 decoding is replacement-marked without losing original byte lengths. Text fixtures `NaN`, `Infinity`, `-Infinity` remain text and are never coerced. BLOB has original/held lengths. All six caps and boundaries are exact.</behavior>
  <action>Write failing accounting/value tables first. Define discriminated DTOs and `RESULT_LIMITS` (1,000 rows, 250 columns, 50,000 cells, 8 MiB protocol payload, 64 KiB source text bytes held, 256 BLOB bytes). In `readBoundedResult()`, inspect SQLite runtime type and `sqlite3_column_bytes()` before copying. Decode held UTF-8 explicitly with replacement detection while preserving source/held byte lengths and embedded NUL; do not claim original invalid bytes are reconstructable. Count deterministic protocol bytes conservatively before append and return all reached truncation reasons. Finalize through one ownership path.</action>
  <verify><automated>cd SeeQLite &amp;&amp; npm run test -- src/engine/result-reader.test.ts &amp;&amp; npm run typecheck</automated></verify>
  <acceptance_criteria>RED-first tests cover every runtime kind/value/boundary, invalid UTF-8, embedded NUL, NaN/Infinity text, duplicate labels, zero rows, first huge cell, and finalization; responses never exceed caps.</acceptance_criteria>
  <failure_modes>If exact byte/64-bit bindings are absent, stop for ADR; never coerce, undercount, or copy full values. Invalid UTF-8 uses explicit replacement metadata, not silent normalization.</failure_modes>
  <done>Result DTOs preserve exact type/position and honest byte-aware preview semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire query.execute through policy, result reader, protocol, and real worker tests</name>
  <read_first>SeeQLite/src/engine/statement-policy.ts; SeeQLite/src/engine/result-reader.ts; SeeQLite/src/engine/protocol.ts; SeeQLite/src/engine/sqlite.worker.ts; SeeQLite/tests/integration/query-statement-policy.spec.ts</read_first>
  <files>SeeQLite/src/engine/protocol.ts, SeeQLite/src/engine/sqlite.worker.ts, SeeQLite/tests/integration/result-values.spec.ts</files>
  <behavior>query.execute validates snapshot/epoch, calls `prepareSingleReadOnly()`, transfers its statement to `readBoundedResult()`, returns typed QueryResult, and publishes no partial DTO on error. Real worker covers values/bounds and next-query health.</behavior>
  <action>Add the RED-first integration fixture/test before dispatcher changes. Extend the shared command/response union with query.execute and QueryResult. In the worker, explicitly call `prepareSingleReadOnly()` then `readBoundedResult()`; do not recreate tail/parameter/readonly checks. Guarantee exactly one response/finalization. Test 250/251 columns, 1,000/1,001 rows, 50,000 cells, 8 MiB accounting, 64 KiB text, 256 BLOB bytes, invalid UTF-8, embedded NUL, text non-finite spellings, allocation error, stale epoch, source hash, and `SELECT 1` recovery.</action>
  <verify><automated>cd SeeQLite &amp;&amp; npm run test:e2e -- tests/integration/result-values.spec.ts --project=chromium &amp;&amp; npm run typecheck</automated></verify>
  <acceptance_criteria>The exact key link query.execute→prepareSingleReadOnly→readBoundedResult→protocol is source/runtime proven; no partial/oversized response; every path finalizes once and recovers.</acceptance_criteria>
  <failure_modes>Protocol clone/allocation failure becomes bounded recoverable error and settles once. Any policy bypass or full large-cell transfer blocks completion.</failure_modes>
  <done>The database worker returns exact bounded results through one explicit pipeline.</done>
</task>
</tasks>

<artifacts_produced>| Artifact | Symbol | Consumer |
|---|---|---|
| `result-types.ts` | `SqlValue`, `QueryResult`, `RESULT_LIMITS` | controller/grid/export |
| `result-reader.ts` | `readBoundedResult()` | worker query.execute |
</artifacts_produced>
<source_audit>| Source | ID | Coverage | Status |
|---|---|---|---|
| REQ | RES-01/RES-02 | Tasks 1–2 | COVERED |
| CONTEXT | D-22/D-23 | Tasks 1–2 | COVERED |
| RESEARCH | PF-03/PF-07 | Tasks 1–2 | COVERED |
| GOAL | exact bounded result | Tasks 1–2 | COVERED |
</source_audit>
<threat_model>| Threat | Category | Mitigation |
|---|---|---|
| T-03-02-01 | Tampering/Disclosure | typed positional DTO, byte-aware replacement/truncation metadata. |
| T-03-02-02 | DoS | six independent caps, pre-copy inspection, unconditional finalization. |
| T-03-SC | Supply chain | No installs. |
</threat_model>
<verification>Run unit and real-WASM integration in all three engines; inspect DTO/payload/handle/source-byte evidence.</verification>
<success_criteria>RES-01/02 and D-22/23 pass; explicit execute pipeline is proven; contracts are ready for 03-04/03-05.</success_criteria>
<output>Create `SeeQLite/.planning/phases/03-query-workspace/03-02-SUMMARY.md`.</output>
