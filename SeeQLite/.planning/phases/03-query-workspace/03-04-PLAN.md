---
phase: 03-query-workspace
plan: 04
type: execute
wave: 3
depends_on: [03-01, 03-02]
files_modified:
  - SeeQLite/src/query/query-controller.ts
  - SeeQLite/src/query/query-controller.test.ts
  - SeeQLite/src/engine/database-client.ts
  - SeeQLite/src/state/app-reducer.ts
  - SeeQLite/src/query/SqlEditor.tsx
  - SeeQLite/src/query/QueryWorkspace.tsx
  - SeeQLite/src/query/QueryWorkspace.test.tsx
  - SeeQLite/src/styles/query.css
  - SeeQLite/src/App.tsx
  - SeeQLite/tests/e2e/query-authoring.spec.ts
autonomous: true
requirements: [SQL-01, SQL-03, SQL-04]
must_haves:
  truths:
    - "A user reaches Query from App navigation, authors with current-catalog completion, and runs the exact immutable snapshot."
    - "Exactly one Run/Plan command is active; stale request/query/epoch outcomes cannot alter reducer, output, announcements, or history."
    - "App → query-controller → DatabaseClient owns serialized command dispatch and correlated lifecycle state; successful QueryResult publication is deferred exclusively to 03-05."
  artifacts:
    - { path: "SeeQLite/src/query/query-controller.ts", provides: "serialized correlated query orchestration", exports: ["createQueryController"] }
    - { path: "SeeQLite/src/query/SqlEditor.tsx", provides: "lazy SQLite CodeMirror/current-catalog completion", exports: ["SqlEditor"] }
    - { path: "SeeQLite/src/query/QueryWorkspace.tsx", provides: "responsive command/error/status/output host", exports: ["QueryWorkspace"] }
  key_links:
    - { from: "SeeQLite/src/App.tsx", to: "SeeQLite/src/query/QueryWorkspace.tsx", via: "Query navigation/mode route", pattern: "QueryWorkspace" }
    - { from: "SeeQLite/src/query/query-controller.ts", to: "SeeQLite/src/engine/database-client.ts", via: "dispatch immutable query.execute snapshot", pattern: "query.execute" }
    - { from: "SeeQLite/src/query/query-controller.ts", to: "SeeQLite/src/state/app-reducer.ts", via: "correlated command lifecycle event only", pattern: "query.*status|requestId|queryId|epoch" }
    - { from: "SeeQLite/src/state/app-reducer.ts", to: "SeeQLite/src/query/QueryWorkspace.tsx", via: "current command/error/status render", pattern: "query.*status" }
  prohibitions:
    - "No command queue, stale completion, raw HTML/stack/logging, global store, textarea fallback, or editor authorization."
---

<objective>Deliver App-to-Query authoring plus serialized, correlated command lifecycle state. QueryResult publication belongs exclusively to 03-05; CAT-05 remains copy-only and every generated-SQL insertion/replacement confirmation is deferred to ER-05.</objective>
<scope_rationale>This 10-file plan is a cohesive authoring/command slice: App navigation, CodeMirror, controller serialization, lifecycle reducer state, and production command evidence must land together to make Run/Plan operable. Result publication and rendering are explicitly excluded and owned by 03-05.</scope_rationale>
<execution_context>@/Users/aneesahammed/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@SeeQLite/AGENTS.md
@SeeQLite/.planning/phases/03-query-workspace/03-UI-SPEC.md
@SeeQLite/.planning/phases/03-query-workspace/03-01-SUMMARY.md
@SeeQLite/.planning/phases/03-query-workspace/03-02-SUMMARY.md
@SeeQLite/src/catalog/types.ts
@SeeQLite/src/catalog/search.ts
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Serialize and correlate command lifecycle state</name>
  <read_first>SeeQLite/.planning/REQUIREMENTS.md SQL-03/SQL-04; SeeQLite/.planning/phases/03-query-workspace/03-CONTEXT.md D-20; SeeQLite/src/engine/database-client.ts; SeeQLite/src/state/app-reducer.ts; SeeQLite/src/engine/protocol.ts</read_first>
  <files>SeeQLite/src/query/query-controller.ts, SeeQLite/src/query/query-controller.test.ts, SeeQLite/src/engine/database-client.ts, SeeQLite/src/state/app-reducer.ts</files>
  <behavior>Run captures request/query/epoch/SQL once; active command rejects another Run/Plan without queue; client completion settles controller/lifecycle state only when all correlation fields match; duplicate/unknown/old responses do nothing; disposal settles once. No successful QueryResult is published here.</behavior>
  <action>Write RED-first controller/reducer race tables. Implement `createQueryController()` over the existing `DatabaseClient`: readiness + active-command guard, immutable scope capture from 03-01, query.execute dispatch, and exact correlation before lifecycle reducer events. Extend app-reducer discriminants for idle/running/planning/stopping/rehydrating/failed only. Test double Run, Run→Plan, Plan→replace, SQL edit, old epoch, duplicate ID, unknown ID, error lifecycle, and disposal. Leave successful QueryResult publication RED and unimplemented until 03-05; do not rely on disabled buttons for serialization.</action>
  <verify><automated>cd SeeQLite &amp;&amp; npm run test -- src/query/query-controller.test.ts &amp;&amp; npm run typecheck</automated></verify>
  <acceptance_criteria>One active command; dispatch and lifecycle correlation are complete; stale/duplicate events change no UI/history/announcement; promises settle once; no successful QueryResult publication exists before 03-05.</acceptance_criteria>
  <failure_modes>Any path without four-field correlation or any implicit queue blocks completion; protocol/client remains one shared channel.</failure_modes>
  <done>Command dispatch and lifecycle state are race-safe without owning results.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build lazy current-catalog CodeMirror and persistent command UX</name>
  <read_first>SeeQLite/.planning/REQUIREMENTS.md SQL-01/04; SeeQLite/.planning/phases/03-query-workspace/03-CONTEXT.md D-19/D-21; SeeQLite/.planning/phases/03-query-workspace/03-UI-SPEC.md Editor/Shortcuts/State Matrix; SeeQLite/src/catalog/types.ts; SeeQLite/src/catalog/search.ts; SeeQLite/src/query/query-controller.ts</read_first>
  <files>SeeQLite/src/query/SqlEditor.tsx, SeeQLite/src/query/QueryWorkspace.tsx, SeeQLite/src/query/QueryWorkspace.test.tsx, SeeQLite/src/styles/query.css</files>
  <behavior>Lazy SQLite CodeMirror; label/undo/selection/IME/current-catalog completion; generation closes/swaps completion; exact Run/Plan shortcuts and contextual Escape; persistent readiness/validation/error/status; chunk failure retains draft and Retry; responsive themes/reduced motion.</behavior>
  <action>Write `QueryWorkspace.test.tsx` RED cases before components. Per D-21, configure approved CodeMirror SQLite dialect and derive completion solely from current normalized `Catalog` exact names/kinds; atomically replace source on epoch. Bind platform shortcuts and `aria-keyshortcuts` without overriding IME/browser/dialog/completion behavior. Build QueryWorkspace around lifecycle states with exact UI copy, one polite region, hard-error alert, and draft/caret preservation. Use only inherited tokens and no lesser editor fallback.</action>
  <verify><automated>cd SeeQLite &amp;&amp; npm run test -- src/query/QueryWorkspace.test.tsx &amp;&amp; npm run typecheck</automated></verify>
  <acceptance_criteria>Current completion only; keyboard/IME/status/error/focus copy matches UI spec; hostile text escaped; no overflow at target widths/themes/zoom.</acceptance_criteria>
  <failure_modes>Chunk failure exposes Retry while draft stays outside editor instance; old completion cannot be accepted; any raw sink or lost draft blocks completion.</failure_modes>
  <done>Accessible authoring controls submit through the serialized controller and recover from all errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire App Query navigation through production E2E</name>
  <read_first>SeeQLite/src/App.tsx; SeeQLite/.planning/phases/03-query-workspace/03-UI-SPEC.md responsive navigation; SeeQLite/src/query/QueryWorkspace.tsx; SeeQLite/.planning/phases/02-safe-database-lifecycle-catalog/02-CONTEXT.md D-18</read_first>
  <files>SeeQLite/src/App.tsx, SeeQLite/tests/e2e/query-authoring.spec.ts</files>
  <behavior>App exposes Query mode; mode navigation preserves draft/status; E2E opens database→Query→completion→Run→finished lifecycle and exercises errors/rapid/stale/responsive/a11y/privacy without asserting a rendered result. Catalog actions remain copy-only and never insert or confirm replacement.</behavior>
  <action>Add the production E2E command path RED before wiring App navigation. Connect App Query mode to QueryWorkspace/reducer. Cover keyboard, mobile/tablet/desktop, editor-load failure, current-catalog swap, normalized lifecycle errors, double action, hostile markers, axe, network/console/storage. Assert a production worker command completes and the next command runs, but leave result value/grid assertions to 03-05. Add a CAT-05 copy-only regression.</action>
  <verify><automated>cd SeeQLite &amp;&amp; npm run test:e2e -- tests/e2e/query-authoring.spec.ts --project=chromium &amp;&amp; npm run typecheck</automated></verify>
  <acceptance_criteria>End-to-end App→Query navigation and serialized command lifecycle are proven; draft/current state survives mode changes; CAT-05 is copy-only; no result-publication assertion or ownership leaks from 03-05.</acceptance_criteria>
  <failure_modes>Any Phase 3 catalog draft mutation or replacement-confirmation behavior, dead navigation, mocked engine, or dev-only evidence blocks completion.</failure_modes>
  <done>The real production app offers a complete safe author-to-command slice.</done>
</task>
</tasks>

<artifacts_produced>| Artifact | Contract |
|---|---|
| `query-controller.ts` | serialized correlated command path |
| `SqlEditor.tsx` | current-catalog CodeMirror |
| `QueryWorkspace.tsx` | authoring and command/error/status UI host |
| `query-authoring.spec.ts` | App→Query real-engine command-lifecycle proof and CAT-05 copy-only regression |
</artifacts_produced>
<source_audit>| Source | ID | Coverage | Status |
|---|---|---|---|
| REQ | SQL-01/03/04 | Tasks 1–3 | COVERED |
| CONTEXT | D-20/D-21 | Tasks 1–3 | COVERED |
| GOAL | author and dispatch one coherent query; result publication excluded | Tasks 1–3 | COVERED |
| UI-SPEC | editor/navigation/error/focus | Tasks 2–3 | COVERED |
</source_audit>
<threat_model>| Threat | Category | Mitigation |
|---|---|---|
| T-03-04-01 | Spoofing/Tampering | request/query/epoch correlation and once settlement. |
| T-03-04-02 | Disclosure/XSS | bounded escaped catalog/error text; no log/network/storage. |
| T-03-SC | Supply chain | Existing exact-pinned CodeMirror; no install. |
</threat_model>
<verification>Run task commands then production E2E in all three engines with axe/theme/responsive/privacy capture.</verification>
<success_criteria>SQL-01/03/04 and D-20/21 pass; App navigation plus command serialization/correlation are runtime proven; 03-05 remains the sole QueryResult publication owner; CAT-05 remains copy-only.</success_criteria>
<output>Create `SeeQLite/.planning/phases/03-query-workspace/03-04-SUMMARY.md`.</output>
