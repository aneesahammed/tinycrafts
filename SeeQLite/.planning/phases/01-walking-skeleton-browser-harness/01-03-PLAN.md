---
phase: 01-walking-skeleton-browser-harness
plan: 03
type: execute
wave: 4
depends_on: [01-05]
files_modified:
  - SeeQLite/vite.config.ts
  - SeeQLite/index.html
  - SeeQLite/public/manifest.webmanifest
  - SeeQLite/public/icon.svg
  - SeeQLite/public/icon-192.png
  - SeeQLite/public/icon-512.png
  - scripts/build-pages.mjs
  - scripts/verify-pages-build.mjs
  - .github/workflows/pages.yml
autonomous: true
requirements: [PLAT-02, TEST-01]
must_haves:
  truths:
    - "The exact Pages artifact assembles at `/seeqlite/` with relative worker/WASM/manifest/icon assets."
    - "The Node 24 shared pipeline builds SeeQLite and keeps DataDuck green."
  artifacts:
    - { path: "scripts/verify-pages-build.mjs", provides: "SeeQLite worker/WASM/subpath and DataDuck artifact gate" }
  key_links:
    - { from: "scripts/build-pages.mjs", to: "SeeQLite/dist", via: "copy to .pages-build/seeqlite", pattern: "seeqlite" }
    - { from: ".github/workflows/pages.yml", to: "scripts/verify-pages-build.mjs", via: "Node 24 install/build/verify job", pattern: "node-version: 24" }
  prohibitions:
    - "No landing-page catalogue mutation, service-worker database caching, OPFS, runtime CDN, analytics, remote font, or broad CSP capability."
    - "No passing evidence from a dev server, mocked engine, one browser, hidden retry, or silent main-thread fallback."
---

<objective>
Assemble and statically verify the exact TinyCrafts production artifact and shared pipeline.

Purpose: Implement D-07 and enforce D-08 before feature breadth.
Output: `/seeqlite/` Pages assembly, manifest/icons, artifact verifier, Node 24 dual-app workflow, and preserved DataDuck gates.
</objective>

## Phase Goal

**As a** TinyCrafts visitor, **I want to** use SeeQLite from the real static subpath in my supported browser, **so that** the local-only worker experience is proven under release conditions.

<execution_context>
@/Users/aneesahammed/.codex/gsd-core/workflows/execute-plan.md
@/Users/aneesahammed/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@SeeQLite/AGENTS.md
@SeeQLite/.planning/REQUIREMENTS.md
@SeeQLite/.planning/research/STACK.md
@SeeQLite/.planning/research/PITFALLS.md
@SeeQLite/.planning/phases/01-walking-skeleton-browser-harness/01-CONTEXT.md
@SeeQLite/.planning/phases/01-walking-skeleton-browser-harness/01-UI-SPEC.md
@SeeQLite/.planning/phases/01-walking-skeleton-browser-harness/01-02-SUMMARY.md
@scripts/build-pages.mjs
@scripts/verify-pages-build.mjs
@.github/workflows/pages.yml
@dataduck/vite.config.js
</context>

<artifacts_produced>
- `manifest.webmanifest` and same-origin icons: install metadata and PLAT-02 path coverage, without an offline claim.
- `build-pages.mjs` / `verify-pages-build.mjs`: exact `/seeqlite/` assembly, relative-asset validation, negative verifier mode and preserved sibling gates.
- `pages.yml`: Node 24 install/test/build/assembly ordering for SeeQLite and DataDuck.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Assemble and verify the exact `/seeqlite/` Pages artifact</name>
  <read_first>
    - `scripts/build-pages.mjs`, `scripts/verify-pages-build.mjs`, `.github/workflows/pages.yml`.
    - `dataduck/vite.config.js` for relative asset handling and root asset emission.
    - PLAT-02 and TEST-01 in `SeeQLite/.planning/REQUIREMENTS.md`.
  </read_first>
  <files>SeeQLite/vite.config.ts, SeeQLite/index.html, SeeQLite/public/manifest.webmanifest, SeeQLite/public/icon.svg, SeeQLite/public/icon-192.png, SeeQLite/public/icon-512.png, scripts/build-pages.mjs, scripts/verify-pages-build.mjs, .github/workflows/pages.yml</files>
  <action>
    Per D-01/D-07, extend the root assembler to run `npm run build` in SeeQLite and copy `SeeQLite/dist` to `.pages-build/seeqlite` without changing existing static tools or DataDuck destinations. Extend artifact verification to require HTML, manifest, icons, hashed JS/CSS, module worker, SQLite WASM, sourcemap policy, relative HTML references, correct manifest targets, no source/package files, no macOS metadata, and no root-relative SeeQLite asset. Update CI to Node 24, cache/install both lockfiles, run both applications' tests/build/bundle checks, then assemble/verify. Preserve the existing DataDuck assertions.

    Emit install metadata and icons now because PLAT-02 requires path/content-type coverage; do not register a service worker or claim offline use. Set a narrow meta CSP based on the built artifact: same-origin default/script/worker/connect/font/image, `object-src 'none'`, restrictive base/form directives, and `wasm-unsafe-eval` only if the measured engines require it. Do not add broad eval or claim header-only framing protection.
  </action>
  <verify>
    <automated>node scripts/build-pages.mjs &amp;&amp; node scripts/verify-pages-build.mjs &amp;&amp; test -f .pages-build/seeqlite/index.html</automated>
    <assertions>
      - SeeQLite assets resolve under `.pages-build/seeqlite`; existing TinyCrafts tools and DataDuck still exist.
      - Worker and WASM are separate emitted files and HTML/manifest use relative references.
      - CI Node/cache/install paths cover both lockfiles.
    </assertions>
  </verify>
  <acceptance_criteria>
    - Direct navigation/refresh at `/seeqlite/` has no root-relative 404 or wrong MIME.
    - Artifact verifier fails on missing worker/WASM/manifest/icon and on leaked source files.
    - Node 24 DataDuck tests/build/bundle check remain required.
  </acceptance_criteria>
  <failure_modes>
    - Missing or stale asset pairs fail assembly; no hidden retry or fallback copy.
    - A CSP-blocked required asset fails verification rather than weakening the policy broadly.
    - Existing sibling destinations and assertions are not renamed or removed.
  </failure_modes>
  <done>The root pipeline produces and verifies a production-shaped SeeQLite artifact beside unchanged sibling tools.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove the assembler rejects broken SeeQLite assets without weakening sibling checks</name>
  <read_first>
    - The updated `scripts/verify-pages-build.mjs` SeeQLite and DataDuck assertion tables.
    - TEST-01 diagnostic and sibling-regression requirements.
  </read_first>
  <files>scripts/verify-pages-build.mjs, scripts/build-pages.mjs, .github/workflows/pages.yml</files>
  <behavior>
    - A missing/renamed worker, WASM, manifest icon, root-relative reference, leaked source file or absent DataDuck artifact makes verification non-zero with the exact path/type.
    - Restoring the artifact passes without deleting or weakening existing sibling assertions.
  </behavior>
  <action>Add fixture-driven negative assertions around the verifier's pure path/reference checks or a controlled temporary artifact mutation in its test mode. Ensure CI orders both installs/tests/builds before assembly and never uploads on verifier failure. Preserve every existing root/static-tool/DataDuck required/forbidden assertion.</action>
  <verify><automated>node scripts/build-pages.mjs &amp;&amp; node scripts/verify-pages-build.mjs &amp;&amp; VERIFY_PAGES_NEGATIVE_CASES=1 node scripts/verify-pages-build.mjs</automated></verify>
  <acceptance_criteria>Every required SeeQLite asset class and sibling regression has a passing and failing verifier assertion.</acceptance_criteria>
  <failure_modes>Negative mode must operate on an isolated temporary artifact and restore/leave the real `.pages-build` intact.</failure_modes>
  <done>Artifact verification is itself regression-tested and upload-blocking.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-01-10 | Tampering | Pages artifact | mitigate | Required/forbidden asset manifest, MIME/path/CSP checks, exact lockfiles. |
| T-01-12 | Elevation of privilege | worker/CSP | mitigate | Same-origin relative assets and narrow CSP; runtime negative tests execute in 01-06. |
| T-01-SC | Tampering | CI dependencies | mitigate | `npm ci`, exact pins, Node 24, researched package sources, DataDuck regression. |
</threat_model>

<verification>
`cd SeeQLite && npm ci && npm run typecheck && npm run build` passes; root assembly/verification and DataDuck test/build/bundle pass under Node 24. Static verification finds every required relative asset and rejects source leakage/root-relative references.
</verification>

<success_criteria>
- `/seeqlite/` is assembled with relative HTML/worker/WASM/manifest/icon assets and verified before browser execution.
- Node 24 installs/builds both applications and preserves DataDuck gates.
- Plan 01-06 can run browser/privacy/visual/performance evidence against this exact artifact.
</success_criteria>

<output>
Create `SeeQLite/.planning/phases/01-walking-skeleton-browser-harness/01-03-SUMMARY.md` with the assembled artifact inventory, relative-path/CSP/static-verifier evidence, negative verifier cases, CI ordering and preserved sibling/DataDuck gate results. Do not create a branch or commit.
</output>
