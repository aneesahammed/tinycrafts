---
phase: 01-walking-skeleton-browser-harness
plan: 06
type: execute
wave: 5
depends_on: [01-03]
files_modified:
  - SeeQLite/scripts/check-seeqlite-bundle.mjs
  - SeeQLite/tests/e2e/capability.spec.ts
  - SeeQLite/tests/e2e/privacy.spec.ts
  - SeeQLite/tests/e2e/production.spec.ts
  - SeeQLite/tests/e2e/performance.spec.ts
  - SeeQLite/tests/e2e/visual-accessibility.spec.ts
  - SeeQLite/tests/e2e/visual-accessibility.spec.ts-snapshots
  - SeeQLite/package.json
  - SeeQLite/.planning/decisions/ADR-001-sqlite-wasm-topology.md
autonomous: true
requirements: [PLAT-01, PLAT-02, PLAT-03, PLAT-04, UX-01, TEST-01]
must_haves:
  truths:
    - "The exact Pages artifact passes real-WASM readiness in Chromium, Firefox and WebKit with `crossOriginIsolated=false`."
    - "Privacy/CSP/request/content-type capture proves database-derived markers remain local."
    - "UX-01 has executable visual/accessibility evidence across themes, four viewports, 200% zoom, forced colors, reduced motion, keyboard and axe."
  artifacts:
    - { path: "SeeQLite/tests/e2e/visual-accessibility.spec.ts", provides: "executable UX-01 matrix and screenshots" }
    - { path: "SeeQLite/tests/e2e/privacy.spec.ts", provides: "local-only marker proof" }
    - { path: "SeeQLite/scripts/check-seeqlite-bundle.mjs", provides: "asset/lazy/source scan and size baseline" }
  key_links:
    - { from: "SeeQLite/tests/e2e/production.spec.ts", to: ".pages-build/seeqlite", via: "exact built artifact baseURL", pattern: "seeqlite" }
  prohibitions:
    - "No dev-server evidence, engine mock, skipped/retried browser, hidden privacy payload in attachments, Blob/data worker, or silent topology fallback."
---

<objective>
Close Phase 1 with production browser, privacy, accessibility, visual, bundle, performance and sibling-regression evidence.

Purpose: Execute D-07/D-08 and make every UX-01 claim machine-checkable before Phase 2.
Output: three-engine suite, exhaustive UX matrix/screenshots, privacy/CSP capture, bundle/performance baseline, DataDuck gate, and conditional ADR-001 failure path.
</objective>

<execution_context>
@/Users/aneesahammed/.codex/gsd-core/workflows/execute-plan.md
@/Users/aneesahammed/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@SeeQLite/AGENTS.md
@SeeQLite/.planning/REQUIREMENTS.md
@SeeQLite/.planning/phases/01-walking-skeleton-browser-harness/01-CONTEXT.md
@SeeQLite/.planning/phases/01-walking-skeleton-browser-harness/01-UI-SPEC.md
@SeeQLite/.planning/phases/01-walking-skeleton-browser-harness/01-03-SUMMARY.md
@SeeQLite/.planning/research/PITFALLS.md
</context>

<artifacts_produced>
- Production failure capture with metadata-only attachments.
- UX screenshot baselines for initial and readiness states in both themes at all four viewports.
- Phase 1 bundle/readiness/long-task baseline.
- Conditional ADR-001 only when topology fails; passing topology records no ADR.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove production topology, capability and privacy in three engines</name>
  <read_first>
    - D-07/D-08, PLAT-01..04, TEST-01.
    - PITFALLS PF-05/PF-12/PF-14.
    - 01-03 built artifact summary and 01-02 worker summary.
  </read_first>
  <files>SeeQLite/tests/e2e/capability.spec.ts, SeeQLite/tests/e2e/privacy.spec.ts, SeeQLite/tests/e2e/production.spec.ts, SeeQLite/.planning/decisions/ADR-001-sqlite-wasm-topology.md</files>
  <behavior>
    - Three engines run real sample/local readiness at `/seeqlite/`; one database and one emitted module-probe worker use same-origin URLs; `crossOriginIsolated` is false.
    - Required capabilities independently fail before intake; optional storage/SW failure remains usable.
    - Unique filename/schema/SQL/value markers are absent from request URLs/headers/bodies, page URL/title, console, localStorage, Cache Storage and service-worker keys.
    - Console/pageerror/CSP/failed request/root path/wrong MIME/data or cross-origin worker fails with metadata-only attachment.
  </behavior>
  <action>
    Test only the exact built artifact in Chromium, Firefox and WebKit. Install network/console/pageerror/CSP/worker/content-type capture before navigation and fail on any unallowlisted event. Verify the module-probe is an emitted same-origin worker and source/bundle contains no Blob/data worker construction. If any required engine/topology fails, stop the phase and create/update ADR-001 with status Proposed, exact artifact/browser evidence, constraints and comparison of vendored official build, `sql.js`, and hosting change; record Phase 2 as blocked in the summary without editing ROADMAP/STATE. If all pass, assert ADR-001 does not exist.
  </action>
  <verify>
    <automated>cd SeeQLite &amp;&amp; npm run test:e2e -- --project=chromium --grep "production|privacy|capability" &amp;&amp; npm run test:e2e -- --project=firefox --grep "production|privacy|capability" &amp;&amp; npm run test:e2e -- --project=webkit --grep "production|privacy|capability"</automated>
    <assertions>
      - No retries/skips/mocks; all marker scans pass.
      - Same-origin probe/database workers and WASM MIME are recorded.
      - Passing topology has no ADR-001; failure has complete Proposed ADR and stops.
    </assertions>
  </verify>
  <acceptance_criteria>
    - PLAT-01..04/TEST-01 production evidence is complete.
    - Diagnostics never contain marker payloads.
  </acceptance_criteria>
  <failure_modes>
    - Private storage exceptions are expected optional limitations.
    - Any topology/browser discrepancy blocks Phase 2; no fallback implementation is authorized.
  </failure_modes>
  <done>Production local-only topology is either proven everywhere or explicitly blocked through ADR-001.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Execute the complete UX-01 visual and accessibility matrix</name>
  <read_first>
    - Every Visual Acceptance, keyboard/focus, responsive, motion and contrast check in 01-UI-SPEC.
    - UX-01 acceptance/edge cases.
  </read_first>
  <files>SeeQLite/tests/e2e/visual-accessibility.spec.ts, SeeQLite/tests/e2e/visual-accessibility.spec.ts-snapshots</files>
  <behavior>
    - Light and dark run at 375×812, 768×1024, 1024×768 and 1440×900; initial and readiness-success screenshots match committed baselines.
    - At 200% zoom for 375 and 1024 in both themes, body scrollWidth never exceeds clientWidth and all actions/status remain reachable.
    - Forced-colors active preserves named controls, meaningful borders/focus and status labels; reduced-motion reports no nonessential animation/transform/pulse/rotation/smooth scroll.
    - Keyboard-only performs skip link → sample/open → readiness → reset with logical focus; axe reports no serious/critical issues for initial, required-missing and readiness states in both themes at each viewport.
  </behavior>
  <action>
    Build a data-driven Playwright matrix over exact themes/viewports/states. Use deterministic system/font/animation setup, mask only elapsed-time text, and commit per-engine baseline screenshots for Chromium; Firefox/WebKit execute semantic/overflow/axe/keyboard assertions without pixel baselines. Add programmatic token/contrast checks for every UI-SPEC pairing, body overflow checks, 200% browser zoom through CSS pixel scaling appropriate to the engine, forcedColors emulation, reducedMotion emulation, visible focus assertions and minimum target sizes. Do not reduce the matrix to a manual checklist.
  </action>
  <verify>
    <automated>cd SeeQLite &amp;&amp; npm run test:e2e -- --project=chromium --grep "UX-01 matrix" &amp;&amp; npm run test:e2e -- --project=firefox --grep "UX-01 matrix" &amp;&amp; npm run test:e2e -- --project=webkit --grep "UX-01 matrix"</automated>
    <assertions>
      - Matrix enumerates 2 themes × 4 viewports × initial/readiness; screenshot count matches declared cases.
      - Separate 200% zoom, forced-colors, reduced-motion, keyboard and axe assertions execute.
      - No serious/critical axe finding or page horizontal overflow.
    </assertions>
  </verify>
  <acceptance_criteria>
    - Every UX-01 criterion has executable evidence, not prose-only review.
    - Visual baselines use exact production artifact and self-hosted fonts.
  </acceptance_criteria>
  <failure_modes>
    - Font/theme nondeterminism is fixed at harness setup; baselines are not widened with broad thresholds.
    - Engine unable to emulate a feature gets a targeted equivalent assertion and documented evidence, never a skip.
  </failure_modes>
  <done>UX-01 is fully covered by deterministic visual, accessibility and interaction automation.</done>
</task>

<task type="auto">
  <name>Task 3: Record bundle/performance baselines and DataDuck regression</name>
  <read_first>
    - STACK dependency loading/baseline guidance and SUMMARY release gates.
    - DataDuck scripts and Phase 1 type-smoke contract.
  </read_first>
  <files>SeeQLite/scripts/check-seeqlite-bundle.mjs, SeeQLite/tests/e2e/performance.spec.ts, SeeQLite/package.json</files>
  <action>
    Report raw/gzip shell, CSS, fonts, probe/database workers and WASM separately; fail on initial CodeMirror/React Flow/ELK/type-smoke code, Worker1/Promiser/main-thread SQLite, root-relative or remote runtime assets. Record environment plus shell/worker/WASM readiness, fixed-query distribution, request count and long tasks without telemetry. Run DataDuck unit/build/bundle on Node 24. Phase 2 preflight consumes the 01-06 summary and may proceed only when topology is PASS and ADR-001 is absent.
  </action>
  <verify>
    <automated>cd SeeQLite &amp;&amp; npm run check:bundle &amp;&amp; npm run test:e2e -- --project=chromium --grep "Phase 1 baseline" &amp;&amp; cd ../dataduck &amp;&amp; npm test &amp;&amp; npm run build &amp;&amp; npm run check:bundle</automated>
    <assertions>
      - Baseline separates asset families and records environment/sample distribution.
      - DataDuck stays green; no production telemetry.
    </assertions>
  </verify>
  <acceptance_criteria>
    - Initial bundle excludes future features/type smoke and preserves sibling app.
    - Summary exposes machine-readable `topology: PASS|BLOCKED` and ADR path when blocked.
  </acceptance_criteria>
  <failure_modes>
    - Noisy timings remain distributions, not false limits.
    - Bundle attribution or DataDuck regression blocks exit.
  </failure_modes>
  <done>Phase 1 ends with measurable artifact and explicit PASS/BLOCKED handoff.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-01-19 | Information disclosure | capture/attachments | mitigate | unique-marker assertions; metadata-only diagnostics. |
| T-01-20 | Tampering | visual/artifact evidence | mitigate | production build, deterministic matrix/baselines, no retries/skips. |
| T-01-21 | Elevation | worker/CSP topology | mitigate | emitted same-origin workers; negative capture; ADR hard stop. |
| T-01-22 | Denial | bundle/main thread | mitigate | separated budgets, long-task/readiness evidence, one DB worker. |
| T-01-SC | Tampering | CI lock/runtime | mitigate | npm ci, exact pins, Node24, DataDuck regression. |
</threat_model>

<verification>
Run full three-engine production suite, UX matrix, bundle/performance checks, Pages verifier and DataDuck regression. Require zero hidden skip/retry and either topology PASS with no ADR-001 or BLOCKED with complete Proposed ADR-001.
</verification>

<success_criteria>
- All six Phase 1 requirements have executable production evidence.
- UX-01 matrix is complete across every requested mode.
- Phase 2 has an unambiguous PASS/BLOCKED preflight source.
</success_criteria>

<output>
Create `01-06-SUMMARY.md` with `topology: PASS|BLOCKED`, browser/privacy/UX matrix, screenshot inventory, bundle/performance and DataDuck evidence. Do not create a branch or commit.
</output>
