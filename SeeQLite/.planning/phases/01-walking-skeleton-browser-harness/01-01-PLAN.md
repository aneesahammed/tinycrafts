---
phase: 01-walking-skeleton-browser-harness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - SeeQLite/package.json
  - SeeQLite/package-lock.json
  - SeeQLite/index.html
  - SeeQLite/tsconfig.json
  - SeeQLite/tsconfig.type-smoke.json
  - SeeQLite/vite.config.ts
  - SeeQLite/vitest.config.ts
  - SeeQLite/playwright.config.ts
  - SeeQLite/src/type-smoke/approved-libraries.ts
  - SeeQLite/tests/e2e/readiness.spec.ts
autonomous: true
requirements:
  - TEST-01
must_haves:
  truths:
    - "The strict Node 24/Vite/React/TypeScript project installs reproducibly and builds for a relative subpath."
    - "Approved later libraries typecheck through type-only imports but are absent from the runtime entry graph."
    - "The production-shaped readiness journey exists as a failing browser test before the engine slice is implemented."
  artifacts:
    - path: "SeeQLite/src/type-smoke/approved-libraries.ts"
      provides: "Compile-only imports for CodeMirror, React Flow, ELK and resizable panels"
    - path: "SeeQLite/tests/e2e/readiness.spec.ts"
      provides: "Walking-skeleton happy-path browser specification"
      contains: "Run readiness check"
  key_links:
    - from: "SeeQLite/tsconfig.type-smoke.json"
      to: "SeeQLite/src/type-smoke/approved-libraries.ts"
      via: "noEmit strict typecheck isolated from runtime entry"
      pattern: "approved-libraries"
  prohibitions:
    - "No type-smoke module imported by index.html, main.tsx, App.tsx, worker, or another runtime module."
    - "No remote runtime, analytics, component kit, router, global state dependency, broad skipLibCheck, or guessed package identity."
---

<objective>
Create the reproducible strict project/toolchain and the first RED production browser journey.

Purpose: Implement D-01 and verify the approved dependency/type surface without pulling later features into the runtime bundle.
Output: Exact-pinned scaffold, strict configurations, compile-only dependency smoke, lockfile, and failing readiness E2E.

Scope rationale: These 10 files are one cohesive scaffold slice because dependency pins, strict compiler/build/test configuration, compile-only compatibility proof and the first production E2E must agree before any runtime feature is implemented.
</objective>

## Phase Goal

**As a** SeeQLite implementer, **I want to** start from a reproducible production-shaped project and failing user journey, **so that** later slices build against verified contracts.

<execution_context>
@/Users/aneesahammed/.codex/gsd-core/workflows/execute-plan.md
@/Users/aneesahammed/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@SeeQLite/AGENTS.md
@SeeQLite/.planning/PROJECT.md
@SeeQLite/.planning/REQUIREMENTS.md
@SeeQLite/.planning/ROADMAP.md
@SeeQLite/.planning/research/STACK.md
@SeeQLite/.planning/research/SUMMARY.md
@SeeQLite/.planning/phases/01-walking-skeleton-browser-harness/01-CONTEXT.md
@SeeQLite/.planning/phases/01-walking-skeleton-browser-harness/01-UI-SPEC.md
@SeeQLite/.planning/phases/01-walking-skeleton-browser-harness/01-SKELETON.md
@index.htm
@dataduck/package.json
@dataduck/vite.config.js
</context>

<artifacts_produced>

| Artifact | Symbol/contract | Consumers |
|---|---|---|
| `package.json` / `package-lock.json` | exact-pinned Node 24 dependency and script contract | Every later plan and CI |
| `tsconfig.type-smoke.json` / `src/type-smoke/approved-libraries.ts` | isolated strict compile-only dependency proof | Later editor/graph/layout plans |
| `playwright.config.ts` / `tests/e2e/readiness.spec.ts` | production-shaped RED readiness journey | Plans 01-02, 01-05 and 01-06 |

</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold a buildable artifact and write the readiness journey RED test</name>
  <read_first>
    - `SeeQLite/.planning/research/STACK.md` sections “Recommended Stack”, “Testing and Quality Tooling”, “Proposed Dependency Declaration”, and “Source Assessment”.
    - `SeeQLite/.planning/phases/01-walking-skeleton-browser-harness/01-CONTEXT.md` decisions D-01, D-02, D-07, D-08.
    - `dataduck/package.json` and `dataduck/vite.config.js` for repository conventions, while preserving SeeQLite's stricter exact pins and TypeScript boundary.
  </read_first>
  <files>SeeQLite/package.json, SeeQLite/package-lock.json, SeeQLite/index.html, SeeQLite/vite.config.ts, SeeQLite/playwright.config.ts, SeeQLite/tests/e2e/readiness.spec.ts</files>
  <behavior>
    - The built page loads under `/seeqlite/` and exposes the SeeQLite heading without console or request failures.
    - The test then fails at the first missing user capability: the `Open SQLite database`/`Try sample database` workbench and readiness result are not yet present.
    - The E2E target is the production build, never the Vite development server.
  </behavior>
  <action>
    Per D-01, create an npm project with `type: module`, Node `>=24`, strict scripts for `dev`, `typecheck`, `test`, `test:coverage`, `build`, `preview`, `test:e2e`, and `check:bundle`. Exact-pin the approved dependencies and dev dependencies from STACK.md; omit `lucide-react` because the approved UI-SPEC locks project-owned inline SVG. Resolve the intentionally unpinned React/Node declaration packages through official npm metadata during execution, verify React 19 and TypeScript 7 compatibility, then write exact versions to `package.json` and the committed lockfile. Treat STACK.md's exact package/source table as the legitimacy evidence; abort on a package name, owner, repository, or registry mismatch rather than selecting a similarly named package.

    Configure Vite with `base: './'`, `assetsInlineLimit: 0`, ES2022, manifest and sourcemaps, deterministic asset/chunk naming, `optimizeDeps.exclude` for the SQLite package, and no production proxy. Configure Playwright's web server to build and serve a path-shaped artifact whose page URL is `/seeqlite/`; define Chromium, Firefox, and WebKit projects but run Chromium for the RED gate. Create a deliberately buildable static SeeQLite heading in `index.html` so the RED test fails on missing product behavior, not a server/build/import error. The test must specify the complete sample → readiness action → semantic result journey and fail at the first absent UI assertion.
  </action>
  <verify>
    <automated>cd SeeQLite &amp;&amp; npm ci &amp;&amp; npm run build &amp;&amp; ! npm run test:e2e -- --project=chromium --grep "opens the bundled sample and runs the readiness check"</automated>
    <assertions>
      - Build exits zero and `/seeqlite/` returns the buildable SeeQLite heading.
      - Playwright output is an assertion failure for a missing named workbench/readiness element, not a build, server, browser-launch, console, request, or asset-path failure.
      - `package.json` contains no caret, tilde, git URL, file dependency, postinstall script, runtime CDN, or package absent from the researched stack/UI decisions.
    </assertions>
  </verify>
  <acceptance_criteria>
    - RED E2E names the complete walking-skeleton behavior and fails for the expected missing behavior.
    - `npm ci` is reproducible from the lockfile under Node 24.
    - Vite's relative base and Playwright's `/seeqlite/` URL are explicit.
  </acceptance_criteria>
  <failure_modes>
    - If TypeScript or declaration packages are incompatible, isolate and record the exact compiler error; do not enable application-wide `skipLibCheck` or guess another major.
    - If package registry metadata disagrees with STACK.md ownership, stop before installation and mark the plan blocked on package legitimacy.
    - If the RED test fails because the server cannot serve the subpath, correct the harness before proceeding; infrastructure failure is not an accepted RED state.
  </failure_modes>
  <done>A reproducible scaffold builds and the production-shaped readiness E2E is red for exactly the intended missing user behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Compile approved later libraries without runtime inclusion</name>
  <read_first>
    - `SeeQLite/.planning/research/STACK.md` TypeScript/toolchain spike and approved CodeMirror, React Flow, ELK, and resizable-panel APIs.
    - Installed package declarations and `SeeQLite/vite.config.ts` runtime entry graph.
  </read_first>
  <files>SeeQLite/tsconfig.json, SeeQLite/tsconfig.type-smoke.json, SeeQLite/vitest.config.ts, SeeQLite/src/type-smoke/approved-libraries.ts, SeeQLite/package.json</files>
  <behavior>
    - Strict no-emit compilation imports representative public types/functions from CodeMirror SQL, React Flow, ELK and resizable panels.
    - Runtime build manifest/chunks contain no type-smoke entry or reachable editor/graph/layout implementation.
    - Any declaration workaround is adapter-local and documented with upstream source; application-wide `skipLibCheck` stays false.
  </behavior>
  <action>
    Add an isolated `tsconfig.type-smoke.json` whose only source is `approved-libraries.ts`. Use type-only imports and compile-only representative contracts for CodeMirror SQLite configuration/completion, React Flow custom node/edge props, ELK layout options/results, and resizable-panel separator props. Add `typecheck:deps` to the normal typecheck pipeline. Do not import this file from runtime code or configure Vite as an entry. Extend the build/bundle assertion to fail if its name or later feature packages are reachable from the initial shell.
  </action>
  <verify>
    <automated>cd SeeQLite &amp;&amp; npm run typecheck:deps &amp;&amp; npm run build &amp;&amp; ! rg -l "approved-libraries|@xyflow/react|elkjs|@codemirror/lang-sql|codemirror" dist/assets/*.js</automated>
    <assertions>
      - All four approved later libraries compile under strict TypeScript.
      - Runtime entry graph stays free of those features until their owning phases.
      - No global declaration suppression is enabled.
    </assertions>
  </verify>
  <acceptance_criteria>
    - Dependency compatibility spike is executable and repeatable.
    - Type-smoke artifacts never ship in the runtime bundle.
    - Package pins/ownership remain those audited in STACK.md.
  </acceptance_criteria>
  <failure_modes>
    - Declaration incompatibility blocks scaffold completion and is isolated before application code depends on it.
    - A package appearing in runtime chunks fails the build gate rather than being accepted as unused weight.
  </failure_modes>
  <done>The approved future dependencies compile while the initial runtime remains free of their code.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Package registry → lockfile | Dependency identity and ownership are supply-chain inputs. |
| Compile-only dependency surface → runtime graph | Type verification must not accidentally ship later features. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-01-01 | Tampering | `package.json`/lockfile | mitigate | Exact pins, npm lockfile, researched source ownership, no install scripts or alternate registries. |
| T-01-02 | Denial of service | dependency graph | mitigate | Type-only smoke project and runtime bundle exclusion checks. |
| T-01-SC | Tampering | npm installs | mitigate | STACK.md source assessment is the package audit; any identity mismatch blocks installation. |
</threat_model>

<verification>
1. `cd SeeQLite && npm ci && npm run typecheck && npm run typecheck:deps && npm run build` exits zero.
2. `npm run test:e2e -- --project=chromium --grep "opens the bundled sample"` remains RED only at missing product behavior.
3. Production output contains no type-smoke entry or CodeMirror/React Flow/ELK runtime code.
</verification>

<success_criteria>
- The strict project and approved dependency type surface build reproducibly.
- The scaffold exposes one behaviorally correct failing production E2E target for later shell/worker plans.
- No out-of-scope engine, persistence, router, analytics, component-kit, or remote-runtime behavior was introduced.
</success_criteria>

<output>
Create `SeeQLite/.planning/phases/01-walking-skeleton-browser-harness/01-01-SUMMARY.md` with artifacts, exact dependency pins, RED-test evidence, commands, and any isolated declaration workaround. Do not create a branch or commit.
</output>
