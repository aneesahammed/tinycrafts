---
phase: 01-walking-skeleton-browser-harness
plan: 04
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - SeeQLite/public/theme-init.js
  - SeeQLite/src/main.tsx
  - SeeQLite/src/App.tsx
  - SeeQLite/src/platform/capabilities.ts
  - SeeQLite/src/platform/module-probe.worker.ts
  - SeeQLite/src/platform/theme.ts
  - SeeQLite/src/components/AppHeader.tsx
  - SeeQLite/src/components/CapabilityGate.tsx
  - SeeQLite/src/components/FileOpenWorkbench.tsx
  - SeeQLite/src/styles/tokens.css
  - SeeQLite/src/styles/app.css
  - SeeQLite/tests/component/capability-gate.test.tsx
  - SeeQLite/tests/component/theme.test.tsx
autonomous: true
requirements: [PLAT-04, UX-01]
must_haves:
  truths:
    - "Supported users see the approved focused TinyCrafts opener in equal light/dark themes."
    - "Missing required capability is named before file intake; optional storage/offline loss remains non-blocking."
    - "Module-worker support is proven by a Vite-emitted same-origin worker under the production CSP, never a blob/data worker."
  artifacts:
    - { path: "SeeQLite/src/platform/capabilities.ts", provides: "required/optional capability report", exports: ["CapabilityReport", "checkCapabilities"] }
    - { path: "SeeQLite/src/platform/module-probe.worker.ts", provides: "same-origin module-worker handshake probe" }
    - { path: "SeeQLite/src/styles/tokens.css", provides: "approved TinyCrafts theme tokens" }
  key_links:
    - { from: "SeeQLite/src/platform/capabilities.ts", to: "SeeQLite/src/platform/module-probe.worker.ts", via: "new Worker(new URL(...), {type:'module'}) handshake", pattern: "module-probe\.worker" }
    - { from: "SeeQLite/src/App.tsx", to: "SeeQLite/src/components/CapabilityGate.tsx", via: "file workbench rendered only for ready/optional-degraded report", pattern: "CapabilityGate" }
  prohibitions:
    - "No Blob URL, data URL, cross-origin worker, file input on required failure, remote font/runtime, raw HTML, or component-local design values."
---

<objective>
Render the approved responsive TinyCrafts workbench and enforce an honest same-origin capability gate before file intake.

Purpose: Implement D-04 through D-06 independently of database-engine work.
Output: theme bootstrap/tokens, semantic shell/opener, exact capability classifier, emitted module-probe worker, and component tests.

Scope rationale: These 13 files are one cohesive user-facing gate because the inherited visual shell, theme prepaint, capability classifier, emitted probe and their component states must ship as a single consistent boundary before file intake appears.
</objective>

<execution_context>
@/Users/aneesahammed/.codex/gsd-core/workflows/execute-plan.md
@/Users/aneesahammed/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@SeeQLite/AGENTS.md
@SeeQLite/.planning/REQUIREMENTS.md
@SeeQLite/.planning/phases/01-walking-skeleton-browser-harness/01-CONTEXT.md
@SeeQLite/.planning/phases/01-walking-skeleton-browser-harness/01-UI-SPEC.md
@SeeQLite/.planning/phases/01-walking-skeleton-browser-harness/01-01-SUMMARY.md
@index.htm
</context>

<artifacts_produced>
- `CapabilityReport` with checking/ready/required-missing/optional-limitation branches.
- Same-origin module-probe worker asset with nonce handshake and deterministic termination.
- Stable TinyCrafts token/theme/focus/responsive shell inherited by later phases.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Render the strict TinyCrafts shell and prepaint theme</name>
  <read_first>
    - Phase 1 UI-SPEC tokens, spacing, typography, layout, focus, motion and copy.
    - `index.htm` visual tokens only; exclude its remote fonts and analytics.
    - UX-01 edge cases.
  </read_first>
  <files>SeeQLite/public/theme-init.js, SeeQLite/src/main.tsx, SeeQLite/src/App.tsx, SeeQLite/src/platform/theme.ts, SeeQLite/src/components/AppHeader.tsx, SeeQLite/src/styles/tokens.css, SeeQLite/src/styles/app.css, SeeQLite/tests/component/theme.test.tsx</files>
  <action>
    Implement exact approved light/dark tokens, bundled Inter/JetBrains Mono, 36px grid, spacing/type roles, semantic landmarks, skip link, status region, focus ring, contained overflow, breakpoints and reduced motion. Add same-origin prepaint `theme-init.js` reading only `seeqlite-theme`, validating `light|dark`, falling back to system and catching storage errors. React theme state uses the same key and tracks system changes only without explicit preference. Render no remote asset, analytics, raw HTML or database-derived persistent metadata.
  </action>
  <verify>
    <automated>cd SeeQLite &amp;&amp; npm run test -- tests/component/theme.test.tsx &amp;&amp; npm run typecheck &amp;&amp; npm run build</automated>
    <assertions>
      - Token values exactly match UI-SPEC; no component-local hex/spacing/radius/breakpoint.
      - Theme storage denial/invalid value falls back without alert or flash.
      - Runtime assets are same-origin.
    </assertions>
  </verify>
  <acceptance_criteria>
    - Shell meets UX-01 semantic/theme/focus/responsive contracts; exhaustive visual evidence executes in 01-06.
    - First paint resolves theme without visible flash.
  </acceptance_criteria>
  <failure_modes>
    - At 200% zoom document reflows rather than clipping.
    - Forced colors retains system borders/focus; reduced motion removes transforms/pulse/rotation.
  </failure_modes>
  <done>The build exposes the approved stable workbench shell in both themes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gate intake with an emitted same-origin module worker probe</name>
  <read_first>
    - PLAT-04 and D-04/D-06.
    - UI-SPEC CapabilityGate/FileOpenWorkbench/StatusNotice/focus copy.
    - Vite worker URL pattern in research STACK.
  </read_first>
  <files>SeeQLite/src/platform/capabilities.ts, SeeQLite/src/platform/module-probe.worker.ts, SeeQLite/src/components/CapabilityGate.tsx, SeeQLite/src/components/FileOpenWorkbench.tsx, SeeQLite/src/App.tsx, SeeQLite/src/styles/app.css, SeeQLite/tests/component/capability-gate.test.tsx</files>
  <behavior>
    - Worker, WebAssembly, same-origin module-worker handshake, BigInt, File/arrayBuffer, structured clone and transferable buffer each independently classify required failure.
    - localStorage/service-worker absence classifies optional limitation and leaves Open enabled.
    - Required failure renders named approved copy and no file input in DOM.
    - Probe timeout/error terminates worker/listeners and reports module-worker missing once.
  </behavior>
  <action>
    Implement pure injectable classifiers plus a bounded runtime handshake with `new Worker(new URL('./module-probe.worker.ts', import.meta.url), { type: 'module' })`. The emitted worker echoes a random non-sensitive probe token; enforce same-origin resolved URL, 1-second timeout, one settlement, listener removal and termination. Do not construct Blob/data workers. Render checking/ready/required/optional states and the exact opener/drop/picker/sample/trust copy; picker cancellation restores focus and produces no alert.
  </action>
  <verify>
    <automated>cd SeeQLite &amp;&amp; npm run test -- tests/component/capability-gate.test.tsx &amp;&amp; npm run build &amp;&amp; test "$(find dist/assets -name '*module-probe*worker*.js' | wc -l | tr -d ' ')" -ge 1</automated>
    <assertions>
      - Required branches have no file input; optional branches retain enabled Open.
      - Built probe URL is same-origin/relative and no `blob:`/`data:` construction exists.
      - timeout/error leaves zero probe workers/listeners.
    </assertions>
  </verify>
  <acceptance_criteria>
    - PLAT-04 is executable under the same production worker/CSP path as the application.
    - Opener follows UI-SPEC copy, focus and bounded filename/drop behavior.
  </acceptance_criteria>
  <failure_modes>
    - CSP/missing probe asset becomes named required failure, not blank UI.
    - Transfer probe failure detaches no user/file data and cleans up.
  </failure_modes>
  <done>File intake is available only after the same-origin module-worker and other required capabilities pass.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-01-13 | Elevation | module probe | mitigate | bundler-emitted same-origin URL, narrow CSP, no blob/data/cross-origin worker. |
| T-01-14 | Denial of service | probe lifecycle | mitigate | timeout, once-settlement, listener removal, terminate. |
| T-01-15 | Information disclosure | theme/capability state | mitigate | UI metadata/non-sensitive tokens only; no file data. |
| T-01-SC | Tampering | dependencies | mitigate | exact existing lockfile; no new package. |
</threat_model>

<verification>
Run theme/capability component suites, strict typecheck and production build. Confirm emitted probe asset, no Blob/data worker string in source/bundle, required/optional branches, focus/live regions and zero leaked probe resources.
</verification>

<success_criteria>
- UX shell and PLAT-04 capability behavior match locked UI/CONTEXT decisions.
- The module-worker check exercises the production same-origin CSP path.
- No file input exists until all required checks pass; optional failure remains non-blocking.
</success_criteria>

<output>
Create `01-04-SUMMARY.md` with emitted probe path, token/theme evidence, capability matrix and exported symbols. Do not create a branch or commit.
</output>
