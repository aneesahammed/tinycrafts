---
phase: 01-walking-skeleton-browser-harness
plan: 05
type: execute
wave: 3
depends_on: [01-02, 01-04]
files_modified:
  - SeeQLite/src/components/FileOpenWorkbench.tsx
  - SeeQLite/src/components/ReadinessSlice.tsx
  - SeeQLite/src/App.tsx
  - SeeQLite/src/styles/app.css
  - SeeQLite/public/sample.sqlite
  - SeeQLite/tests/component/readiness.test.tsx
  - SeeQLite/tests/e2e/readiness.spec.ts
autonomous: true
requirements: [PLAT-01, PLAT-03, TEST-01]
must_haves:
  truths:
    - "A user opens sample/local SQLite, runs fixed readiness SQL through the real worker, sees `ready=1`, resets and repeats."
    - "Failures/reset invalidate the old epoch, keep source bytes unchanged and restore an actionable opener."
  artifacts:
    - { path: "SeeQLite/src/components/ReadinessSlice.tsx", provides: "fixed readiness action/result/recovery UI" }
    - { path: "SeeQLite/tests/e2e/readiness.spec.ts", provides: "complete real-WASM happy/recovery path" }
  key_links:
    - { from: "SeeQLite/src/components/ReadinessSlice.tsx", to: "SeeQLite/src/engine/database-client.ts", via: "open/readiness/reset typed commands", pattern: "runReadiness|terminate" }
  prohibitions:
    - "No editable SQL, main-thread SQLite, arbitrary statement, database persistence, or source mutation."
---

<objective>
Connect the approved opener to the proven worker and complete the smallest user-visible open-to-query slice.

Purpose: Make the Phase 1 RED E2E green without broadening into the Phase 2 lifecycle or Phase 3 editor.
Output: bundled sample, local/sample open, fixed readiness action/result, reset/reopen UI and component/E2E coverage.
</objective>

<execution_context>
@/Users/aneesahammed/.codex/gsd-core/workflows/execute-plan.md
@/Users/aneesahammed/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@SeeQLite/AGENTS.md
@SeeQLite/.planning/REQUIREMENTS.md
@SeeQLite/.planning/phases/01-walking-skeleton-browser-harness/01-UI-SPEC.md
@SeeQLite/.planning/phases/01-walking-skeleton-browser-harness/01-02-SUMMARY.md
@SeeQLite/.planning/phases/01-walking-skeleton-browser-harness/01-04-SUMMARY.md
</context>

<artifacts_produced>
- `ReadinessSlice`: fixed action, semantic bounded table, status/error/recovery/reset.
- `sample.sqlite`: same-origin synthetic sample matching smoke fixture notes.
- Green readiness E2E using actual module worker and official WASM.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire sample/local open and fixed readiness result</name>
  <read_first>
    - Existing RED `tests/e2e/readiness.spec.ts`.
    - Phase 1 UI-SPEC FileOpenWorkbench/ReadinessSlice/state/focus copy.
    - Protocol/DatabaseClient exports from 01-02 and capability gate from 01-04.
  </read_first>
  <files>SeeQLite/src/components/FileOpenWorkbench.tsx, SeeQLite/src/components/ReadinessSlice.tsx, SeeQLite/src/App.tsx, SeeQLite/src/styles/app.css, SeeQLite/public/sample.sqlite, SeeQLite/tests/component/readiness.test.tsx, SeeQLite/tests/e2e/readiness.spec.ts</files>
  <behavior>
    - Sample fetch/local File open occurs only after capability readiness and never changes URL/title/storage.
    - `Run readiness check` disables repeat activation and renders captioned positional `ready`/`1` plus elapsed/bounded metadata.
    - Filename is bounded 80 chars, `dir=auto`, React text only.
  </behavior>
  <action>
    Wire the opener to `DatabaseClient`, retain selected File only in ephemeral controller ownership, transfer a fresh ArrayBuffer, and load the bundled sample from the same origin. Render exact opening/running/success/error copy and semantic table. Expose only the fixed readiness command. Component tests use a typed client seam; E2E must use real WASM/worker and committed bytes.
  </action>
  <verify>
    <automated>cd SeeQLite &amp;&amp; npm run test -- tests/component/readiness.test.tsx &amp;&amp; npm run test:e2e -- --project=chromium --grep "opens the bundled sample and runs the readiness check"</automated>
    <assertions>
      - No worker/engine route mock in E2E.
      - Semantic result and live status are role/name asserted.
      - Source hash is unchanged and no database marker enters persistent surfaces.
    </assertions>
  </verify>
  <acceptance_criteria>
    - User completes sample/local open → fixed query → one bounded result.
    - Main thread remains responsive and handle-free.
  </acceptance_criteria>
  <failure_modes>
    - Picker cancellation is silent; sample/read failure keeps opener and focus.
    - Old-epoch result cannot publish after a new open.
  </failure_modes>
  <done>The walking-skeleton happy path is green through real SQLite WASM.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete deterministic reset, failure and reopen behavior</name>
  <read_first>
    - UI-SPEC readiness failure/reset/focus/live-region rules.
    - DatabaseClient terminate/epoch behavior and reducer states.
  </read_first>
  <files>SeeQLite/src/components/ReadinessSlice.tsx, SeeQLite/src/App.tsx, SeeQLite/tests/component/readiness.test.tsx, SeeQLite/tests/e2e/readiness.spec.ts</files>
  <behavior>
    - Reset terminates current worker, clears file/result, restores opener and focuses Open.
    - Reset during open/run settles once and all late responses are ignored.
    - Failed readiness offers `Reopen database` only when retained File exists; one explicit reopen recreates epoch.
  </behavior>
  <action>
    Implement reset/reopen through termination and new generation, never uncertain connection reuse. Preserve only approved UI state, announce state once, and prevent repeat/late result publication. Add crash/readiness-failure/reset-during-run/double-reset tests with listener/worker counters returning to baseline.
  </action>
  <verify>
    <automated>cd SeeQLite &amp;&amp; npm run test -- tests/component/readiness.test.tsx &amp;&amp; npm run test:e2e -- --project=chromium --grep "reset|reopen"</automated>
    <assertions>
      - Reset focus and zero-worker baseline are asserted.
      - Late old-epoch result never appears.
    </assertions>
  </verify>
  <acceptance_criteria>
    - Failure/reset/reopen paths remain actionable and local-only.
    - Pending work settles exactly once; source remains immutable.
  </acceptance_criteria>
  <failure_modes>
    - Reopen failure keeps opener and retained SQL-independent UI; no retry loop.
    - Cleanup exception cannot show Ready.
  </failure_modes>
  <done>The smallest user slice is repeatable and recoverable without stale state.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-01-16 | Information disclosure | file/result UI | mitigate | ephemeral File, bounded DTO/text, no network/storage/log/URL. |
| T-01-17 | Repudiation/race | reset/reopen | mitigate | request IDs, epochs, once-settlement, stale ignore. |
| T-01-18 | Denial | repeated open/reset | mitigate | one worker, explicit terminate, resource counters. |
| T-01-SC | Tampering | dependencies | mitigate | no new dependency. |
</threat_model>

<verification>
Run component tests, strict typecheck, production Chromium readiness/reset/reopen E2E, source-hash/resource assertions and privacy-marker scan.
</verification>

<success_criteria>
- The real open → fixed SELECT → bounded result → reset/reopen journey passes.
- No database-derived network/persistence and no source change occurs.
- All generation/race/failure paths remain deterministic.
</success_criteria>

<output>
Create `01-05-SUMMARY.md` with user flow, component/real-WASM evidence, source hash and lifecycle counters. Do not create a branch or commit.
</output>
