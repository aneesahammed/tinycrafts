---
phase: 04-er-understanding
plan: 04
type: execute
wave: 3
depends_on: [04-01, 04-02]
files_modified:
  - SeeQLite/src/er/ErCanvas.tsx
  - SeeQLite/src/er/ErCanvas.test.tsx
  - SeeQLite/src/er/TableNode.tsx
  - SeeQLite/src/er/RelationshipEdge.tsx
  - SeeQLite/src/styles/er-canvas.css
  - SeeQLite/src/er/ErWorkspace.tsx
  - SeeQLite/src/er/er-view-preference.ts
  - SeeQLite/src/er/er-view-preference.test.ts
  - SeeQLite/tests/e2e/er-layout.spec.ts
autonomous: true
requirements: [ER-03]
must_haves:
  truths:
    - "Within budgets, lazy React Flow renders canonical nodes/edges with search/select/pan/zoom/fit/explicit Arrange; list-only remains primary on any budget/failure."
    - "Only the last Diagram/Relationships choice persists as a small versioned UI preference; no DB identifier, node ID, position, viewport, or relationship data persists."
    - "Preference storage denial/corruption falls back to session/default without blocking ER; mobile first use defaults Relationships."
  artifacts:
    - { path: "SeeQLite/src/er/ErCanvas.tsx", provides: "lazy bounded React Flow viewer", exports: ["ErCanvas"] }
    - { path: "SeeQLite/src/er/er-view-preference.ts", provides: "versioned view-only preference", exports: ["loadErViewPreference", "saveErViewPreference"] }
  key_links:
    - { from: "SeeQLite/src/er/ErWorkspace.tsx", to: "SeeQLite/src/er/graph-limits.ts", via: "budget result chooses canvas or list-only before lazy import", pattern: "assessGraphScope" }
    - { from: "SeeQLite/src/er/ErCanvas.tsx", to: "SeeQLite/src/er/layout/layout-client.ts", via: "Arrange/schema/subset layout only", pattern: "createLayoutClient|arrange" }
    - { from: "SeeQLite/src/er/ErWorkspace.tsx", to: "SeeQLite/src/er/er-view-preference.ts", via: "persist only diagram/relationships", pattern: "ErViewPreference" }
  prohibitions:
    - "No node drag, animation, minimap, unbounded canvas, relayout on selection/theme/resize, or persistence of node IDs/positions/viewport/database affinity."
---

<objective>Attach the bounded lazy canvas and responsive view choice while persisting only a harmless view-mode preference.</objective>
<execution_context>@/Users/aneesahammed/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@SeeQLite/.planning/phases/04-er-understanding/04-UI-SPEC.md
@SeeQLite/.planning/phases/04-er-understanding/04-01-SUMMARY.md
@SeeQLite/.planning/phases/04-er-understanding/04-02-SUMMARY.md
@SeeQLite/.planning/phases/03-query-workspace/03-07-SUMMARY.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Render bounded canonical nodes/edges and named controls</name>
  <read_first>04-UI-SPEC Table Node/Relationship Edge/Canvas Controls; SeeQLite/src/er/graph-types.ts; SeeQLite/src/er/graph-limits.ts; SeeQLite/src/er/layout/layout-client.ts</read_first>
  <files>SeeQLite/src/er/ErCanvas.tsx, SeeQLite/src/er/ErCanvas.test.tsx, SeeQLite/src/er/TableNode.tsx, SeeQLite/src/er/RelationshipEdge.tsx, SeeQLite/src/styles/er-canvas.css</files>
  <behavior>Lazy only in allowed Diagram mode; canonical IDs/count; node preview dimensions/priorities; child→parent/self/cycle/parallel/unresolved exact; named search/zoom/fit/arrange; no drag/minimap/animation; selection sync without focus theft; list remains mounted.</behavior>
  <action>Write RED component tests. Map canonical bounded scope to React Flow adapters only after `assessGraphScope` allows it. Implement exact node/edge/legend/control/theme/reduced-motion contracts, one initial layout per schema/subset and explicit Arrange only; Fit never relayouts. Preserve full pairs in list/detail and use list-only for budget/failure.</action>
  <verify><automated>cd SeeQLite &amp;&amp; npm run test -- src/er/ErCanvas.test.tsx &amp;&amp; npm run typecheck</automated></verify>
  <acceptance_criteria>No canvas over any budget; canonical counts/semantics; controls accessible; forbidden triggers/features absent; list survives.</acceptance_criteria>
  <failure_modes>Chunk/layout invalid result chooses exact Retry/list-only state; no auto loop/partial diagram.</failure_modes>
  <done>Canvas is a bounded faithful enhancement to Relationships.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist only last ER view with safe fallback</name>
  <read_first>SeeQLite/.planning/phases/04-er-understanding/04-CONTEXT.md D-33; SeeQLite/.planning/phases/04-er-understanding/04-UI-SPEC.md responsive ER layout; SeeQLite/AGENTS.md persistence rules; SeeQLite/.planning/phases/03-query-workspace/03-07-SUMMARY.md storage adapter patterns</read_first>
  <files>SeeQLite/src/er/er-view-preference.ts, SeeQLite/src/er/er-view-preference.test.ts, SeeQLite/src/er/ErWorkspace.tsx</files>
  <behavior>Record exactly version+view (`diagram`|`relationships`); no DB/node/edge/position/viewport/search. Invalid/corrupt/denied/quota falls back memory/default. Mobile first use Relationships; over-budget/failure always Relationships regardless saved Diagram; later successful allowed diagram may restore preference.</behavior>
  <action>Write RED storage schema/failure tests. Implement one project-owned versioned key containing only view mode and a session fallback. Integrate responsive defaults/overrides exactly; inspect serialized record to prohibit all database-derived fields. Do not reuse query-history record or persist layout positions despite earlier research suggestion; revised locked scope permits only this small UI choice.</action>
  <verify><automated>cd SeeQLite &amp;&amp; npm run test -- src/er/er-view-preference.test.ts &amp;&amp; npm run typecheck</automated></verify>
  <acceptance_criteria>Exact two-field record; all storage failures nonblocking; no derived identifier/position; responsive/budget overrides correct.</acceptance_criteria>
  <failure_modes>Unknown version/value resets. Any extra field/database keying blocks completion.</failure_modes>
  <done>Only harmless last-view preference persists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove responsive canvas/list scope and failure recovery</name>
  <read_first>04-UI-SPEC responsive/scope/failure/copy; SeeQLite/src/er/ErWorkspace.tsx; SeeQLite/src/er/RelationshipList.tsx; SeeQLite/src/er/ErCanvas.tsx</read_first>
  <files>SeeQLite/src/er/ErWorkspace.tsx, SeeQLite/tests/e2e/er-layout.spec.ts</files>
  <behavior>0/1/25/75 full; 150/500 subset; extreme parallel/dense/list-only; exact scope; rapid Arrange/stale/crash/timeout/replacement local; responsive modes/focus/theme/reduced motion; preference fallback; Query/List always usable.</behavior>
  <action>Add RED production E2E before workspace wiring. Exercise node/edge/payload/DOM boundary and failure fixtures, layout races, view persistence/storage denial, 375/768/1024/1280/1600, 200% zoom, themes, axe, network/console, and Query independence. Assert no whole/partial canvas when budget says list-only.</action>
  <verify><automated>cd SeeQLite &amp;&amp; npm run test:e2e -- tests/e2e/er-layout.spec.ts --project=chromium &amp;&amp; npm run typecheck</automated></verify>
  <acceptance_criteria>All states/counts/budgets/recovery/responsive/persistence/a11y exact; no stale positions or blocked Query/List.</acceptance_criteria>
  <failure_modes>Performance/failure never silently tightens truth or retries; list-only reason/action is explicit.</failure_modes>
  <done>Responsive ER canvas/list behavior is production-proven.</done>
</task>
</tasks>

<artifacts_produced>| Artifact | Contract |
|---|---|
| ErCanvas/node/edge | bounded visual adapter |
| er-view-preference | last view only |
| er-layout E2E | scope/failure/responsive evidence |
</artifacts_produced>
<source_audit>| Source | ID | Coverage | Status |
|---|---|---|---|
| REQ | ER-03 | Tasks 1–3 | COVERED |
| CONTEXT | D-31/D-32/D-33 | Tasks 1–3 | COVERED |
| UI-SPEC | canvas/responsive/failure | Tasks 1–3 | COVERED |
| GOAL | interactive bounded diagram | Tasks 1–3 | COVERED |
</source_audit>
<threat_model>| Threat | Category | Mitigation |
|---|---|---|
| T-04-04-01 | DoS | pre-render budgets/list-only/no repeated layout. |
| T-04-04-02 | Disclosure | preference allowlist excludes all derived data. |
| T-04-04-03 | XSS | bounded text adapters only. |
| T-04-SC | Supply chain | pinned React Flow; no install/minimap. |
</threat_model>
<verification>Run tasks then E2E all three engines, budget/failure/preference/axe/theme/responsive/query independence.</verification>
<success_criteria>ER-03/D-31/32/33 pass at canvas/UI layer and persistence contains view only.</success_criteria>
<output>Create `SeeQLite/.planning/phases/04-er-understanding/04-04-SUMMARY.md`.</output>
