---
phase: 04-er-understanding
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - SeeQLite/src/er/graph-limits.ts
  - SeeQLite/src/er/connected-subgraph.ts
  - SeeQLite/src/er/connected-subgraph.test.ts
  - SeeQLite/src/er/layout/layout-types.ts
  - SeeQLite/src/er/layout/layout-graph.ts
  - SeeQLite/src/er/layout/layout-graph.test.ts
  - SeeQLite/src/er/layout/layout.worker.ts
  - SeeQLite/src/er/layout/layout-client.ts
  - SeeQLite/tests/integration/layout-worker.spec.ts
autonomous: true
requirements: [ER-03]
must_haves:
  truths:
    - "Whole graph is allowed only when nodes≤75, edges≤600, encoded layout payload≤2 MiB, and projected canvas DOM≤4,000 elements."
    - "Any independent node/edge/payload/DOM budget breach selects list-only mode; it never renders a misleading partial diagram."
    - "Otherwise >75 nodes selects deterministic nearest connected≤75; ELK runs only in a disposable correlated worker."
  artifacts:
    - { path: "SeeQLite/src/er/graph-limits.ts", provides: "independent canvas/layout budgets", exports: ["GRAPH_LIMITS", "assessGraphScope"] }
    - { path: "SeeQLite/src/er/connected-subgraph.ts", provides: "deterministic bounded BFS", exports: ["selectConnectedSubgraph"] }
    - { path: "SeeQLite/src/er/layout/layout-client.ts", provides: "generation/token correlated worker", exports: ["createLayoutClient"] }
  key_links:
    - { from: "SeeQLite/src/er/connected-subgraph.ts", to: "SeeQLite/src/er/graph-limits.ts", via: "scope assessed before layout/React Flow allocation", pattern: "assessGraphScope" }
    - { from: "SeeQLite/src/er/layout/layout-client.ts", to: "SeeQLite/src/er/layout/layout.worker.ts", via: "bounded payload with generation/layoutToken", pattern: "generation|layoutToken" }
  prohibitions:
    - "No main-thread ELK, >budget partial canvas, unbounded show-all, custom layout settings, node mutation, or automatic retry loop."
---

<objective>Establish independent graph budgets, deterministic connected scope, and a bounded disposable ELK worker before UI rendering.</objective>
<execution_context>@/Users/aneesahammed/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@SeeQLite/AGENTS.md
@SeeQLite/.planning/phases/04-er-understanding/04-CONTEXT.md
@SeeQLite/.planning/phases/04-er-understanding/04-UI-SPEC.md
@SeeQLite/.planning/phases/04-er-understanding/04-01-SUMMARY.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Gate whole/subgraph/list-only scope by independent budgets</name>
  <read_first>SeeQLite/.planning/REQUIREMENTS.md ER-03; SeeQLite/.planning/phases/04-er-understanding/04-CONTEXT.md D-31; SeeQLite/.planning/phases/04-er-understanding/04-UI-SPEC.md Graph Scope; SeeQLite/.planning/research/PITFALLS.md PF-08; SeeQLite/src/er/graph-types.ts</read_first>
  <files>SeeQLite/src/er/graph-limits.ts, SeeQLite/src/er/connected-subgraph.ts, SeeQLite/src/er/connected-subgraph.test.ts</files>
  <behavior>Limits nodes75/edges600/layout payload2MiB/projected DOM4000. ≤all budgets→all. >75 nodes with otherwise viable chosen component→deterministic BFS≤75. Any edge/payload/DOM breach after scope→list-only exact reason. Extreme parallel FK/dense 75-node fixtures list-only. 150/500 never whole. Stable counts/ties.</behavior>
  <action>Write RED-first 0/1/25/75/76/150/500, disconnected, cycle, self, 601 parallel edges, dense edge, 2MiB boundary, and DOM projection cases. Implement `assessGraphScope()` and schema-order/ID-tied BFS. Assess node cap before building renderer arrays, then independently count in-scope edges, encoded layout bytes conservatively, and projected project-owned+React Flow DOM. Do not truncate edges into a misleading diagram; return `list-only` with exact budget reason.</action>
  <verify><automated>cd SeeQLite &amp;&amp; npm run test -- src/er/connected-subgraph.test.ts &amp;&amp; npm run typecheck</automated></verify>
  <acceptance_criteria>All four budgets exact-boundary tested; scopes deterministic; 601-parallel/dense/payload/DOM cases are list-only; no partial relationship diagram.</acceptance_criteria>
  <failure_modes>Unknown projection/encoding must fail conservative to list-only, not underestimate. Invalid anchor gives selection state.</failure_modes>
  <done>Graph work is bounded before any layout or canvas allocation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Normalize bounded ELK input/output without changing truth</name>
  <read_first>SeeQLite/.planning/phases/04-er-understanding/04-CONTEXT.md D-31/D-32; SeeQLite/.planning/research/ARCHITECTURE.md ER Graph; SeeQLite/src/er/graph-types.ts; SeeQLite/src/er/graph-limits.ts</read_first>
  <files>SeeQLite/src/er/layout/layout-types.ts, SeeQLite/src/er/layout/layout-graph.ts, SeeQLite/src/er/layout/layout-graph.test.ts</files>
  <behavior>Serializable bounded envelope carries generation/token/scope; complex self/cycle/parallel/unresolved supported; request/response each≤2MiB; IDs/count unchanged; positions finite; oversize/invalid output rejected to list-only.</behavior>
  <action>Write RED-first payload/complex-output tests. Define one layered ELK configuration and deterministic fixed node dimensions. Encode/check request and normalized response byte size, validate all returned IDs exactly once and positions finite, and preserve canonical relationships. No exposed settings or GraphModel mutation.</action>
  <verify><automated>cd SeeQLite &amp;&amp; npm run test -- src/er/layout/layout-graph.test.ts &amp;&amp; npm run typecheck</automated></verify>
  <acceptance_criteria>Both payload directions bounded; extreme parallel/dense rejected before transfer; exact canonical IDs/count; invalid output never publishes.</acceptance_criteria>
  <failure_modes>ELK exception/oversize/NaN/missing ID produces scoped failure/list-only; never partial positions.</failure_modes>
  <done>Layout adapter accepts/returns only validated bounded DTOs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Run ELK in a disposable generation-safe worker</name>
  <read_first>SeeQLite/src/er/layout/layout-types.ts; SeeQLite/src/er/layout/layout-graph.ts; SeeQLite/.planning/phases/01-walking-skeleton-browser-harness/01-02-SUMMARY.md module-worker pattern; SeeQLite/.planning/phases/04-er-understanding/04-CONTEXT.md D-32</read_first>
  <files>SeeQLite/src/er/layout/layout.worker.ts, SeeQLite/src/er/layout/layout-client.ts, SeeQLite/tests/integration/layout-worker.spec.ts</files>
  <behavior>Lazy worker only on layout intent; generation/token match; superseding Arrange/schema/scope terminates old; duplicate/stale/crash/timeout settles once and never publishes; no DB worker impact; list remains usable.</behavior>
  <action>Add RED-first real production layout-worker test. Implement app-owned module worker/client with once settlement, bounded envelope validation, timeout, and terminate/dispose. A new request replaces worker; there is no queued layout backlog or auto-retry. Assert worker URL/subpath/chunk and database worker/query independence.</action>
  <verify><automated>cd SeeQLite &amp;&amp; npm run test:e2e -- tests/integration/layout-worker.spec.ts --project=chromium &amp;&amp; npm run typecheck</automated></verify>
  <acceptance_criteria>ELK absent main thread; all race/failure outcomes isolated; bounded tokenized result only; list/query remain usable.</acceptance_criteria>
  <failure_modes>Chunk/worker failure becomes list-only Retry state; stale/duplicate cannot announce or position.</failure_modes>
  <done>Bounded ELK layout is an isolated disposable enhancement.</done>
</task>
</tasks>

<artifacts_produced>| Artifact | Contract |
|---|---|
| graph-limits.ts | node/edge/payload/DOM gates |
| connected-subgraph.ts | deterministic ≤75 BFS |
| layout worker/client | bounded correlated ELK |
</artifacts_produced>
<source_audit>| Source | ID | Coverage | Status |
|---|---|---|---|
| REQ | ER-03 | Tasks 1–3 | COVERED |
| CONTEXT | D-31/D-32 | Tasks 1–3 | COVERED |
| RESEARCH | PF-08 | Tasks 1–3 | COVERED |
| GOAL | performant bounded graph | Tasks 1–3 | COVERED |
</source_audit>
<threat_model>| Threat | Category | Mitigation |
|---|---|---|
| T-04-02-01 | DoS | independent node/edge/payload/DOM caps and list-only fallback. |
| T-04-02-02 | Spoofing/Tampering | generation/token/ID/finite/bounds validation. |
| T-04-SC | Supply chain | exact-pinned ELK, worker-only, no installs. |
</threat_model>
<verification>Run unit and real worker tests across three engines; inspect independent budgets, extreme fixtures, worker/chunk/race/query independence.</verification>
<success_criteria>ER-03/D-31/D-32 pass at engine/layout boundary; no over-budget diagram can instantiate.</success_criteria>
<output>Create `SeeQLite/.planning/phases/04-er-understanding/04-02-SUMMARY.md`.</output>
