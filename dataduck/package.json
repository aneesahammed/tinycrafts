{
  "name": "dataduck",
  "version": "0.1.0",
  "private": true,
  "description": "DataDuck is a local-first Parquet and CSV viewer PWA powered by DuckDB-WASM.",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "test": "vitest run --environment jsdom"
  },
  "dependencies": {
    "@duckdb/duckdb-wasm": "1.30.0"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.1.9",
    "jsdom": "^25.0.1",
    "sql-formatter": "^15.7.3",
    "vite": "8.0.10",
    "vitest": "^2.1.9"
  },
  "engines": {
    "node": ">=20.19.0"
  }
}
